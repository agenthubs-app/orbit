# iOrbit Confirmed Risk Register

This register contains code- or runtime-confirmed risks. Static candidates remain in `product-surface-risk-register.md` until reviewed.

| ID | Priority | Surface | Issue and trigger | User impact | Evidence | Status | Follow-up |
| --- | --- | --- | --- | --- | --- | --- | --- |
| AUTH-RETURN-001 | P0 | Login, Signup, Forgot Password | Anonymous navigation from `/app/agent` reached Login correctly, but Login helper links rendered `next=/home`, a non-canonical/non-production route. | Switching authentication mode could lose the original destination or redirect an authenticated request to a dead route. | Browser URL/DOM evidence in `runtime-verification-log.md`; source fallback was `defaultNext: "/home"`. | Fixed and desktop/mobile verified on 2026-07-27. | Keep loader, hydrated client query, proxy, and already-authenticated redirects on `normalizeOrbitAuthReturnPath`; retain targeted tests. |
| AUTH-RESET-001 | P0 | Forgot Password | Submitting a registered email only advanced local React state to a verification-code/new-password screen; no reset provider, API call, email, code, or persistence existed. | Users were told to expect a reset flow that never happened and could believe a password was changed. | `OrbitRealAccountAuth` pre-fix `setForgotStep(2)` branch; post-fix production Chrome evidence in `runtime-verification-log.md`. | Fixed and desktop/mobile verified on 2026-07-27. | Keep the explicit unavailable alert until a named reset provider, expiring token store, delivery audit, rate limit, and end-to-end test exist. |
| EVENT-REG-FALLBACK-001 | P0 | Event registration fallback | When the registration guide and event loader disagreed, the legacy fallback rendered editable textareas and a “confirm staged answers” button even though no handler or persistence existed and the page stated it would not write Profile data. | A user could enter participant-profile answers and press a confirmation control that saved nothing. | Pre-fix `RegistrationGuideForm`; source regression test; normal production registration path verified in `runtime-verification-log.md`. | Fixed on 2026-07-27; normal desktop/mobile registration path verified. | Keep mismatched/unverified event records read-only with explicit no-save copy and real recovery links; only `EventRegistrationWorkspace` may expose answer submission. |
| CONTACTS-FILTER-001 | P0 | Contacts list | The desktop “Ask” control and three hard-coded value-type chips were visible buttons with no handlers. The natural-language examples copied their full sentences into a plain substring filter, usually producing an empty result. | Users could press prominent search/filter controls without a meaningful result and could mistake local filtering for AI search. | Pre-fix `OrbitRealCardsList`; pure filtering tests; authenticated production Chrome evidence in `runtime-verification-log.md`. | Fixed and desktop/mobile verified on 2026-07-27. | Keep search explicitly local; derive value filters from route-provided `valueTags`; add AI semantics only through a real provider-backed search boundary with visible provenance/failure. |
| CONTACTS-PIPELINE-001 | P0 | Contacts pipeline | The route mixed source-backed contact cards with a hard-coded “AI Summit 2026” triage queue, reminders, and six dead actions. Stage buttons only changed component state and showed a success toast; the available stage API is explicitly mock/staged, read-only in live mode, and uses a different domain enum. | Users could believe relationship stages, summaries, tags, reminders, or email drafts had been saved when no authoritative write occurred. | Pre-fix `OrbitRealCardsPipelineView`; live provider and stage contract inspection; source regression test; authenticated production Chrome evidence in `runtime-verification-log.md`. | Fake/dead production affordances removed and desktop/mobile verified on 2026-07-27. | Keep the pipeline an explicitly read-only, source-backed classification until an authenticated write service maps contact/connection identity to domain `RelationshipStage`, persists to the live record store, returns readback/provenance, and has end-to-end tests. |
| STATE-RECOVERY-001 | P0 | Shared route-state boundary | `StateViewRecoveryAction` allowed actions without an `href` or callback and rendered them as buttons. The shared test explicitly locked this dead-button behavior across every route-state consumer. | Failure and pending states could present a prominent recovery control that did nothing on up to 24 direct consumers. | CRITICAL upstream impact analysis (24 direct callers, 49 upstream symbols, 16 flows); StateView render regression tests; production build TypeScript validation of all consumers. | Fixed on 2026-07-27. | Recovery actions must keep a real local `href`; use a separate typed callback control only after a concrete retry boundary exists. |
| ADMIN-ACTIONS-001 | P0 | Admin dashboard | Export, global AI matching, and team invitation buttons had no handlers or named service targets. The admin route-state contract also states the surface is source-reading only. | Operators could attempt work that never exported data, ran matching, or invited anyone. | Pre-fix `AdminDashContent`; admin-platform route boundary inspection; source regression test; production runtime evidence. | Removed and desktop/mobile verified on 2026-07-27. | Keep source metrics explicitly read-only until each action has an authenticated, scoped service and auditable result. |
| PARTY-FILTER-001 | P0 | Party recommendations | The visible Filter icon button had no handler even though every recommendation already carried a route-provided industry classification. | Users could not narrow recommendations through the advertised filter. | Pre-fix `PartyRecommendations`; route-derived industry regression test; authenticated production runtime evidence. | Fixed and desktop/mobile verified on 2026-07-27. | Keep options derived from `viewModel.recommendations`; do not introduce provider or AI claims for this local filter. |
| PLATFORM-MODERATION-001 | P0 | Platform admin | “More actions” was dead, while approve/reject controls only removed an item from React state and claimed publishing plus organizer notification without a write service. | Platform operators could believe an event was moderated, published, or communicated externally when nothing authoritative happened. | Pre-fix `OrbitRealPlatform.decide`; admin-platform route boundary explicitly prohibits these writes; source regression test; production runtime evidence. | Fake moderation removed and desktop/mobile verified on 2026-07-27. | Keep review and account access read-only until an authenticated moderation service persists decisions, publishing state, rejection reason, and notification receipt. |
| CONTACT-DETAIL-001 | P0 | Contact detail | The canonical detail page mixed route data with a hard-coded relationship stage, two-way value, timeline, next step, and fake status updates. | Users could mistake demo relationship intelligence and local-only status changes for persisted CRM data. | Pre-fix `OrbitRealCardConnection`; contact-detail source regression tests; authenticated production browser evidence in `runtime-verification-log.md`. | Fixed and desktop runtime re-verified on 2026-07-27. | Keep status read-only until a live authenticated stage writer returns readback and provenance. |
| CONTACT-DETAIL-RECOVERY-IDENTITY-001 | P0 | Contact detail failure state | A failed `/app/contacts/contact_003` detail route rendered “Retry contact detail” with the hard-coded `/app/contacts/demo-contact-1` target. | Retrying could silently switch the user from the requested live contact to a different demo identity. | Pre-fix authenticated browser DOM; exact-ID route-state regression test; post-fix production DOM shows `/app/contacts/contact_003`. | Fixed and desktop runtime verified on 2026-07-27. | Every entity-detail recovery action must derive its canonical href from the exact route ID; never borrow a fixture ID. |
| CONTACTS-GLOBAL-ACTIONS-001 | P0 | All Contacts surfaces | `OrbitCardsInteractions` intercepted unknown buttons and placeholder links, displayed generic success toasts, and injected a hard-coded email composer whose “send” only claimed demo success. | Any missed control could look successful without a domain operation, and contact pages could open a second fake composer. | MEDIUM impact analysis across five contact surfaces and two flows; source regression test. | Generic fallback and fake email sheet removed on 2026-07-27. | The shared layer now handles provenance popovers only; every business action must own an explicit handler or real link. |
| REGISTER-DUPLICATE-001 | P0 | `/app/register` | A duplicate registration prototype simulated AI extraction, generated a pass code locally, and claimed registration plus email delivery without calling the registration service. | Invitees could believe they were registered and received credentials when no record or email existed. | Pre-fix `OrbitRealRegister`; canonical `/app/events/[id]/register` live service inspection; redirect regression tests. | Duplicate prototype retired; legacy invite codes now resolve and redirect to the canonical workspace. | Keep one authoritative registration implementation; preserve explicit language/mock test parameters through the redirect. |
| CHAT-GET-WRITE-001 | P0 | Chat | The chat route loader accepted `action=record-local-reply` on a GET render and called `sendMessage`, creating a storage write from navigation. | Crawlers, refreshes, shared links, or prefetch could record a reply without an explicit mutation request. | Pre-fix `loadAppChatRouteViewModel`; LOW loader impact across Chat and Agent; source regression test. | GET write branch removed on 2026-07-27. | Future message recording must use an authenticated mutation endpoint with explicit confirmation and idempotency. |
| CHAT-COMPOSITION-001 | P1 | Chat | `/app/chat` loaded source-backed conversations but rendered the generic Agent launcher, so the selected participant and thread were absent from the initial Chat surface. A dead fixture-backed command center remained in the source tree. | Users could not review actual conversations on the Chat page, while static audits still found obsolete Mock behavior. | Loader-to-UI regression tests; dead-chain impact analysis; authenticated production browser evidence in `runtime-verification-log.md`. | Independent source-backed Chat workspace added, obsolete command center removed, and desktop runtime verified on 2026-07-27. | Keep the workspace read-only until a dedicated authenticated mutation flow with confirmation and idempotency is approved; mobile behavior remains component-tested rather than newly browser-verified in the fixed desktop viewport. |
| CHAT-SELECTION-IDENTITY-001 | P1 | Chat conversation query | `/app/chat?conversation=conversation_004` kept the first conversation as the current thread even while the requested conversation appeared in the list and URL. The Mock adjunct summary service also recognized only its first listed conversation. | A bookmarked or selected conversation could show another person's thread, summary, relationship context, and assist suggestion. | Pre-fix authenticated browser DOM; LOW loader and HIGH mock-service impact analyses; 20 focused tests; post-fix production URL, `data-selected-conversation`, `aria-current`, thread heading, and relationship context evidence. | Fixed and desktop runtime verified on 2026-07-27; unknown IDs now fail closed without substitution. | Keep list, thread, assist, summary, extraction, privacy, and Agent context bound to one exact conversation ID; add dedicated source-backed adjunct rules before presenting non-empty derived signals for a new Mock conversation. |
| EVENT-AUTO-MOCK-001 | P0 | Event detail | The canonical `event_001` route automatically selected a demo detail fixture even without an explicit Mock request. | Production navigation could silently replace unavailable live detail with a polished fixture. | Pre-fix `resolveEventDetailRouteMode`; route and hierarchy regression tests. | Fixed on 2026-07-27: the production page no longer accepts query-selected Mock mode. | Keep Mock injection at direct service-test boundaries only; production navigation must use server configuration and fail closed. |
| CONTACT-ACQUISITION-FALLBACK-001 | P0 | Contacts acquisition | A live acquisition failure offered “Continue in mock mode” as a normal recovery action. | Users could leave a failed live workflow and unknowingly create/review fixture drafts. | Pre-fix `AcquisitionStateBoundary`; live route regression tests. | Mock recovery removed; retry and back navigation remain. | Never offer Mock as production recovery for failed writes. |
| INBOX-MOCK-PIN-001 | P0 | Relationship inbox | The API hard-coded `createAsyncRelationshipConversationService("mock")` even in Live/Hybrid runtime, and proactive fixtures were merged into badges and alerts. | Production users could see invented conversations, unread counts, reminders, and locally staged “threads” as real relationship data. | CRITICAL impact analysis for top-nav badge/alerts; API mode tests; 24 targeted inbox tests. | Proactive GET removed; Live/Hybrid inbox now fails closed with 503; explicit Mock tests remain. | Implement a dedicated live async-conversation provider before enabling the drawer in Live; do not adapt incompatible chat fields with placeholders. |
| PARTY-CHECKIN-001 | P0 | Party check-in | The page waited 700 ms, marked the attendee checked in, and displayed a recorded time without a write service. | Attendees could believe venue check-in was persisted when nothing was recorded. | Pre-fix `OrbitRealPartyCheckin`; party source regression tests. | Fake success removed; page states that check-in is unavailable. | Re-enable only with authenticated event/attendee identity, idempotent write, readback, and staff verification. |
| PROFILE-LOCAL-SAVE-001 | P0 | Profile | Document extraction and profile save paths could fall back to local preview/state and show success without authoritative readback. | Users could believe extracted or edited profile data had been persisted. | Profile API boundary inspection; source regression tests; production build; authenticated save, API readback, and reload evidence in `runtime-verification-log.md`. | Fixed on 2026-07-27; profile save/readback desktop runtime verified, document extraction remains source/test verified. | Keep explicit failure states when storage or extraction providers are unavailable; do not auto-save extracted preview fields. |
| ACQUISITION-MERGE-AUTH-001 | P0 | Duplicate detection and merge APIs | Merge suggestions and apply preview were unauthenticated; the Live provider called the actor-scoped contact graph without an actor and therefore either exposed an unscoped draft graph or returned misleading empty results. Apply also accepted a caller-supplied `actorLabel`. | Anonymous or cross-account callers could probe relationship candidates, while audit identity could be spoofed and authenticated users could see a false empty state. | Pre-fix route/provider inspection; actor-isolation and spoofing tests; exact production GET/POST returned `401 UNAUTHORIZED` before provider access. | Fixed on 2026-07-27. Both APIs derive the actor from the server session; Live draft/contact reads share that actor; client audit identity is ignored. | Keep handler factories outside `route.ts`, retain cross-actor empty tests, and require a future destructive merge writer to add explicit confirmation, idempotency, readback, and compensation. |
| ACQUISITION-CAPABILITY-AUTH-001 | P0 | Contact acquisition APIs | Static route inspection found the contact-draft list, manual, QR, external import, referral, recommended-contact confirmation, and email/calendar relationship-signal endpoints without the shared authenticated actor boundary; their older Live providers also do not consistently scope records by actor. | An anonymous or different authenticated account could read or stage another account's acquisition candidates, or a write could be attributed to caller-provided identity. | Source inventory under `app/api/contact-drafts` and `app/api/relationship-signals`; central draft list/manual creation now have injectable authenticated handlers, actor-scoped Live factories, cross-actor memory-store coverage, and exact production 401 evidence for list/create/confirm. | Partially fixed on 2026-07-27; remains open for QR, external import, referral/recommended confirmation, and email/calendar signal boundaries. | Apply the same thin authenticated handler and actor-scoped Live provider pattern to every remaining capability; reject anonymous requests before parsing/provider access and retain cross-actor tests before marking fixed. |
| EVENT-CONTEXT-IDENTITY-001 | P0 | Event detail and Party | Event Detail rewrote `event_001` to `demo-event-1` for roster, recommendations, readiness, matches, encounter notes, review, opening lines, and intent writes. The composed page could combine one event record with another event's relationship context. | Users could see or write attendee context belonging to a different event. | Pre-fix fallback mapping; exact-ID route tests; authenticated production browser showed no demo fixture on `event_001`. | Fixed on 2026-07-27; every child capability receives the exact requested event ID and mismatches fail closed. | Keep event identity invariant across every composed capability and retain exact-ID regression coverage. |
| PARTY-IDENTITY-001 | P0 | Party, Check-in, Graph | Party pages did not pass the authenticated actor, used attendee IDs as contact-detail IDs, and synthesized group numbers, seats, and an access code suffix that no provider supplied. | One account could receive unscoped context, broken contact links, or apparently authoritative venue credentials and seating that never existed. | Pre-fix Party route/presenter/UI; LOW impact analysis across three pages; 20 targeted tests; authenticated production browser on all three routes. | Fixed on 2026-07-27: all routes require and propagate actor identity; contact links require a known contact ID; source-absent seating/pass/check-in stays unavailable. | Add these affordances only when an authenticated event-attendee/check-in provider returns source-backed assignments and readback. |
| EVENT-GET-WRITE-001 | P0 | Event detail / want-to-connect | The page forwarded `action=want-to-connect` and `targetContactId` from GET into a storage-writing loader. The POST endpoint was also unauthenticated, accepted caller-supplied actor identity, and defaulted the target to Priya. | Navigation could write intent; an anonymous caller could spoof the actor or write an unrelated/demo target. | Pre-fix page/route; handler tests for anonymous, spoofed actor, invalid target, and success; production anonymous POST returned 401. | Fixed on 2026-07-27: the loader is read-only; POST derives actor from session, verifies event ownership, and accepts only a source-backed event match target. | Add idempotency before exposing a production UI action; keep writes on authenticated POST and retain GET-no-write/401/target-validation coverage. |
| EVENT-CAPABILITY-AUTH-001 | P0 | Event child capability APIs | Source inspection found attendee, goal, readiness, match, encounter, and post-event endpoints that did not resolve an authenticated actor before reading or writing event context; adaptive registration generation also accepted anonymous requests. | Direct API callers could bypass protected pages, access or mutate event relationship data by record ID, or invoke registration generation without an account. | Shared owner-guard tests; adaptive participant-auth tests; exact production build returned `401 UNAUTHORIZED` for all 18 anonymous Event API inventory requests; authenticated Event Detail remained fail-closed for an event unavailable to the account. | Fixed on 2026-07-27. Owner-only child routes now authenticate and verify actor-scoped Event Detail before child storage; registration generation requires an authenticated participant. | Keep route files thin, retain injectable handler tests, and require any new Event child endpoint to declare owner, participant, organizer, or explicitly public access before running storage or providers. |

## Open baseline groups

The 57 full-suite failures are individually named and classified in `test-baseline-2026-07-27.md`. They remain open until each contract is either restored, deliberately replaced with an updated product decision and test, or proven to be an environment-only failure.
