# Web 开发派发与验收台账

2026-09-17：用户明确要求主代理控制各领域 session 派发任务直至实现。本轮从设计审阅进入实施；“派发成功”不等于编码、验收或部署完成。

## 当前基线

- 已核对W0最终交接及干净提交`161e9e6c4d1f314db90365a4d840718adfa12c70`，应用版本`02ec26f0`已部署；最终交接文档提交不意味着另一次部署。
- W1–W4已通过`git worktree list`确认HEAD均快进到161e9e6c。保留各自设计稿，不复制旧主目录代码覆盖新基线。
- 原主目录仍为e643572加用户未提交文件及本协调文档；不是发布源。
- 生产切换已完成，W0四类剩余治理未完成。后续产品开发不自动获得生产发布、云回归、付费模型或新云资源权限。
- 主独立集成树最新HEAD：`1f492f49679ee0d9190322a3d19cb6889de82224`，工作树干净、未部署。HEAD已含B1、活动F、W2 A+B及审计适配、R、B3/B5、W1及失败copy/P窄读取/全站Web导航资料门禁、严格推荐服务、未接线首页facts/跟进F窄读取/W5 A聚合action、W3 P1-24、readback测试事务边界适配及离线completion夹具路径修复。以下当前状态表优先于后文历史追加记录，精确验证证据保留在各次记录。

## 已批准的编码批次

- W1夹具A主集成`1f492f49679ee0d9190322a3d19cb6889de82224`，父a2f22cbc，严格两文件与源a822a8e4逐字一致（+153/-13）。主Node25/Tokyo及Node24.19/LA各17/17零skip、完整Web types0，日志`/tmp/orbit-main-fixture-a-{node25,node24,types}.log`；完整runtime旧DB/Node22/环境/清理guard不变。预impact入口LOW1直接/2总，路径const及test UNKNOWN由context/源码补证。force结束后113127nodes/258012edges，最终raw15symbols/2paths齐、关键helper/WEB_CWD/APP_CWD齐，无flags/空ID/missing/extra，`/tmp/orbit-main-fixture-a-detect-{full,summary}.json`；全图9778入口/8689callee/61walk/5depth限制保留。领域先前FTS失败/0symbols废弃，最终all/compare亦15/2完整；领域5负控制有效。主未重跑全量，不把a2全量53失败自动改成52。B type-only三文件已转独立树，C投影重构仍未批准。
- B4微秒真实PG四case证实shared rowToRecord的JS Date截断使严格CAS三次仍失败。主追加批准原四产品路径内TX专用只读metadata reader，parameterized workspace/collection/physicalID/owner（含null）绑定，保留created/updated及未变事实时间原始UTC精度；仍调用共享CAS，禁止外层pool/全局parser/自制CAS SQL。每retry重新读，missing/重复/非法/owner生命周期不符failclosed；每写实体额外1SELECT计入成本。父接入候选reader四case已success/1attempt，仅dirty中间证据，事实时间/真实并发/回滚及最终图待验。仅attempt数量有界，当前无生产statement/lock deadline，不声称总耗时或容量SLA已验，不自行新增生产预算。
- W2新a2索引的eventIdFor真实预impact为HIGH（1直接/3总、10关联流程），覆盖旧树LOW判断；风险已披露且不能以riskSharedAxes LOW冲销。原批准五代码测试路径继续TDD；来源字符串变化保留Bridge交接，P114日期/App和B4边界不变。
- W3新a2真实浏览器主要矩阵已完成：30截图/AX/hash，A窗口内签到POST200及刷新/唯一DB记录、future窗外禁用、B未报名Party拒绝且0membership/checkin；模型/task/generation/publication未调用。新增时区显示不一致及未报名错误copy泛化仅列未解决项，精确报告/证据索引和主目检待完成；不标六route整业务关闭。原域将仅停自有4613/55463并保留schema与证据。
- W4领域picker独立7/7、合并API/entry/scope/store/picker110/110零skip；原Luna继续可信analysis持久回执核验及Agent收口，三UI锁未释放。主未集成，先前A3 106/106/HTTP证据不等于整域通过。
- W1门禁主集成`a2f22cbc0f277519987c723f90c0bd8193d4dd33`（父77c46617），九文件与源a1218b08逐字相等。主13文件107/107零skip含自有Unix PG，两types0；基线真实incomplete tasks GET200对预期307为RED，应用后冷启动14HTTP/真实Chromium七步保存→权威GET→continue→跨tab刷新/换号通过，主实际目检最终截图。首次HTTP/browser并发使全局SQL trace串样，其成本断言弃用；随后browser关闭后的串行及全新Next冷启动串行各14通过。普通gate4、legacy5 SELECT，API/POST/static新增gate0，auth另计；旧页无网络即时撤销非目标。日志`/tmp/orbit-main-w1-gate-{tests,http-cold-serial,types,app-types}.log`，截图/HTTP/浏览器记录在`/tmp/orbit-main-w1-gate.ipF1lT`。force完成后raw123symbols/9paths齐/无flags空IDmissingextra、六关键函数存在；`/tmp/orbit-main-w1-gate-detect-{full,summary}.json`，图113120nodes/257997edges/860flows、全局9778入口/8689callee/61walk/5depth盲区保留。proxy/continue为UNKNOWN由真实Next补证，旧validator LOW1，共享resolver CRITICAL只读。主4620/UnixPG均停，仅删除本轮随机schema及合成账号（seed可重建），证据保留；next-env生成变化已精确恢复，未部署。
- B2原Luna max已获准完整12路径TDD/服务端/私有GET及UI接线；旧query/source/status/tag/value作为legacyBase，新q/stage/valueTag独立，旧base先完整query歧义拒绝再结构化筛选，Web统计基于base。tokens空且无base用SQL browse，其余窄projection服务器JS复用原matcher，不称关键词有界降本；SQL reader CRITICAL15/1直接/6流程、tokens/matcher/readContactsInput HIGH已披露。UI复用既有SessionContext仅作主体变化信号，SSR subject不是授权；ownerDigest与request scope/generation分离，render同步遮挡错owner/未确认身份，GET每次canonical re-auth，迟到结果丢弃，401/owner变化清页并有限refresh。同owner同scope503可保页；不依赖W4、不改Provider、不承诺跨tab无网络即时撤权。
- W3只读证实六活动route审计的历史-only pending断言与默认fresh-required断言冲突，不能仅补记录同时变绿。已派原领域以a2f22cbc新隔离树做六route真实本地浏览器/DB取证，只新增域CURRENT-RUNTIME-AUDIT.md；无模型/云/真实用户写入，无匹配结果不造graph正例。共享generator/测试/根输出尚未获编辑权，历史证据保留原版本；运行部分通过不关整业务。

- W5 A主集成`77c4661761f64e454e1c82da5d367664cb478e28`（父410be5），严格四新增路径与源64ad07fb逐字一致。主env-i＋外网禁止，Tokyo/LA各102/102零skip，全types0，diffcheck0；日志`/tmp/orbit-main-w5-a-{tokyo,la,types}.log`。预impact新增两入口UNKNOWN且源码无旧调用；resolver只读依赖CRITICAL18直接/29流程/14模块，不修改既有身份系统。force结束后最终raw354symbols/4paths齐，无flags/空ID/缺项/额外，五关键函数存在；`/tmp/orbit-main-w5-a-detect-{full,summary}.json`，索引中途0映射结果未接受。图113065nodes/257737edges/860flows，全局9774入口/8689callee/61walk/5depth盲区保留。未接UI/HTTP传输、未运行PG/云/App；新聚合9+R逻辑读数另计auth/旧home，不称实测整页成本。child测试缺有限timeout留后续维护；W4锁及B另审不变。

- a2f22cbc固定HEAD离线全量完成：4435项／4055通过／53失败／327跳过，312.827s，日志`/tmp/orbit-main-a2f22c-offline-suite.log`。逐失败名称比较410be5的53项完全相同、无新增或移除；其余旧基线/环境失败及跳过项不因此关闭。全量结束，解除主树本轮测试期间冻结。上一410be5为4400／4024／53／323，readback两项已从af978全量消失。
- 同一a2f22cbc清环境/外网禁止的Next16.2.9 webpack构建exit0：编译21.5s、TypeScript17.9s、静态50/50及路由trace完成，日志`/tmp/orbit-main-a2f22c-next-build.log`，构建后git clean。使用应用自带Node24.19.0、直接next build，不执行图片/CSS生成脚本；不是完整npm build流水线或Node22生产验证。仅本地QueueClient缺region默认iad1警告，无凭据/云队列激活/部署。W1新增只读调查契约两旧失败和机器路径fixture，W2新增只读调查详情映射旧失败，未授产品修改。
- B4同毫秒冲突已查明共享PG/memory CAS均要求candidate updatedAt严格大于expected。主明确批准仅域provider成功持久化版本采用max(now,current+1ms)，不改共享store或添加同timestamp专用SQL，不新增noop策略，不改变互动/笔记事实时间；重试重新读版本、非法/溢出failclosed、detail+industry同事务。固定时钟三真实PG原失败oracle必须保留并转绿；预impact/完整最终图仍由域执行。
- W1旧债调查实证fixture子进程status=null来自硬编码cwd的ENOENT，批准新a2树原Luna A两文件离线可移植性批（fixture模块URL目录、真实源码临时缺Appconsumer负例、5s/网络隔离不减），完整runtime旧DB/Node22/环境guard保留。后续B三文件type-only barrel及生成App index、强编译等价断言获批串行；重复三类型显式去重，不星号冲突、不改HTTP结构。C公开投影移出contract需要新单源同步边界，暂不批准14路径重构，原隐私规则和红测试保留。报告`/tmp/orbit-w1-baseline-debt-review.md`。
- W2实证旧来源断言取错字段，同时QR命名背景被替换为人名、当前writer模板未识别，通用displayText还裁掉未知标签的slash正文。五文件设计已主完整审阅并正式批准新a2树原Luna TDD：localizer、详情adapter、三个既有测试文件；可追加域SOURCE-LABEL-REPORT.md。精确系统模板/未知正文保留、met与关系分区、结构型事件身份不解析翻译文字，三语3×3重入；保留presenter隐私过滤，不涉及P114日期/B4或App实现。旧树impact LOW/UNKNOWN、两个生产文件与a2相同，新树仍须重查；共享API输出字符串变化由主最终Bridge交接。设计`/tmp/orbit-w2-qr-source-five-file-design.md`。
- B2来源统计窄例外获批：父真实PG16默认collation对İ/i的SQL lower去重与旧JS eventIdFor不等价。browse同条SQL额外返回授权/base范围内去重原始event_import sourceLabel，仅服务器按旧JS计算source统计，不下发标签。DB egress为O(page+distinct source labels+facets)，最坏来源各异仍O(N)，不宣称全browse常量有界；保留Unicode负例与10/100/1000成本证据，不新建tuple快路/字符特判/cache/schema。
- W5 I独立初读服务端批正式批准新a2树原Luna：四新文件home-dashboard-initial-route-service/actions及两initial测试，可新增域INITIAL-DESIGN.md；A/F冻结、不碰W4 UI锁。初读一次auth/resolver并行新A service与旧home保留区，手动刷新仍原A窄action。主发现deterministic registration get与原authorized list不等价（outerowner、legacy physicalID、重复排序），已修方案为原projected/canonical batch list只读lookup＋原gated get选择，冲突/损坏/缺映射registered:null；不复制validator/SQL、不改共享runtime/身份迁移。未知不能假false；语言走实际proxy头并测试。保留成本2batch＋Nenrollment＋M Published证明、旧home全读与shell费用；慢保留区仍会拖单response，不承诺流式。本批仅服务端/离线验证，UI接线仍待W4冻结与另批。
- B4追加批准同树测试依赖：逐字引入bb9af6b9的`tests/fixtures/w0-read-write-fixture.ts`，并适配已批真实双连接并发测试；不引入其余B0测试，不扩大产品边界。
- B2只读preflight证实Web搜索基于本地化展示结果，且Node25/Unicode17与PG16 NFKC有37个scalar反例。批准设计纠正为同一服务端解析语言进入SSR/API及cursor scope；先交最小完整设计与准确路径，尚未开SQL编码。不得特判37字符或把B1 lower认证当NFKC等价；B4不受此阻。
- W1真实Next旧基线已复现未完成资料的未知/app路由404而非引导307，作为真实HTTP RED；API健康路由SQL对照已记录，编码继续。W4发现picker pending第二页卸载未abort，仍由原Luna在原锁文件补正确性与精确覆盖，UI锁未释放。

| 领域 | 实施范围与约束 | 本地资源 | 当前状态 |
| --- | --- | --- | --- |
| W1 | 资料A+B/copy/P/全站Web资料门禁/离线fixture已验；type-only出口小批 | fixture a822冻结；contract-exports新树原Luna三路径 | fixture主Node25/24各17/17、types/raw已验并合入1f492f49；出口三文件实施；C投影重构未批准 |
| W2 | 原名片A+B已验；追加QR来源正文/三语重入/结构型事件边界五文件 | 旧57d5/P114稿冻结；新a2树准备原Luna | 名片主76/76＋90/90/types/raw已验；来源修复已批TDD，P114日期/App仍待决定且B4锁不变 |
| W3 | F/R/推荐/P1-24已主验；新增六活动route实际运行审计 | a2f22cbc新树、自有资源及新schema；取证后只停本人服务 | 六路由主要实测矩阵完成，报告/主核验待完成；时区/copy缺陷未解决，不改共享审计规则/产物，历史/App发布门不变 |
| W4 | A1联系人ID引用/入口显式发送；A2全局actor-scoped草稿；A3expected-actor；B版本正确性后再布局 | 4614；专用库orbit_web_w4_20260917 | A3/picker领域合并110/110＋6HTTP/DB无写，主未验；原Luna继续B可信回执/Agent收口，三UI锁未释放 |
| W0 | B1/B3/B5已主验；B4详情原子runner/CAS/tombstone；B2两层兼容的Web私有分页 | 各域隔离资源；主PG55478验后已停 | B4原Luna实施，局部单调版本及TX原精度metadata已批准；B2原Luna完整12路径含UI身份接线实施；不改App/schema/云队列 |
| W5 | 已验facts/F/A窄聚合；新I初读composition独立获批，UI B串行等待 | F树46e8、A树64ad冻结；I新a2树准备 | A主Tokyo/LA各102/102/types/raw已验；I原Luna四新文件＋域文档获批，旧home延迟入口由I提供；W4三个UI锁持续 |

上述库由各领域确认未占用后分别创建；不得清空既有库。依赖可按锁文件安装，不改lockfile；各树独立构建目录。不得用历史Preview做隔离测试。

浏览器QA还须隔离host：W1–W4分别使用`w1.localhost:4611`、`w2.localhost:4612`、`w3.localhost:4613`、`w4.localhost:4614`及各自AUTH_URL。不同端口本身不能隔离同host cookie；W3已实际遇到JWT secret冲突并切换专属host，不更改产品认证或清除其他任务cookie。

## 共享文件单一写入者

- W1：`orbit-profile-route-view-model.ts`页面VM、`account/orbit-real-account-auth.tsx`仅认证成功/callback导航。不动W0账号/认证服务。`profile-service-factory.ts`若确需调整，先报精确差异。
- W2：追加`contacts/new/batch2/page.tsx`仅标题三语接入；不动全局Provider、shared媒体/业务契约或后台writer。
- W3：`orbit-landing-route-view-model.ts`、`orbit-registered-event-route-view-model.ts`；追加`home/compose-app-home-from-previously-approved-mock-first-capabilities/home-route-view-model.tsx`仅去除canonical人数硬编码0及nullable兼容和测试。W5不并发写此文件。共享mapper HIGH（3直接调用/7影响符号）已向用户披露，必须覆盖详情/home/公开主办方消费者。
- W4：`agent/page.tsx`仅既有actorId/key，`orbit-product-href.ts`仅纠正q自动执行旧注释与相应测试；A2已移交`orbit-global-ask/orbit-ask-draft.ts`及批准的context/owner/global-ask局部状态给W4单一写入。主代理/W5新首页方案不得并发改`agent/page.tsx`或`orbit-real-agent.tsx`，需W4交付后接线。A3的AI handler仅可选expected-actor拒绝错scope，不改变身份授权。
- 所有路径均相对`repos/orbits/app/(app)/app`，领域PLAN中的其他精确文件继续有效；新申请由主代理审查后登记。

### 本轮追加批准与实际进展

- **chat-agent 合并冲突登记（2026-09-18）**：`origin/chat-agent` 在 29efb4c9 之后另有 sprint 0066（`7a87b89a`→`9c460d9a`，报名画像流），与主集成线（1f492f49）及 newui/foundation-batch-1 合并时 git 自动合并 `event-registration-workspace.tsx`，全 typecheck 0；但 0066 的 `RegistrationPortraitWorkspace` 在已登录非准入活动上**替换**旧问卷，W3-R `66087fff` 新增的 `tests/pages/event-registration-readback.test.tsx` 14 项（驱动旧问卷 Founders/Next 按钮）全部失败，`event-registration-workspace` 10/10、账号 scope 5/5、探索/资料模型与失败路径测试通过。这是两条线的产品语义冲突，未删断言、未回退 0066；需 W3 与 0066 负责人决定回执回读在画像流中的等价覆盖。`app-event-registration-guide` 13 项因环境 `ORBIT_EVENT_DATABASE_URL` 指向旧 Neon（配额耗尽）失败，与合并无关。

- **新 UI 基础批次 1（主代理自派）**：分支`newui/foundation-batch-1`，工作树`/Users/li/work/orbit-web-newui-foundation-20260918`，父`1f492f49`，三提交a4e357bd→cbe9c442→d867c694。探索页/报名工作区/资料页各抽一个纯模型（`events/explore-model.ts`、`register/registration-workspace-model.ts`、`profile/profile-save-model.ts`）＋独立单测，零行为变化；13 文件 110/110 零 skip、全 typecheck 0。预 impact：探索 5 符号 LOW；`matchesAdmissionApplicationReceipt` HIGH（工作区内 3 直接调用/2 流程，逐字搬迁，回读/账号 scope/工作区测试覆盖全部调用方）；`OrbitRealProfile` 图谱 UNKNOWN 经 rg 确认消费者为 `profile/page.tsx`＋5 测试。detect-changes compare 报 `saveProfile` CRITICAL/857 流程，rg 证实为同名碰撞（闭包仅本文件 2 处调用），未以 riskSharedAxes 冲销，如实登记；新文件符号在 compare 中仅列类型别名，新函数未被完整列出，不当完整图谱通过。未合入主集成树、未接新 UI、未部署。边界决定 A 见 NEW-UI-DECISION.md，接口表 §二 已更新。

- B1主集成`410be5d90944e4e1b72fbf3da325fd01a549cb55`（父236c202f），严格六路径与源4119c574逐字一致，父69f3＋bb9＋39b7等价。主C库分页30/30零skip（含真实微秒/歧义/独立证据/查询计划），19文件下游81/81，B0外置固定oracle直连主源码9/9；主两types0。父en_US也30/30，两locale独立SQLoracle说明未改产品排序。日志`/tmp/orbit-main-b1-{pagination-c-final,consumers,b0-oracles,final-types,app-types}.log`。最后增量图252漏buildPayload等实际函数，已拒绝并force；最终raw329symbols/6paths齐/4query关联flows/MEDIUM，无flags/空ID/缺项/额外，七关键函数逐名核对found；`/tmp/orbit-main-b1-final-force-detect-{full,summary}.json`。图112699nodes/257002edges/860flows，wholegraph预算盲区保留、不降级预编辑CRITICAL。随机schema0、主55478正常停，提交后clean；B4已明确释放原批准范围，B2依赖解除但精确SQL等价/路径充分性仍preflight，W2 P114日期/App决定与B4锁仍待。生产Unicode tuple/真实云/任意容量未验；未知tuple仍完整fallback，不称全面降本。已启动410be5合并版全量离线回归，完成前不变更此树。

- W1 [页面门禁方案](/Users/li/.codex/worktrees/197d/orbit/repos/orbits/docs/development/web-2026-09-17/W1/ONBOARDING-GATE-DESIGN.md)174行已完整主审，W0确认proxy和维护测试无锁，正式批准8代码测试＋设计md。新独立树基于236c202f、旧197d冻结；页面精确豁免与失败恢复按方案，匿名/API/POST行为不扩，已登录公开app页同样先补全。常见4/5、continue→目标8/10增量SQL接受为正确性首批且须实测；Next真实runtime导入、RSC/prefetch/语言与缓存无请求边界独立验收。W5已批新server action使“当前无use server”的固定旧源码事实即将过期，POST不受本页面gate约束，双方已同步。
- B1父bb9af6b9与69f3累计六路径已主精确应用，等最终测试修订；主19files81/81下游＋B0两query oracle9/9（在/tmp仅重定位imports指向主源码，不作为Web matcher证明）、两types0。分页30项29过1硬编码collation失败：主PG C/C顺序A,a,Ω，父en_US.UTF-8独立VALUES顺序Ω,a,A；产品fast/fallback都依数据库存储顺序，无产品错序证据。已批原Luna仅该test用独立3record SQL序列作oracle、固定identity集合及全部跨引擎/终止断言不减；禁止改SQL或guard。初次main URL scheme/host被既有精确guard拒绝，修正参数后才实际跑库。当前完整raw329symbols/6paths全映射、无flags/空ID/额外，四条query同名关联流程不冒称精确运行链；全图112699nodes/257000edges/860flows、9760入口/8689callee/61walk/5depth限制保留。B4/B2未释放，自有55478临时运行、随机schema0。
- W4 A3客户端拒绝闭环批准原owner hook局部invalidation通知，仍原已锁文件/测试，无新文件：仅失效后重验account/me、不接收新owner授权；当前owned request/actor/generation/session门与迟到A不干扰B必须用两hook真实挂载证明。监听同步失效旧generation，再异步verify，不双重请求、不伪造focus；不声称跨tab即时一致或原子鉴权。主图context未索引在途新hook为UNKNOWN，源码明确Agent/Provider两个消费者补证；领域须自身预impact及最终图检查。

- 主af978全量离线4373项4018通过/55失败/300跳过，377.565s；日志`/tmp/orbit-main-af978-offline-suite.log`。较42d041新出现的两项readback失败实证为B5新增事务runtime未被memory-store harness覆盖：旧161四项全过，af978原版2/4，生产配置失败关闭不回退。显式Luna max Mencius仅改单测试，主完整review后4/4＋types0，普通发送/续聊/重放/无权均保留0任务SQL/transaction断言；只shim tasks/reminder两个importer，不激活session GET分析report链。最终提交`236c202f`（+9/-1），无生产修改。主更新图后完整raw11symbols/1path映射齐、无flags/空ID/额外，`/tmp/orbit-main-readback-final-detect-{full,summary}.json`；初次索引进行中读到的8symbols不用作最终证据。全图9746入口/8731callee/62walk/5depth盲区保留。修复后未再跑全量，不将旧53失败或300skip视作已解决；真实PG补测证据按各批保留。main无运行中PG/Next。
- B1歧义修复精确阶段已批准：统一allContacts投影后仅一次完整includesText查询；unpaged在queryMatchedContacts上先拒歧义再结构化筛选；paged切页后拒歧义，owner全局facets保留，页外歧义使用contact stage＋既有pending。fallback用匹配对象identity Set，不退回旧简化searchText、不按domain ID吞物理重复。六真实PG oracle按父子交接执行，原6路径不扩大。
- W1门禁设计默认只针对Web导航/SSR；API 403/503作为另行决策选项，不擅改App/HTTP合同。GET/HEAD、Server Action POST、prefetch、客户端缓存和assets须分别说明，不能向所有API/资源无差别追加资料查询。

- 主将P0-03/P1-09全站首次资料门禁余项交原W1 Astra high做只读最小设计，固定主af978，不启动Luna编码；只允许新增W1/ONBOARDING-GATE-DESIGN.md。复用既有权威completeness/字段，核对layout/登录回跳/onboarding豁免、异常恢复、防循环和读取成本，不定义正式词表/新必填、不改W4 Provider或身份系统。最终全局协调仍归主/W5；没有放开新的共享文件写锁。

- W5首页接线设计已完整主审，[方案](/Users/li/work/orbit-web-home-facts-20260917/repos/orbits/docs/development/web-2026-09-17/W5/HOME-DASHBOARD-INTEGRATION-DESIGN.md)。A可独立先行：只新增home-dashboard-route-service.ts、home-dashboard-actions.ts、app-agent-home-dashboard-entry.test.ts及设计文档；基线主af978独立新树，原F冻结不动。无参action每次live/config workspace检查、auth/canonical解析，拒mock伪身份及workspace错配，失败零业务读取；只调用已验facts/W3，9+R已含推荐、auth/旧home另计。B必须等W4冻结三UI文件及真实owner/generation接口；刷新仅四源＋推荐，旧home保留区不重读/不冒称同步，非dashboard入口不附加新全集。未批准UI、云、共享契约或新身份gate。

- B5主集成`af9784f41a329d134778d5a0386ad5f31833b920`：12文件与固定源`bfff19849cc6c2ebaaad184bcb088ad3d66f1680`逐字一致（父a600＋d38e等价）。主独立本地核心5files55/55；关联14files82项81通过/1缺R2库跳过，随后在自有55478新库orbit_reminder_test完整旧PG组12/12零skip，其中含此前跳过父组及子测试，不简单相加冒称全suite通过。全types0、diffcheck0；日志`/tmp/orbit-main-b5-{core-tests,consumer-tests,legacy-pg,typecheck}.log`。Dalton固定f05..bfff无新blocking，schedule NOT_FOUND/typed basecreate/纯verifier及实际UPDATE后回滚oracle均审。main预impact factory CRITICAL33/5直接，domain service CRITICAL26/4直接，错误类MEDIUM53/14直接、verifier LOW2；queue File UNKNOWN由Next POST源码补证。force112392nodes/254395edges/862flows，raw939symbols/12paths全映射、148flows CRITICAL、无flags/空ID/缺项；`/tmp/orbit-main-b5-detect-{full,summary}.json`。wholegraph9746候选、8731callee、62walk、5depth上限保留。随机schema零残留，专属PG正常停止；未push/deploy/云脚本/真实消息/queue激活。公平性不是delivery SLA，纯站内试点不替代mixed/push旧路径。
- B1新增一致性finding已由父真实PG确认：无效connection引用的较新evidence进入graph/unpaged/detail，污染generatedAt；scope物理key读取须与最终合法domain evidence并集相交。另unpaged无query+sourceFilter错误延后全scope歧义检查；paged页外歧义contact在SQL/fallback任取canonical与回退contact的差异造成facets/filter分叉。主批准仅原6路径修复：unpaged保旧构图拒绝；paged页外统一contact自身stage＋既有pending规则，返回页仍拒歧义。不得恢复paged全scope拒绝、复制整套scope decoder或扩大到既有detail域/物理键问题。Noether源码推断与父PG确认分列，不称安全泄漏。

- W5 F主集成`0385ffba7d04b5496258ec0d9c2d78fce8859ae9`：六路径与源`46e8f46790b2e00491b3b70916a532dab0ad47cc`逐字一致。主Tokyo/LA各51/51、PG9/9零skip、默认真实configured链facts1SQL/旧tasks4次collection读取、types0、diffcheck通过，日志`/tmp/orbit-main-f-{tokyo,la,pg,default-chain,typecheck}.log`。主核对B1固定候选scope仍为contact owner OR actor connection，F严格JSON字符串差异已批准；未依赖B1未提交代码。预impact loader LOW1直接、followupLoaderFrom LOW1直接/2上游，其他mapper LOW；旧home函数引用边缺失以源码补证。force后111387nodes/250527edges/793flows；完整raw514symbols/6路径齐、无flags/空ID，`/tmp/orbit-main-f-detect-{full,summary}.json`。第一次在索引完成前读取的旧图仅映射2文件已拒绝，重跑完整结果方提交；全图9665候选/8799callee/61walk预算盲区仍保留。自有PG首次未传端口启动失败、未连接共享5432；显式55478后正常验证，随机schema零残留并已停止，未部署/未接UI。
- B1源69f3仍不放行：主实际reader证明Date.parse后toISOString把PG微秒压成毫秒，父真实PG六组合确认相邻微秒记录被跳过；证据`/tmp/orbit-main-b1-microsecond-proof.log`。原Luna同6路径修复，不改cursor版本。父另用1000独立evidence而非共享evidence的fixture确认百万semi-join工作与宽字典排序约50MB落盘；只读membership字典＋materialized窄scalar实验三mode完整rows与原版相等，query/empty约147ms、temp0，完整fallback仍有扫描/落盘。均待正式回归，不以实验标完成；B4共享锁继续。
- B5 c988已修真实schedule NOT_FOUND归类，但base create仍吞40001/40P01。主批准原8路径加reminder-plan-service、其test、verify-cloud-reminder-chain脚本及其test，严格12路径；只转换明确TARGET_NOT_OWNED领域错误，其余原样透传。脚本仅提取纯taskService.list authorizer供本地fake测试，禁止执行云脚本，不加配置/API/文件。共享factory/domain service CRITICAL风险已审，不扩大为全域重构。父已实际UPDATE cursor后同事务读到新seq/leased再抛错，回滚后cursor及wake恢复；需正式回归保留，不能称作服务端自然死锁。该批尚未主集成。
- W4 C3b2继续同Luna串行修复真实草稿持久化与旧history列表回退已确认revision。A3后B阶段获准仅客户端专用analysis展示分支：原分析请求＋可靠completed回执＋现有session GET的持久verification/sourceVersion/确切assistant:requestId正文均匹配才展示，普通无证据回复限制不放宽；POST本身没有版本不能伪称直接匹配。只本地fixtures、不调用模型、不改服务/API；W5文件锁不释放。

- B5增量`f05b3265`父75/75仍未接受：原SQLSTATE与generation-only两缺陷已修，主及Dalton再审发现schedule的真实`AppError(NOT_FOUND)`没有归一化为新目标拒绝类型。主实际helper证据`/tmp/orbit-main-b5-schedule-domain-proof-red.log`，父PG确认consumer抛错后intent仍leased；原Luna仅在B5 helper转换明确NOT_FOUND，不改personal-schedule、不吞DB/network/schema异常。缺失/异actor/失效occurrence及schedule SQLSTATE需补回归。cursor写故障原测试是SQL发送前JS异常，不能称服务端写失败，补实际UPDATE后事务失败验证。公平次数明确两版：旧eligible-history固定26×11是250/25/10/1/0，新持久cursor同fixture为250/34/2/0，均286；hot/cold实测两轮14/12，不冒称四轮。主2a684c62预impact factory CRITICAL33总/5直接，queue File UNKNOWN按Next POST源码补证，未应用此批。
- W3主验后仅停止其确切自有Next4613/PG55463；停前只读确认六场ID全在、无其他client，停后PG状态shut down。数据/目录/截图未删，恢复账本`/tmp/orbit-w3-services-stopped.json`、`/tmp/orbit-w3-services-recovery.md`；恢复脚本仅语法验证、未实际重启。域源码仍e07f6b92干净，其他服务未动。
- W3 P1-24源`e07f6b9219947642dc6ac1bd8e8d4745a00abeb3`已主独立集成`2a684c6261b7747623b00e53dcba89699b109a30`，精确三文件与源逐字一致。主实际目检最终03/04/05/06/07，地图声明不被卡片遮挡，窄屏双组、长标题和英文换行符合批准；主离线44/44零skip、全typecheck、diffcheck通过，日志`/tmp/orbit-main-p124-{tests,typecheck}.log`。预impact Explore LOW1直接、Map LOW1直接/2总，AppEventsPage十条关联流程保留；主索引标记落后一提交但对应P已暂存内容与实际HEAD一致，最终force110869nodes/249441edges/793flows。完整raw58symbols/3diff/3mapped、无flags/空ID/缺项，`/tmp/orbit-main-p124-detect-{full,summary}.json`；wholegraph仍有9640候选入口/8799callees/61walk预算盲区，不以0affected冒称无调用方。未部署，示意地图、头部约增120px与真实语言导航重置query/topic均明确保留。
- 2026-09-18继续复核：B5 `5055aaef`仍未验收。Dalton独立发现reschedule的target-owned catch吞掉SQLSTATE，父另确认consumeLeasedWake同根；原Luna须只映射真实领域not-owned，透传数据库/网络异常，实际target-read处验证40001整事务重试、40P01原错失败。generation-only过滤还会推进全局cursor并破坏公平性：父图谱/源码确认生产wake传完整intentId+planId+generation、repair不传generation，只有新增测试使用generation-only；主正式修订原设计为无intentId/planId却有generation时事务前拒绝、0SQL/0cursor/0publisher。原八路径内修复，不新增公开协议或忽略generation。真实SQL计量已取代早期10+N估计：空4、首次14/稳态12、mixed15/13、定点6/空5（含3个SET，不含BEGIN/COMMIT）；不能靠漏计维持旧数字。
- W5 F主独立源码/直接执行发现两项真实失败，已退原Luna六路径修复：SQL decoder归一化省略可选null后，loader二次校验误用raw必需键而拒绝正常结果；NULL owner但连接合法授权时，SQL布尔proof返回null导致整源拒绝。证据`/tmp/orbit-main-f-reader-review.ts`。要求真实loader往返和本地PG回归，分别保留raw输入与窄事实校验，不能通过删除校验或放宽授权解决。另指出重复组反复计算fingerprint的二次方成本；未冻结、未主集成。
- B1 `2e66`父77/77仍不足验收：forced unknown的Unicode `İstanbul`多页重复首条，SQL lower生成的cursor rank与JS matcher不等价；原Luna改为JS真实prefix rank结合SQL storage tuple边界。1000联系人且关联关系同比增长时新方案EXPLAIN约1494ms/空1465ms，旧候选约885/827ms，不能只凭RowsRemoved下降宣称性能改善；B1/B4共享锁继续保留，其他领域独立推进。
- W1 P源`0f5cd336`主独立集成`3e0bfa15`，五文件与源逐字一致。19文件158/158、零skip，含新parity/reader、实际CAS14、新strict recommendation/runtime22及profile编辑/homefacts消费者，全types通过；`/tmp/orbit-main-profile-p-{tests,typecheck}.log`。main60062c18预force后精确impact factories LOW2/4、mapper/readProfileGraph UNKNOWN，源码补普通load及mutation实际调用；final force110813nodes/249364edges/793flows，完整raw179symbols/5diff/5mapped、无flags/空ID/缺项，0flows仍有全图入口/callee/walk预算盲区。`/tmp/orbit-main-profile-p-detect-{full,summary}.json`。自有UTF8 socket无TCP，随机schema零残留、验后停止。仅正常读返回量降低，custom与mutation不变，未部署。
- 原全量249跳过已只读分类为47项已有主验、188项可候选本地补跑、14项需精确旧环境/attestation仍保留。只启动其中165项对应60文件白名单，不开放其他23前提或真实库存repair。专用UTF8 PG `/tmp/orbit-main-pg-supplement.ltLhf0/data`、127.0.0.1:55478、库orbit_main_supplement，无env/云凭据、外网禁止、文件串行；C组14文件实际117/117、0skip（含原67跳过），日志`/tmp/orbit-main-pg-supplement-c.log`。A/B组随后独立跑，混合repair文件排除真实库存用例，不改guard。数量按原skip与解锁后总测试分开，不能相加冒称全suite绿色。
- 上述60文件补跑收尾：A名片15文件59/59零skip；B活动30文件74项73通过/1失败零skip；混合repair另3通过/1明确真实库存guard跳过。合计254项252通过/1失败/1保留skip，日志`/tmp/orbit-main-pg-supplement-{a,b,b-filtered,c}.log`。唯一失败为owner迁移测试157行比较SQL `order by record_id`与JS `localeCompare`；本机PG16.12 UTF8/C排序对冒号/下划线次序不同，旧161e基线同文件2通过1同失败，相关源码零diff；不改期望或旧guard凑绿。随机schema零残留、专用55478实例已停，未触碰共享5432/云环境。原14特殊前提和另23候选仍未冒充已验。
- W2审计增量源`57d5a2c9`已主集成`60062c18`，仅正式audit测试与域REPORT两文件，逐字节对齐。主定向1/1、0skip、完整types、diffcheck通过；日志`/tmp/orbit-main-w2-batch-audit-{test,typecheck}.log`。主File impact UNKNOWN，29实体范围均不覆盖匿名callback647/663；fullraw2符号/2diff/1mapped无flags/空ID，保留匿名测试missing，不把LOW/0flows当作穷尽证明。原全量新增1失败已定向关闭，但没有重跑完整suite冒称全部绿色。
- W3 P1-24主已实际目检04–07桌面/390截图并核对context/源码，批准原Luna在client、现有view-switcher测试及AUDIT文档三路径内实现A–C：同源可见话题搜索、移动独立状态/话题滚动组、44px触控及正确aria-pressed、稳定内容/地图控制。D仅真实能力澄清：图中东京角标改“活动分布示意（非实际位置）”，位置计数改活动计数；不改哈希坐标、SDK/API，不宣称真实地图完成。长标题字号/封面密度保持，待真实交互与1440/390截图主验。
- B4内部原子runner设计及五个精确测试路径补充已正式批准（域design约183行），仍等待B1冻结释放业务文件。生产PG同事务重授权/physicalID解析；无runner有效live mutation零写fail-closed；测试自有串行copy-on-write harness不代替双真实PG并发证据。原业务4路径不扩，HIGH/CRITICAL及旧无mutationID/stale草稿边界保留。B5新候选08905312在父独立20/20后仍查出generation-only参数槽与sentinel碰撞，已退原Luna修正，继续禁止集成。
- 主集成`42d04159`清环境、Tokyo、禁止外部网络的全组合测试实际4278项：3975通过、54失败、249跳过（约412秒）；日志`/tmp/orbit-main-integrated-offline-suite.log`。36条缺数据库配置失败已逐项在专用本地PG补验通过：报名18/18，其他20项先14通过/6失败，canonical两文件修正测试连接配置后7/7。六项暂时失败的根因是base URL内search_path覆盖测试Pool.options，fixture与runner落在不同schema；pg连接解析源码与去除URL options后的复验共同证实，无产品修改。日志`/tmp/orbit-main-{registration-with-pg,pg-missing-config-rerun,canonical-pg-unscoped}.log`。剩余18非PG失败：17项在隔离基线161e9e6c复现（审计13、契约2、展示1、机器路径夹具1）；新增batch2审计1项由W2源57d5a2c9精确修正，主独立验收中。两个start隐藏入口实际是首次选择/追加照片，不能称作正反面数量。没有将54全称既有失败，也没有将分项通过称完整suite绿色；249跳过和旧债仍保留。
- W1新增独立P读取成本批已批准，仍原Astra high审阅＋Luna max编码。五文件：新`profile-actor-postgres-reader.ts`、原`profile-live-record-provider.ts`、新`tests/storage/profile-actor-postgres-reader.test.ts`、`tests/capabilities/profile-actor-postgres-parity.test.ts`、域`PROFILE-READ-DESIGN.md`。只优化正常configured provider外层两次actor范围SQL/字段投影；JSON身份保持严格类型、NULL owner与legacy recordId语义、原排序/删除态及invalid授权record的generatedAt贡献。不加LIMIT/索引/cache/shared API；reader不得进入mutation回调或共享options，原事务store读取/写入/receipt不变。真实本地PG parity、失败不全读fallback、outerreader不得越过事务、CAS/推荐回归及10/100/1000无关actor rows/bytes/plan为验收门；不承诺两次变一次或CPU有界。
- W2来源与日期待决方案已形成可持续档案：[P114-DESIGN.md](/Users/li/.codex/worktrees/5c7b/orbit/repos/orbits/docs/development/web-2026-09-17/W2/P114-DESIGN.md)。当前仅文档，未实施；包含v3+D精确契约及旧客户端发布边界，不因App代码合并而假定存量客户端已升级。
- W5 F窄facts读取已正式批准六文件内原Luna max编码：新`features/followups/storage/relationship-lifecycle-facts-reader.ts`，原`app/(app)/app/tasks/relationship-lifecycle-tasks.ts`、`agent/home-facts-route-service.ts`，新`tests/pages/app-home-facts-followup-reader.test.ts`、`tests/services/relationship-lifecycle-facts-reader-postgres.test.ts`及域`FOLLOWUP-READ-DESIGN.md`。仅home显式新reader，原Tasks/default provider不变；单SQL读取actor所有tasks及其引用连接/联系人，联系人必须自身owner或完整actor连接授权，task-only引用不授权。严格JSON身份、重复冲突fail closed、失败unavailable/count=null、不fallback全图；不取evidence集合/联系人私密详情，不新建索引/cache/连接/共享协议。不预截top3，不改W4 UI；跟进4→1只是逻辑读取，10/100/1000行/字节/执行计划与真实parity仍待验收。
- W1 copy源`f65500966051f3626fece7ed83df56ba1cc23c4a`四文件独立主集成`42d04159c2c8bc39acd3014df9042f15f5c82c4a`，无冲突且与源逐字节一致。主83/83、真实live未配置loader→StateView可见恢复文案SSR1/1、全typecheck及diffcheck通过；`/tmp/orbit-main-copy-{tests,visible,typecheck}.log`。预编辑精确impact CRITICAL直接1/总9，最终force raw7symbols/4diff/3mapped/0flowsLOW不冲销风险；唯一缺项匿名test有File＋22实体且hunk与range无交，`/tmp/orbit-main-copy-unmapped-ranges.json`及完整diff/实跑补证，原raw保留missing。raw无flags/空ID/额外路径；builder9634入口候选、8799callees、61walk预算限制保留。不重启PG/Next，不宣称浏览器或部署验收。
- W4 C3新增窄授权`agent/orbit-agent-dashboard.tsx`单一写入至冻结：仅可选受控briefText/onBriefTextChange与父级accepted/revision清稿，保留uncontrolled兼容；先RED证明owner核验遮罩卸载后恢复、拒绝保稿、新输入不被旧提交清除。不得改home/signals/appointments/推荐/布局。W5将消费W4冻结接口，不重复提升草稿。C2a领域独立16/16已过，C2b写队列继续。
- W3第二批P1-24进入只读产品审计：本人本地专属host/PG、当前截图桌面/窄屏、搜索/筛选/视图/空态证据，先目检再提出局部候选文件；未批准Luna或布局编码，F/R/推荐已交付代码保持冻结。不用W4标签页，不调用云或模型，不以旧截图/源码断言视觉缺陷。
- B5 P2提醒专属持久轮转设计经主完整审阅及Dalton Astra独立复审，正式批准原八文件内由原Hooke Luna max实施，不等于B5验收。单workspace保留cursor以`lastVisitedActorId`按明确环形ordinal推进；全SKIP LOCKED仍推进一次，targeted旁路，租约与cursor同事务；40001最多三次完整重试，40P01必须作为缺陷。保留600秒链、hasMore仅快照不忙循环、真空0DML、精确kind/key排除与损坏fail-closed。接受每SQL前单调时钟5s本地事务预算；在途SQL仍有5s statement限制，不宣称硬5s。只有源码实际可取得caller deadline时复用，不新增API/配置。单次10+N与最多三次累计分别计量，父真实PG并发/回滚/旧新单次delivery矩阵后再复审，CRITICAL风险未关闭。
- W2 P1-14只读v3已证明：首次仅填互动摘要当前依赖联系人updatedAt伪日期；改为空后provider丢记录、Web ACK拒绝而App隐藏摘要。因此不能只改读投影便标修复。主已异步请求用户选择保留无日期摘要并同步小范围App展示、要求显式日期或暂缓日期/App；未答前不实施A/B/App。cardNotes旧createdAt+Web隐藏也只算兼容止损，独立无日期来源投影仍设计中。B4服务/provider优先锁不变，AI纯生成器空互动语义问题交W4记后续独立小批，不混入A2。
- W5接线设计修订为真实dashboard入口意图完成后才读窄鉴权action，取消page对新facts和旧home预读；聊天直接进入不新增这些读取。复用W4 actor/epoch/generation gate，不通过router.refresh卸载Agent；当前仅设计，待W4冻结重审。W0核算12+R仅新成功分支逻辑读取，followup四读和profile两读仍全workspace取回后筛选，不是SQL/账单上限。精确reader下推与同请求真实复用继续只读设计，未批准UI或storage扩展。
- 用户明确批准W2本批私有名片预览的局部例外：首次显示明确加载状态，授权读取后由客户端生成真实缩略预览并渐显，不要求本批SSR LQIP。追加`contacts/new/batch2/ingest-v2-private-image.tsx`及`tests/pages/ingest-v2-private-image.test.tsx`（测试相对Web根）。同源凭据、固定尺寸、contain不裁字、decode gated/reduced motion、错误/过期、取消和object URL回收必须验证；不公开图片、不宣称SSR或传输节省、不放宽其他产品图片规范。
- W1/W2/W3负责人已回报真实显式Luna max编码已启动，Astra负责review。W3本地55463专用PG＋Next4613已通过真实产品页复现policy8但详情20的错误；这只是失败基线，不是修复完成。
- W5已发现全局draft/pending/prefill无actor作用域；已向W4提出A2单一写入方案申请，待签名/调用方和文件review后批准。不允许将旧无scope内容仅降级为草稿却展示给另一个账号。
- 已创建主代理独立集成树`/Users/li/work/orbit-web-integration-20260917`，由161e9e6c快进至W3-F提交`55dc5ec9062abe75c33bd254de56049c258ebc8e`。主代理独立本地库`orbit_web_integration_20260917`（127.0.0.1:5432）、独立锁文件安装依赖，11文件90/90测试通过、0跳过（TZ=Asia/Tokyo）；完整`tsc --noEmit --incremental false`退出0，测试随机schema已清理，工作树干净。F通过主代理验收。没有合入原旧主目录，也没有部署。
- 已建立绑定主任务的10分钟heartbeat“Web开发主代理调度”（automation ID `web`），持续核对真实进度、审阅交接并派发已批准任务。不是另起项目或生产发布授权；用户暂停或授权范围完成后暂停该检查。无变化时不重复催促/制造文档，具体工作仍按批准范围推进。
- W3追加`home/orbit-real-home.tsx`仅未知人数隐藏原数值span及render回归，不改首页布局、推荐、读取或CSS。
- W4的[A2-SCOPE](/Users/li/.codex/worktrees/2eef/orbit/repos/orbits/docs/development/web-2026-09-17/W4/A2-SCOPE.md)已审阅并批准A2/A3及B正确性窄修：全局draft/context/owner与global-ask局部状态由W4单一写入；真实canonical actor envelope、旧无scope不展示；AI handler可选expected-actor header只用于拒绝错scope，不授权；无header旧App兼容。目标保存后的只读版本/stale刷新获批，三tab布局仍待审。同一Luna串行重叠文件。
- W0设计文档提交`ec71f1c4`已完整审阅。批准B0→B1、B3、B4精确白名单；B1/B4共享provider串行，B1/B3可分文件并行。CRITICAL已披露，必须独立oracle、真实本地PG、旧API/App完整性和原子回滚回归。B2私有Web协议继续设计；B5先查既有outbox/queue，提出首个真实producer事务+可靠wake/repair闭环，不先加入孤立通用调度原语，更不关闭600秒安全网。这些依赖不阻塞已批准B0/B1/B3/B4。
- W5只读审查已确认真正可复用的是canonical推荐reader/匹配逻辑，旧推荐HTTP不是当前首页发现源。W3获下一批设计任务：真实目标与公开活动文本命中，先筛未来公开有效并排除本人主办/已报名，再排序最多3项，无匹配显示空态，不伪造AI分数/人群。具体代码文件待审，dashboard装配仍由主代理协调。
- W4在A及B正确性独立review后获批B单页收敛：标题/刷新→长期目标→四维结构→已存报告/显式分析，其他覆盖率/规则机会折叠保留；旧tab深链兼容。按PLAN B精确文件，追加contacts/dashboard/page.tsx仅旧tab映射；新增组件先申请。Agent移动历史抽屉仅移除重复模块导航组，保留统一移动菜单及会话操作。先截图核对再实施、Astra视觉及交互review；不据源码审查冒称视觉通过。
- W4的contacts/dashboard/page.tsx另批准传既有canonical actorId/key，分析消费者显式归属；不改认证/聚合。AI可靠回执与任务授权测试的require allowlist仅准增加expected-actor纯helper，原负例/回读断言保留。
- W0 B0 create_thread仍只有client ID、d935工作树，未确认启动且无工具可取消pending；不重复派B0，保留其五个预约文件。为继续独立工作，已放行内部Luna max执行B3及其自身既有测试oracle/PG验证，不写B0文件；B0后补充验收。B1/B4等联系人oracle冻结，B2/B5继续设计。不能将单任务创建问题升级为W0整体阻塞。
- W0 B3已确认内部Luna max `01a0ae99-7edf-7182-a450-af5156d4bf82`（Euler），独立`/private/tmp/orbit-w0-b3.epbpzC/worktree`，ec71f1c4起点。B2/B5补充方案已完整review：B2私有Web协议获批但实施顺序仍在B1 review后；B5改为纯in_app真实producer→同事务专属intent→wake→dispatch→repair试点，批准补充文档精确文件，测试路径/锁序须先补齐再编码。保留600秒安全网和25×10上限＋续扫，不改云配置/延迟消息能力、不删除mixed-channel Push；只跑本地注入fake，不等于全域idle治理完成。
- W3实施前补充风险：`OrbitEventStatsView` CRITICAL（24直接消费者、2流程、5模块）；`OrbitLandingEventView` HIGH（20直接消费者）。已向用户披露并要求Astra逐项核对真实消费者，授权不扩大到未批准文件；UNKNOWN补源码，完整消费者矩阵/回归/typecheck后再主代理验收。
- W2追加`contacts/new/batch2/[id]/page.tsx`仅复核页返回“导入中心”链接的en/zh/ja文案，保持现有布局/服务/路由目标；与此前start page同一局部三语范围。
- 核对W3公开主办方消费者后，追加`orbit-organizer-route-view-model.ts`的events人数number局部约束，以及`o/compose-app-organizer-public-from-previously-approved-mock-first-capabilities/organizer-public-route-view-model.ts`显式投影已校验人数；仅两个文件，不用强转或`??0`掩盖未知，不改presenter/读取/API。必须验证所有该VM生产者和公开主办方负例。
- W0 B0持续创建未完成时，批准B1精准解除该等待依赖：先在既有`tests/capabilities/contact-search-pagination.test.ts`建立独立字面oracle/本地PGfixture，Astra审阅后实施原B1范围，不占B0五文件或声称B0完成。B1/B4仍串行，B2在B1 review后，B0出现正式ID再协调补充验收。
- W3生命周期测试允许仅修正读取时序：写后重新读取真实canonical聚合，再组装VM，不手工替换预期数；保留摘要/名单不一致的mapper测试。提交前排除仅由本任务dev进程产生的next-env.d.ts变化。
- W3-F已完成领域review并交付19文件，原始detect记录66/66符号、1流程、medium，无error/partial/truncated字段；主代理核对原始记录与diff。预编辑CRITICAL/HIGH消费者风险仍保留，不能被事后medium抹去。F仅关闭真实容量、人数未知与0、名单不覆盖摘要及消费者适配；R准入回执与独立GET正在同Luna继续。Shanghai时区既有agenda测试失败已在干净161e9e6c复现，Tokyo90/90不能表述为所有时区通过。
- W0 B3追加两文件：`features/mobile/contacts-dashboard-service.ts`、`app/(app)/app/dashboard/compose-app-dashboard-from-previously-approved-mock-first-capabilities/dashboard-route-view-model.ts`（相对Web根）。仅包裹现有只读Promise.all，provider内AsyncLocalStorage批次scope按provider identity+actor复用在途promise，finally关闭清空；无scope不复用，不跨请求、不声称同一数据库快照。分析装配在scope外；测试early rejection后迟到请求、nested scope、跨请求同actor写后新读。与W4的contacts/dashboard/page.tsx不同，已同步单一写入边界。Mobile响应/隐私/错误契约不改。
- W0 B2私有Web counts须保留owner全集distinct eventCount，不能从当前页算“来自N场活动”；valueFilters保留全集首次出现前3标签顺序，不擅改SQL字母排序。仍须B1 review后编码，公共DTO不变。
- W5只读Astra继续设计首页真实待办/日程聚合及失败/空态事实展示，复用现有reader，不改首页架构，不与W3推荐数据或W4 Agent交互并发写。具体文件、TDD和冲突边界交主代理审阅后才编码。
- W5两轮只读设计已由主代理核对源码并批准第一步：内部Astra high `01a0ae8e-3597-7420-bc2f-63f1aaccb4da`（Helmholtz）在`/Users/li/work/orbit-web-home-facts-20260917`以显式Luna max新增5文件：`app/(app)/app/agent/home-facts-route-service.ts`、`home-facts-view-model.ts`（同目录）、`tests/pages/app-home-facts-route-service.test.ts`、`app-home-facts-view-model.test.ts`（同测试目录）、`docs/development/web-2026-09-17/W5/HOME-FACTS.md`。只做四源读取/投影/TDD，不改现有UI/API/features/shared。复用ORBIT_DISPLAY_TIME_ZONE及既有日历转换，固定snapshotAt，含今天7产品日半开开始窗口，不承诺窗外进行中事项；每源最多3条但保留截断前分组数量。personal reader实际先读actor全集N再每重复系列读例外，成功1+R storage读取，不是固定SQL预算。该成本作为未接线基础层被接受，接入首页仍须单独核验成本与W4文件释放，W0已知情；不通过窗口截断伪称解决底层扫描。
- W3推荐设计已完整审阅，获准R交付/review后由同Luna实施：仅`features/events/event-recommendation-tool.ts`新增纯matchedTokens导出、新增`public-goal-recommendations.ts`及`public-goal-recommendations-runtime.ts`（同features目录）、新增两份对应`tests/services/public-goal-recommendations*.test.ts`及域文档。本人relationshipGoal只匹配真实title/description；公开目录权威、有效未来、有效owner且非本人、自有canonical rsvped排除后再稳定top3，无匹配不兜底、不假称语义AI。缺owner/冲突重复元数据或membership错actor/异常数据失败关闭，不能忽略后当未报名。W5 UI不在此批；原recommend行为回归必须保留。旧canonical-participant-event-journeys rawSubject与runtime缺失[]由主/W5后续核查，尚未修复，不扩大当前四源foundation。
- B0创建问题的最新状态（替代此前pending快照）：正式thread `01a0ae93-5584-7e10-b56e-c3f46b9cbbef`现已回报提交`af889ee33dcf7579d04b7a09deec69702c8533fa`，主核对d935树和仅原保留5测试文件。回报API parity4/4、dashboard1/1、query PG3/5及detail PG1/5，红灯保留、未集成。已交W0审查oracle、显式本地环境守卫与真实PG命令，并对齐B1/B3/B4；不得由子直接cherry-pick到旧chat-agent，不再创建重复B0。
- W3推荐编码前风险：tokensFor LOW（1直接/2影响）、textMatchesToken LOW（1直接/3影响）；同文件createEventsRecommendationTool为CRITICAL（1直接/11影响/1流程，涉及AI conversation与automation）。主任务已披露；原函数体/行为不准改，只新增纯helper导出，避免导入副作用/循环依赖。必须核对这些消费者和原行为回归，不用riskSharedAxes LOW抵消真实risk；测试不调用真实模型。
- B0独立审查退回修正：隐式硬编码PG、fullwidth记录导致e查询应7非6、缺cursor早返回假绿、未真实调用Web matcher的伪验证。B4并发oracle按层次确认：未显式同expectedVersion的服务窄命令可双成功，但每个成功效果必须保留；合法冲突须无部分写且至少一成功，任意异常不能冒充可接受冲突。显式同旧版本CAS与底层insert-if-absent分别检验单winner；不得为错误测试预期破坏安全事务合并。双success丢note的memory反例须补真实PG回归，B0修订尚未接受。
- R身份整链追加：W0确认无占用后，批准W3仅`app/api/events/[id]/registration/route.ts`及`registration/cancel/route.ts`的resolveActor接既有canonical resolver，新增`tests/api/event-registration-account-scope.test.ts`；handler/DTO/storage不改。impact UNKNOWN/GET lower-bound未完全解析，需源核对Web/App GET/POST/cancel及签名消费者，不当低风险。源码已确认旧记录稳定ID/actor_id仍精确raw userId、既有事件canonical迁移不转换账号ID；历史raw≠account记录不会自动被新canonical读出。没有审计生产，不断言真实库有无此记录。R只可标未发布candidate今后身份对齐，历史兼容仍是发布门；W0另做本地合成与只读兼容/审计方案，不自动alias双读、不迁移、不云写。此门不阻塞其他本地独立开发。
- R发布门补充：W0真实legacy service/provider＋memory合成证明raw报名后canonical GET/cancel为null，再canonical报名可形成同event双rsvped。后续审计只用有效同workspace profile→account权威映射，分类raw同ID/canonical-only/raw-only/双key/孤儿歧义，覆盖取消与重激活，未知不当0；如受影响，迁移须另批完整覆盖membership/profile/projection/capacity/幂等receipt，不能只转一行owner。W3又确认App `EventRegistrationScreen.tsx`用auth.user.id校验GET/POST/cancel回执，而AuthSessionProvider.actorId才是accountId；分离ID将可能拒绝本人canonical ACK。App文件未获本Web批次编辑权，不能宣称跨端兼容；记录为后续精确App修复与验证门，不阻塞当前无依赖本地开发。
- W2主集成完成：源`482f337fa41398fa214aa7bd5131da94302462d6`经独立review，集成树先impact三个既有入口（各LOW/1直接；详情另有10条所列流程）后no-commit应用，完整force索引再分析。主原始detect`/tmp/orbit-main-w2-detect-full.json`及summary为14文件/434完整符号/6流程/HIGH，无error/partial/truncated字段，路径集合与Git完全一致；私图impact HIGH/2直接/5上游节点。全仓流程采样上限和bitmap.close测试同名误连如域报告保留，不能声明图谱穷尽。主独立本地库＋独立依赖：名片目标48＋API/repository28合计76/76，活动11文件90/90（Tokyo），全`tsc --noEmit --incremental false`退出0，随机schema无残留，diff-check通过。最终集成`7d89d4c6e460a4c7bfa911a36aeb28949cbb6df7`，工作树干净；未改旧主目录产品代码、未发布。实体相机/真实OCR/云清理/跨端远程回读仍未覆盖。

## 仍需设计/决定的独立事项

### 当前主审补充

- W1后续错误文案批次已独立批准原Luna：仅既有`profile-route-view-model.ts` failure对象12个中英literal、两既有profile live-route/navigation测试、REPORT四文件。主已读`/tmp/orbit-w1-failure-copy-proposal.md`与拟议patch；routeState入口CRITICAL1direct9total/loader3direct8total已披露，消费者admin/home重建自身copy但须回归。只去Ari/错误“来源复核”归因并明确重试读取，不改控制流/构造/服务mode/error/evidence/id/href/语言机制；先RED→GREEN＋types/图谱后增量交付，不重开taxonomy。
- **W5基础层主集成完成**：`4e15421eaac2c82285d286391101855211be4492`（父501ed5，源701864＋49b），严格5个新增文件，与源冻结内容一致。主Tokyo63/63含个人重复日程/关系待办/Today，LA22/22，fulltypes0；env-i/fetch禁止，默认无配置四源均unavailable/countnull。force后raw489符号/5路径齐、无flags/空ID/额外，0flowsLOW保留builder预算边界。日志`/tmp/orbit-main-home-facts-{tests,la-tests,typecheck}.log`和raw `/tmp/orbit-main-home-facts-detect-full.json`。提交后clean，未新增server/DB，未接UI/未部署。W4写锁、1+R读取成本、窗外开始的进行中事件排除均保留，首页原错误显示未因此关闭。
- **推荐服务主集成完成**：`501ed5c869a961555975891646134f696c6b6190`（父fd5，源69c9＋9aa），7文件。主env-i/fetch禁止110/1100skip＋自有PG catalogue5/50skip、fulltypes0；真实默认缺provider为unavailable，删新增helper后旧tool逐字节相同。主预impact旧入口CRITICAL（1直接/11上游/会话POST流程）保留。force后raw238符号/7diff/6mapped，无flags/空ID/额外路径；唯一非执行JSON已独立hash核对ee3ab…，0flows不等于无影响。日志`/tmp/orbit-main-recommendation-{tests,catalogue-tests,typecheck}.log`、raw `/tmp/orbit-main-recommendation-detect-full.json`。提交后树clean、随机schema0。未接UI/未部署、R历史/App发布门不变。W5源701864＋docs49b当前进入下一串行主验，尚未提交。
- B5 P2父真实PG claim确认固定26×11四非空轮250/25/10/1，持续25hot四轮均挡住cold；该试验用fixture将领取变终态，不替代real dispatch。新workspace级durable RR cursor方案已写原addendum第291行，主和Dalton Astra正在审：lastVisited而非lastServed、scheduler→row→intent短tx、targeted旁路、C排序、reservedkind排除、损坏failclosed/empty0DML。尚未授权新增metadata编码；P1源e98独立验收继续，不接受40P01作为正常成功。
- **W1主集成完成**：`fd5dcdc274d46cfabb6f85e995b0606862ce8a58`，基于7066串行整合源68d26→6ff→`394c4e70ded90d776338cf5f533b8096e3feb73b`。主自有UTF8 Unix socket实跑14文件134/134、0skip含14真实CAS，全typecheck0；生产及failure测试SHA与冻结一致。日志`/tmp/orbit-main-w1-reload-final-tests.log`、`/tmp/orbit-main-w1-reload-final-typecheck.log`。一次force后完整raw489符号/16diff文件/15映射/5flowsMEDIUM，无flags/空ID/额外路径；匿名route测试range例外与2条crypto同名误连保留，builder9588候选/8799callee/61walk限制不是穷尽。raw `/tmp/orbit-main-w1-final-reload-detect-full.json`，summary同前缀；提交前diffcheck、提交后树clean，CAS临时PG已停。资料/onboarding/窄字段保存/Reload竞争本地子项已验收；未部署，真实Google、原生、正式词表、旧共享loader错误copy仍分别保留。
- W2候选ContactDetail/LastInteraction类型影响HIGH（分别18直接/61上游、16直接/62上游），涉及API/App/AI，主已披露，仍只读设计。原contact.notes只有正文、contact.updatedAt跟随公司编辑而变化，亦不能当互动日期。App decoder可接受可选字段/空互动只证明读形状兼容；新首次summary-only写入缺真实日期的既有行为另需核对ACK/保稿/刷新，不擅自批准强制日期、补now或扩大B4写入。
- W1冻结报告“正式Reload失败重试已覆盖”经主逐行复核不实：现有case是PUT409后成功Reload，另一个是PUT回读失败；/tmp独立safety有该覆盖，但仓内没有。领域已承认并退回原Luna仅在获准既有测试补真实Reload GET失败→保稿/禁保存→显式retry，不改production、不扩大范围。主已独立重跑safety 3/3；等待最终SHA。主CAS资源已确认为`/tmp/orbit-profile-cas.wQBARP/data`数据库目录，socket在其父目录，已重启供本批重验（无TCP）。
- W5 docs-only纠正已冻结`49b944f3dddb48287400435fb4cd23f3e2efd553`。主核对固定55dc..701864的复跑raw `/tmp/orbit-w5-detect.eWcnca/detect-55dc5ec9-to-701864d9.raw.json`：489完整符号/5路径齐、无flags/空ID、0flowsLOW不等于穷尽；SHA256 `75bd8baf36e0c9f53f9c15a1071ab190351564d10a5734e47724251e75e41810`。原raw未存档，已如实标复跑；provenance保存各内容hash与索引meta差异解释。另批准Astra只读后续首页接线设计，不赋予W4文件写权。
- 推荐纠正增量已冻结`9aa359a2fda6730e8f1d3a0612c5ad4b7c8cf11f`（父69c9），仅helper/tests/REPORT三文件、域树clean。主独立22/22通过；完整diff及80实体ranges确认新增匿名callback无重叠，接受已披露的测试路径缺映射而不改测试形态。raw4/4symbols/3files、无flags/空ID，保留missing path；SHA256 `9458e910229259e4e96b57a0d9f235203357f6c5982e659a64ea59dda606b5bc`。领域110/110及types通过，队列等待W1提交后的主串行应用，不把基础服务交付当首页接线完成。
- W5未接线foundation源`701864d96aec564fd55b038088139c86790bd143`已交付（父55dc，仅5新文件）；实际Luna max子`01a0af7e-7221-7f03-852b-756c85c95b4f`。领域补测13绿9红→22/22，UTC/LA各22、旧回归24、全types通过。主已完整审route966行/VM206行及依赖关键路径、Tokyo独立22/22、无env真实默认工厂四源全部unavailable/count=null（fetch禁止），尚未应用main。完整raw489/5files证据待核；索引包含提交前内容，meta父SHA落后一提交如实保留。主发现报告误称operationHref参数connectionId，实际既有loader用contact.id，代码复用正确，交原Luna仅纠正文档。W4锁与首页未修状态不变，个人读取1+R/N全集成本门保留。
- W0已确认B4必改live-detail-service.ts、不改detail-contract.ts。主分配该共享服务B4优先（仍等B1释放provider/live-service），W2 P1-14继续只读契约设计，不能为绕锁先实现半套UI/DTO；待B4冻结新基线后再批准P1-14编码。
- W2 P1-14只读实证确认未解决：真实storage provider→live route→adapter→SSR可见DOM的本地合成fixture中，snippet-only/summary-only被重复投影为背景、简介、时间线及最近互动；零证据fallback note日期随读取时钟变化。已有bio/intro空白去重不足，私有Memo独立回读/actor拒绝现有路径有效，不重做。证据`/tmp/orbit-w2-p114-results.json`及`/tmp/orbit-w2-p114-design-review.md`。主继续批准只读精确类型设计/impact/App decoder兼容核查，尚未批准产品编辑；优先保留来源与原始信息、删除虚构投影/日期，不扩为语义合并。与W0 B4共享detail文件的锁待确认，A+B仍冻结已验收SHA。
- B5父补充真实PG独立确认P1（a600原版）：无提醒task首次delete报错，但tombstone和create/delete两receipt已提交，同请求重放成功且callback只执行一次。原Hooke继续修复，主不重复相同复现。父docs-only `db7d6e7d`撤回可集成及“26×11三轮”结论：旧测试仅fairPasses>=3且含终止空轮，不能证明精确轮数或持续热actor公平。P2 PG与纠正增量仍待；a600保持禁止集成。B1实际paged App消费者为EditNoteScreen.searchContacts，cursor JSON字符串透传，contactsListPath未请求分页，不混淆notes接口。
- W1 Reload修复尚未交付：Astra独立先RED 0/2，当前中间版安全probe 3/3覆盖同tick重复、JSON解析期间持锁、旧save/reload闭包互斥及失败重试；最终正式用例/卸载迟到/response owner检查仍收尾。主16文件暂存维持未提交，不把中间GREEN当最终验收。
- W3推荐大小写增量当前22新＋88旧=110/110、0skip及全types通过；父核对旧工具除新helper外逐字节不变，最终图谱/提交待完成，主未应用。W4 A2 Agent C1独立9/9与本地可编辑草稿通过，C2历史/组织队列及C3用户已点击发送的单次发送仍未完成；A3错actor header当前400的真实HTTP RED已取得，无模型调用。
- B3主集成`7066e5ecdc96ca4c5986232318402c96ade7c09c`，仅源`88e98dd5`七文件，不依赖/不混入B0八项RED。主完整审查SQL/helper及provider请求scope，预impact共享provider CRITICAL（3直接/17影响、5条分布/机会API流程）、mobile factory HIGH（1直接/4影响，含analysis/AI），风险保留。自有PG16端口55469，初建SQL_ASCII导致chr(5760)错误，原日志保留；改用新UTF8临时实例后10文件38项37通过、1既有精确URLguard跳过，全typecheck退出0，新增PG全跑，schema残留0。测试`/tmp/orbit-main-b3-tests-utf8.log`，类型`/tmp/orbit-main-b3-typecheck.log`。force/index-only/skip-fts后raw254符号/7路径全部映射、无flags/空ID，0流程不代表无影响；`/tmp/orbit-main-b3-detect-full.json`。实测返回行JSON的UTF8字节base35769→1529，同actor1000时677219→16471，各1 SQL；不是wire bytes、常数成本或SQL扫描上限，异常日期仍回退原图。提交后主树干净、临时PG已停，未部署；B5下一主审。
- B5领域冻结源`9de9d5fb`→父`a600732b`，8文件、11测试文件51/51和两份typecheck通过，fullraw582符号/148流程CRITICAL、8路径齐全无flags/空ID；保留旧600秒扫描、ACK丢失修复/锁序/投递fence与公平续扫证据。主尚未应用，不标提醒治理完成。领域本地报告路径`repos/orbits/docs/operations/2026-09-17-w0-local-read-write-acceptance.md`正在独立记录，不改业务SHA。
- 主正在优先复核W1最终增量（领域129/129及真实CAS已过，强制图谱收尾）；并行派出明确Astra high只读sidecar Dalton `01a0afa4-b17b-7df2-87a0-38bef1198130`独立审B5事务/锁序/租期fence、旧扫描并发与ACK丢失恢复。该sidecar无编辑/云/PG写权，不抢W0索引；结果回主后才进入B5主集成。主临时UTF8库`/tmp/orbit-main-b3-utf8.BeK7sM`已停止，后续确需可明确复用本任务实例，不碰其他任务库。
- B1兼容候选A仍待精确设计批准：只读检查已有deterministic ICU collation，不满足时完整保留旧JS matcher，不默改语义或声称fallback降本。需要一次single-flight探针的真实成本/失效策略、runtime pair依据、cursor/总数/facets同一引擎快照和精确文件；18上下文＋3037标量本地零差异只是证据，不能推定生产pair兼容。本轮未批准新依赖/CREATE COLLATION/云探测，统一规范化搜索投影另列后续。
- B1缩减A已获条件批准：在原B1白名单内，先落精确design/第六原文件与五个A文件差异、cursor消费者/预impact，再由原Luna实现。完整3037/18语义matrix留本地/CI，不在每个provider运行时重复发送35KB样本；运行时最多一次singleflight元数据只读SQL核验已审tuple，未知/变化/异常走同snapshot旧JS完整matcher，负能力有限退避、DB失败不当empty。生产未知pair默认fallback，SQL搜索降本发布门仍未关闭；无新文件/shared DTO/云/CREATE授权。B1原候选f5340153领域独立25/25已把B0查询3RED转绿，B4的5RED未关闭，候选尚未主集成。
- W1最终源链`68d26c77`→`6ff3db6164c2089cae46c39d67c23d89acf389b7`已无冲突应用主`7066e5ec`上的16文件暂存，尚未commit。主自有UTF8 socket `/tmp/orbit-profile-cas.wQBARP`（禁TCP）实跑14文件129/129、0skip含14真实CAS，全typecheck通过。日志`/tmp/orbit-main-w1-final-tests.log`、`/tmp/orbit-main-w1-final-typecheck.log`。末轮源码审查提出新409恢复路径的具体竞争：reloadLatestProfile无reload single-flight/epoch，连续Reload的旧GET可能在首次GET完成并再次保存后迟到重置profile/dirtyAtReload；已交W1 Astra用现有mounted/deferred fixture只读反例验证，未先断言已复现或扩大修改。主暂停对应commit，不影响其他批次；main树目前有本任务W1暂存，禁止下一批覆盖/清理。
- W1主完整raw同为478符号/5流程/MEDIUM，无flags/空ID，16 Git路径中15映射；主独立Cypher确认缺项测试File＋22实体均存在，修改hunk不与实体range相交，已完整diff与实际测试补证，仍如实保留missing路径。日志`/tmp/orbit-main-w1-final-detect-summary.json`、`/tmp/orbit-main-w1-unmapped-ranges.log`。临时CAS库验后已停止，若修复增量重验仅重启本任务上述路径。
- W2 A+B保持冻结；P1-14已追加给原Astra领域只读设计核查：用源码/本地合成fixture区分关系背景、公开简介、intro、私有Memo和时间线的来源与编辑权限，确认已有去重是否已满足明确需求后再给最小方案。未批准UI/adapter编码、SQL合并或自动摘要，不重启已延期自动账号发现。
- W1 Reload竞争已由领域Astra用mounted真实组件＋deferred fetch实证（不是后端CAS绕过）：GET2/GET3并发，GET2后允许编辑并PUT/GET保存成功，迟到GET3使姓名/expectedUpdatedAt退回旧快照，dirtyAtReload复活导致无新编辑又发PUT。证据`/tmp/orbit-w1-reload-race-evidence.json`、探针`/tmp/orbit-w1-reload-race.rk325888/probe.test.tsx`。批准原Luna只改既有`orbit-real-profile.tsx`、`tests/pages/app-profile-editor-failure-paths.test.tsx`及域REPORT，先RED后同步single-flight/ref、busy与保存互斥、必要最小epoch/卸载保护。必须验证双击、失败重试保稿、迟到旧response/finally不回退确认状态；最终新增量依赖6ff3db61，主仍暂停commit。
- B5 Dalton独立审查提出两finding并暂缓主集成：P1实际内存task service/repository＋新command证明删除事务已提交/tombstone与receipt存在，但取消提醒校验仍排除tombstone，首次删除（甚至无提醒）报错，重放不再调用清理；应验证同actor tombstone供终止清理，不能吞异常。P2源码内存排序模型显示只从eligible intents聚合lastClaimedAt会丢已完成领取历史，25热actor持续补充可使第26个饥饿；26×11固定模型是250/25/10/1四非空轮，非报告三轮。上述尚待W0独立真实PG确认/原Luna修复；批准原8files局部TDD，若需新持久公平状态/文件先申请。旧600秒扫描、mixed/push、锁序/fence和ACK恢复门不变；sidecar未写库/文件/云。
- 推荐候选`69c9adcce64888c560d969b0339d91906d4851df`（父23646028）领域交付，批准5产品/测试＋REPORT/raw共7文件。领域20新＋88旧消费者＋5PG目录共113/113，types0，实际本地runtime三态/缺provider与默认automation旧入口探针通过。force后236符号/7files，无flags/空ID，0流程不否认原工具入口CRITICAL；raw JSON hash `ee3ab3f376179339e88f88c3393599bc0b04f84b15ada46c86168f68402ae011`。未主验/未接UI/未部署，不与主暂存W1混合应用。
- 推荐主审发现并以真实导出/无网络guard实证：新matchedTokensForText未沿用旧eventText的小写化；同目标“AI交流会”对大写标题仅命中ai，对小写标题命中ai交流会＋ai，改变计数/排序。69c9暂不接受，批准原Luna仅新export比较层恢复既有toLowerCase语义、原测试及报告，先RED再验证混排大小写/排序一致与AI非Kansai边界。展示原文和旧入口/原tokensFor/textMatchesToken不改；交依赖69c9的增量并重验消费者/types/图谱。
- R主集成`66087fff853f0133cbf1f996e39b1e03bd23bb7c`：基于W2/F的`7d89d4c6`无冲突应用源`23646028`。主独立本地PG运行14文件84/84、0skip，全typecheck退出0，随机schema残留0，diff-check及提交后工作树干净。增量索引曾产生空resolveActor ID和缺路由节点，未被接受；一次force/index-only/skip-fts后raw完整267符号、9流程、HIGH、无error/partial/truncated/空ID；9产品/测试/文档路径映射齐全，raw JSON证据按SHA256 `1a235568c024a70e097462d4d79cdaa1698d2d1d9fce97fe561e2b9e02004641`独立核对，不要求索引自身。全仓流程采样上限仍披露，FTS禁用，不宣称图谱穷尽。日志`/tmp/orbit-main-r-tests.log`、`/tmp/orbit-main-r-typecheck.log`、`/tmp/orbit-main-r-detect-full.json`。未部署，历史身份/App ACK门转[BR-031](../../../bridge/2026-09-17-web-registration-identity-candidate.md)。同Luna max已按批准继续推荐服务，不写首页UI。
- W1候选`68d26c772f86574444a34cc11df5fd2c5eb445d8`虽领域107/107与typecheck通过，主审未接受：两页直接用session.user.id，而API与profile provider权威是canonical accountId。实际probe中raw读empty、resolver映射account后读真实profile、无membership返回null；无mode env默认factory返回示例profile。已批准原Luna仅两页复用既有canonical resolver，真实入口non-live fail-closed不挂编辑器，continue显式live，保留内部scenario mock；不改变CRITICAL共享loader（3直接/8影响，含admin/home/Agent流程）或API默认模式。不把开发环境直接API的既有mock约定误称全链已修复。
- W1原始图谱16 Git文件只映射15路径已查明：缺项测试文件有File及22实体，但新diff位于匿名test回调，未与实体range相交，File自身range为null。保留16 diff/15 mapped事实，以完整diff与实际测试补核，不造File symbol；修复增量仍需新的完整变化检查。
- W1增量存储证据：领域Astra以真实StorageProfileProvider/StorageAccountSessionProvider＋MemoryLiveRecordStore双账号独立复验，raw profile ID读0 accounts/0 profiles，canonical A/B各只读本人，resolver映射两raw subjects及legacy account subject正确；日志`/tmp/orbit-w1-storage-identity-proof.json`。尚待两页行为测试和最终增量交付。旧共享loader的live错误分支仍构造mock services并显示含Ari的固定错误copy，但不返回mock profile数据；本批non-live/membership错误绕开该分支，既有真实store失败文案单列后续，不扩大CRITICAL共享默认语义修改。
- W3资料身份依赖已解除：W1 probe与主/W3源码复核均支持accountId。R page和后续推荐runtime保留canonical读取，推荐显式live；不倒退raw，也不据此宣称历史错误owner记录兼容。R的历史报名与App ACK发布门仍有效。
- W4同一IAB浏览器新tab恢复本地UI验证，桌面/窄屏Escape焦点与显式session优先历史通过，未新增发送；A2仍在修迟到身份响应、旧target和异步保稿竞争，未验收。未使用待批准的外部Playwright工具。
- W5实际子模型已核对gpt-5.6-luna/max；缺模块RED→首版6通过2失败→部分fixture修正后12/12，但Astra仍查history/orphan误作可行动待办、snapshot时钟漂移、无时区时间、窗口边界与被top3截断掩盖的取消例外断言，未放行。主另发现HOME-FACTS错误宣称W4释放文件，已要求原Luna纠正；W4锁持续有效，不能凭12/12推进UI。
- B0增量`2372fc84`仅原5测试文件：W0独立18项10通过8失败、0跳过，红灯分别定位搜索/全量facets与窄命令并发丢失/回滚/tombstone。无env13项PG明确跳过，非法远端应失败；新测试索引覆盖仍待W0完成。此为正确失败基线，不是业务完成，也不直接进入主集成。
- W0最新交付进展替代上条索引待核快照：B0通过精确fixture include的本地索引诊断取得真实节点，恢复本人临时配置后完整raw204符号/5文件/0流程/LOW、无flags/空ID，领域提交`a08c1ab2`；18项10绿8红仍是待修复业务基线。B3整合35项34通过、1既有URLguard跳过，新PG5项实跑及两份typecheck通过，但增量索引再次损坏（空ID与缺文件），已暂停提交做一次force/skip-fts。B5源`9de9d5fb`已交领域，父13新＋38旧测试通过，待B3后串行整合；以上均未通过主代理集成，不提前关闭W0。
- B1新增真实语义门：本地PG16的lower('İ')与JS toLowerCase输出不同（i对i＋combining dot），lower('ΟΣ')的末尾sigma亦不同；旧API依JS字面语义，不能声称SQL lower全面等价。允许本地只读调查既有ICU collation及独立负例，未批准生产探测、新依赖/迁移、全中文全量回退或逐字特判；若需统一规范化搜索文档须先设计/影响审查。该门仅影响B1及直接依赖，B3/B5继续。

- 正式职位/供需/话题词表及topics上限：不得拿测试数据或模型建议冒充正式政策，W1交待审提案。
- email/phone账号发现：用户已明确决定本轮保留邀请绑定，自动发现只记录为后续开发方向，不再作为当前待决定或阻塞项。本轮不实现验证/可发现偏好/电话挑战；W2保留已调查的能力缺口和未来前置条件，既有邀请接受及通信鉴权保持。该决定已同步W0/W2。
- W4分析页收敛已按下方追加记录批准，须先通过A/B正确性review；W5首页真实推荐/日程聚合及全局语言仍按具体方案审阅。首页事实快照调查发现约谈`startsAtUtc`被误读成`startAt`、失败被显示0、结束活动被误推断无复盘；方案正在核对时间窗口及reader成本，未批准实施。不得把个人日程显式from/to查询误说成覆盖所有窗外开始的进行中实例。
- W3-R身份风险已查明并批准最小修复：报名page的raw session.user.id与API canonical accountId可能不同。W0确认无占用，W3仅追加`app/(app)/app/events/[id]/register/page.tsx`复用既有resolveAuthenticatedApiActorFromSession，无canonical时fail closed不回退raw。currentRegistrationActor impact LOW/1调用，Next page UNKNOWN已rg确认路由/测试；新增`tests/pages/app-event-registration-account-scope.test.tsx`，readback测试覆盖account ACK接受、raw/其他actor拒绝，保留匿名及已有测试分支。不改身份服务/API/shared，随R独立review后交付。
- 真实Google回调、实体相机/原生、真实provider报告、生产业务验证：分别登记环境与权限，不阻塞已批准本地实现。

## 主代理关闭标准

每批必须回报真实Luna模型、代码SHA、精确文件、失败路径、实跑命令/结果、未验项和跨端影响。Astra独立review后，主代理核对diff与测试、共享文件和业务语义冲突，在独立集成树整合并回归。提交前完整图谱变化分析；UNKNOWN需补查，partial/truncated不是通过。

状态按“执行中→领域review→待集成→主代理验收→已解决子项”推进。已有失败/跳过保留，部署、源码完成、运行验收分开。一次设计、测试或提交不是整个待办完成；存在独立已授权工作时继续调度。

## 2026-09-19 Orbit_0918 换肤批次记录（工作树 newui/batch-0-shell-landing）

- **W4 锁释放（用户指令）**：2026-09-19 09:24 用户明确“w4台账记录锁释放，后续改动也相应落库方便后续检查”。据此，W4 对 iOrbit/agent 域三个 UI 文件及 `orbit-agent-dashboard.tsx` 的写冻结视为已释放，Orbit_0918 批次 4（iOrbit home / 历史抽屉 / chat 视觉 / actions 等）可以推进。注意：主仓库台账（mtime 2026-09-18 14:54）第 176 行仍写“W4锁持续有效”，未同步；以用户本次指令为准，本工作树先行落库，主仓库台账由协调者后续统一更新。W4 释放仅限 UI/视觉层；逻辑层改动（home-facts、provider、live-service 等）仍按各自锁与 review 流程，不随本释放解禁。
- **报名屏决定（用户指令）**：“报名屏用之前的，但是ui要修改匹配成适合新的”——批次 1c 保留 0066 RegistrationPortraitWorkspace 全部逻辑、data-* 钩子与 readback 语义（BR-031 严格回执不变），仅做 Orbit_0918 视觉替换。
- **connect 连接屏决定（用户指令）**：“先占位”——按设计稿 个人中心.dc.html 连接区（Google Calendar / Gmail / Google Contacts / Notion 四卡）做「即将开放」禁用占位态，不写假 OAuth 流程。
- **calendar tab 决定（早前用户指令）**：calendar tab 不再单独做，已合入 iOrbit；iOrbit home 的日程/月历微件即其承载。
- **已交付 0918 批次提交清单**（全部通过全量回归 387 项 + typecheck，两处基线自带失败除外：product-surface-manifest 11 条 P1 在 /app/agent 锁区、app-events-live-route-services 演示目录日期过期）：
  - `866b90de` 批次0：shell + landing 0918 换肤
  - `a32678b5` 浮岛顶部导航（0918 样式替换旧导航栏）
  - `718129e9` 批次1a、`3f673e1b` 批次1b、`06b517f1` 批次1b+（Events 列表/详情/相关）
  - `99cec095` 批次2：个人中心 profile
  - `b22a88b2`、`094e1798`、`22f240fb`、`b5ace54e` 批次1d 四屏
  - `ad3ac3e0` 批次3a、`dba7fe2e` 批次3b、`f8b75e94` 批次3c、`35824a5b` 批次3d（Network 联系人/分析）
  - `ad6b9594` 批次2b：settings 页作用域 token 重映射
  - `d167f209` 批次3e：联系人详情页作用域 token 重映射（0918 indigo，nc-* 结构与逻辑不变）
- **进行中**：批次 1c 报名屏视觉替换 → connect 占位 → 批次 4a iOrbit home（消费已交付 D25/home-facts 聚合，真实 ready/empty/unavailable 三态，不伪造设计稿 mock 数字）→ 4b/4c 历史抽屉与 chat 视觉 → 4d actions/plan/strategy（仅做有真实数据的静态聚合，无接口能力不做假）。每批提交后在本台账追加记录。
- **批次 1c 完成** `d3e6fd8d`：报名屏 0918 换肤，0066 RegistrationPortraitWorkspace 逻辑/data-*/readback（BR-031）零改动（diff 审计纯 CSS/色值）。portrait 作用域 CSS 换 0918 靛蓝体系＋serif 900 标题；访谈层 `[data-orbit-registration-profile-guide=register]` 作用域全套 token 重映射＋color-scheme:light。验证：报名 5 套件 37/37 绿、ratchet 8/8、typecheck 净、SSR 截图核验通过。顺带修复基线缺陷：event-registration-readonly-ssr fixture 缺 `resolveAuthenticatedApiActorFromSession` 导出（干净 HEAD 上即红，W3-R 遗留），补导出后转绿。**环境阻塞如实记录**：event-registration-readback（15 项）与 app-event-registration-guide（13 项）当前因云端 PG「data transfer quota exceeded」无法运行/挂起，主仓库（无本批改动）同样复现，与本批 CSS 改动无关；待 PG 配额恢复后需补跑。
- **踩坑记录（后续批次遵守）**：本工作区 Bash 默认 cwd 是主仓库 `/Users/li/work/orbit`，`cd repos/orbits` 会落进主仓库；所有命令必须显式带工作树 cwd。也不得把 `git stash` 与可能挂起的测试串在同一命令里（超时 kill 会导致 stash 未弹出）。
- **connect 占位完成** `82cb3f80`：个人中心新增第三 tab「连接」，按 个人中心.dc.html 四张集成卡（Google Calendar / Gmail / Google Contacts / Notion）全部「即将开放」禁用占位，无假 OAuth、不记连接状态（用户 2026-09-19「先占位」决定）；0918 样式走 pf-connect-* 作用域类；connect tab 上隐藏无关的保存按钮。按钮 ratchet 上限 153→154 并注明理由（一个新设计规范 tab 控件；卡片按钮均用 .btn）。验证：profile 套件 58/58（4 基线跳过）、ratchet 8/8、typecheck 净、浏览器点击截图核验四卡渲染与说明文案。
- **批次 4a 完成** `acb4c249`：iOrbit home 新增「今日」四卡——今日日程（personal+appointments 当日项）、月历微件（当月网格+有事日标记+今日高亮，链 /app/schedule）、联系人机会（followups.current 规则版前 3）、继续对话（/api/ai/conversations/sessions 前 3，跳 /app/agent?session=）。数据消费已交付 D25 聚合：facts 经既有 refreshHomeDashboardAction（use server），运行时动态 import + window 守卫，SSR/测试零副作用；真实 ready/empty/unavailable/pending 四态，无设计稿 mock 数字。0918 视觉在 [data-orbit-agent-dashboard] 作用域 token 重映射。验证：agent 域 61+37+50+30+44 项绿（app-agent-contact-recommendations 1 条基线自带失败，干净 HEAD 复现，本地化 fixture 漂移，与本批无关）、ratchet 8/8、typecheck 净、浏览器截图核验四卡与月历。
- **批次 4b/4c 完成** `dcb3cd38`：agent 控制台整体换肤（W4 锁已按用户 2026-09-19 指令释放后首个 agent 文件改动）。做法：CONSOLE_STYLES 模板字符串末尾追加 0918 作用域 token 重映射（--agent-*/--accent*/--text*/--bg*/--surface*/--border* 全套）＋ serif 900 标题（.h-display）＋主按钮 #0E1225→hover #2E3270、ghost 白/#DDDEFA/#3B3F7A、btn-soft #ECEEFB ＋ .agent-history 背景随 canvas token；根 div inline --text-3/--text-4 值改为 #6B6F99/#9FA3C4（仅改色值，不碰尺寸）。文件属 scale-ratchet SNAPPED，故全部走模板 CSS，inline 几何零改动。4b 历史抽屉口径：既有侧栏（分组/置顶/搜索）交互不变，仅视觉换新，设计稿抽屉属同一能力换皮。验证：typecheck 净；agent 域 49+38+50（其中 app-agent-contact-recommendations 1 条基线自带失败，干净 HEAD 复现）全绿；ratchet 8/8；headless 浏览器截图核验欢迎页+对话页 0918 皮肤渲染、零 pageerror（夹具用后已删）。detect-changes：1 文件 3 符号，风险 low。
- **回归收尾** `42cc1434`：①connect 占位「即将开放」由 disabled button 改为非交互 note（aria-disabled span）——占位本就不该是可交互控件，消除 full-product-functional-audit 新增的 candidate-missing-handler 指控；②core-product-ux-optimizations 可读性规则不变、期望值随 4c 更新为 0918 色值。审计三件套回到与主仓库 377609b6 完全一致的基线失败集（13 条，均为既有 P1 锁区/覆盖缺口）。
- **全量回归口径（2026-09-19，dcb3cd38+42cc1434 之上）**：tests/pages 152 文件分三组 344+394+269 项（除下列外全绿）、tests/ui 29 文件 148/148、tests/audits 170 项 157 绿、ratchet 8/8、typecheck 净。**未通过项全部为基线/环境，非本分支引入**：app-agent-contact-recommendations 1 条（本地化 fixture 漂移，主仓库复现）；app-events-live-route-services agenda clocks 1 条（既有）；app-register-live-route-services + app-registered-event-lifecycle 7 条（需本地测试库环境，直跑 node --test 无 npm test 的 assertLocalTestDatabases 前置）；event-registration-portrait-browser 1 条（需 sibling orbit-app 的 playwright，工作树布局缺）；event-registration-readback 15 项 + app-event-registration-guide 13 项（云端 PG 配额，待恢复补跑）；audits 13 条（基线 P1）。
- **批次 4d 结论（范围界定，不实施）**：actions/plan/strategy 三屏按提案 §四属「等 W4 / 等产品决定」——①建议动作服务（W4 逻辑层）至今未交付（仓库无 suggested-actions 服务，仅有 /api/agent/signals）；②plan「4 周推进」与 strategy「四段式」无服务端接口，需产品决定静态聚合 or 新 AI 能力（涉模型预算审批）；③actions 三档优先级分档规则来源未定。依「不伪造原则」不做无接口假能力、不塞占位文案；待用户对 iorbit-batch4-proposal-2026-09-19.md §五 三个开放问题逐项拍板后实施。
- **批次 5b 完成** `4493846a`：iOrbit actions 屏（/app/agent/actions）。三档分档规则按用户 2026-09-19 拍板「复用账本既有归属」实现：awaiting_confirmation→需要你决定、approved/executing→建议今天做、deferred→可稍后；终态条目不进三档，completedAt 落在本地今天的计入今日进度环分子，分母=三档合计+今日已完成，全部为账本真实计数，无设计稿 mock 数字。写操作复用既有 OrbitTodayDecisionForm（待确认条目）与 OrbitAllActionsControls（撤销/重试/取消），与 Today / All actions 同一写入口。?entry= 决策深链落此屏。0918 视觉在 [data-orbit-real-page=agent-actions] 作用域（serif 900 标题、三档白卡、进度环、aga-* 类）。验证：新增 VM 测试 10/10、ratchet 8/8、typecheck 净、浏览器截图核验三档+进度环渲染（夹具用后已删）；detect-changes 无变化（纯新增文件）。
- **批次 5b 完成** `4493846a`：iOrbit actions 屏（/app/agent/actions）。三档分档规则按用户 2026-09-19 拍板「复用账本既有归属」实现：awaiting_confirmation→需要你决定、approved/executing→建议今天做、deferred→可稍后；终态条目不进三档，completedAt 落在本地今天的计入今日进度环分子，分母=三档合计+今日已完成，全部为账本真实计数，无设计稿 mock 数字。写操作复用既有 OrbitTodayDecisionForm（待确认条目）与 OrbitAllActionsControls（撤销/重试/取消），与 Today / All actions 同一写入口。?entry= 决策深链落此屏。0918 视觉在 [data-orbit-real-page=agent-actions] 作用域（serif 900 标题、三档白卡、进度环、aga-* 类）。验证：新增 VM 测试 10/10、ratchet 8/8、typecheck 净、浏览器截图核验三档+进度环渲染（夹具用后已删）；detect-changes 无变化（纯新增文件）。
- **批次 5c 完成** `c6cab88c`：iOrbit plan 屏（/app/agent/plan），按用户拍板先静态聚合。本周重点任务 = home-facts followups.current + 账本 approved/executing（账本项链回 actions 屏 ?entry= 深链）；本周日程 = home-facts appointments（七日窗已确认约谈）；概览计数 = 重点任务/本周日程/等你决定/进行中真实计数，来源不可用显示「—」不放 mock。「4 周推进节奏」手风琴无服务端接口 → 不做（见待办）。数据走既有通道：facts 快照经 refreshHomeDashboardAction（运行时动态 import + window 守卫，SSR/测试零副作用），账本经只读 GET /api/agent/ledger。0918 视觉 [data-orbit-real-page=agent-plan] 作用域（agp-* 类）。验证：VM 测试 6/6、ratchet 8/8、typecheck 净、浏览器截图核验概览四计数+重点任务+日程（夹具用后已删）。/app/schedule 重定向改指本屏（见 5a）。
- **批次 5d 完成** `c1550914`：iOrbit strategy 屏（/app/agent/strategy），静态聚合。先联系谁 = followups.current（姓名/组织/理由/到期/链接，无 href 且无 contactId 时不伪造链接）；下一步去哪 = D17 公开活动目标词法推荐（snapshot.recommendations 原样四态：success/needs_goal/no_match/unavailable，前三）；「缺什么人 / 准备什么」依赖 W4 策略生成（无接口）→ 显式虚线「等 W4」等待卡（引导对话中问 iOrbit），不放 mock（用户拍板）。数据同样经 facts 快照 server action。0918 视觉 [data-orbit-real-page=agent-strategy] 作用域（ags-* 类、头像 chip、token chip）。验证：VM 测试 5/5、ratchet 8/8、typecheck 净、浏览器截图核验四区块（夹具用后已删）。
- **批次 5a 完成** `f3147fc6`：/app/today 收窄成纯重定向（同 /app/home 先例）：裸进 → /app/agent；?entry= 决策深链 → /app/agent/actions?entry=（决策落 actions 屏）。/app/schedule 由 /app/today#arrangements 改指 /app/agent/plan；/app/followups 由 /app/today?view=day 同步改指 /app/agent/plan。today-page-content 及其组件（决策表单、arrangements、时间脊柱）全部留原不删——仍被 actions 屏等复用，VM 单测不受影响。5 个测试文件的路由级断言改为解析 NEXT_REDIRECT digest；actor 边界门改为只覆盖 All actions（Today 不再触碰账本）。回归：today/schedule/followups/boundary 套件 63/63；定向大组 409/411，2 个失败均为文档化基线（app-agent-contact-recommendations 本地化 fixture 漂移，干净 HEAD 复现；product-surface-manifest 11 条 P1 全部在既有锁区文件 agent-chat-history-organization 9 + orbit-real-agent 2，逐一核对无一条来自新屏）。detect-changes：8 文件 4 符号，low。
- **待办（用户拍板记入）**：①plan 屏「4 周 AI 计划」手风琴——需新 AI 能力（涉模型预算审批），无服务端接口，plan 静态版已交付 `c6cab88c`；②strategy 屏「四段策略 AI 版」（缺什么人/准备什么）——依赖 W4 策略生成能力，当前为显式等 W4 空态 `c1550914`；③plan/strategy 目前入口为 /app/schedule、/app/followups 重定向 + 直接 URL，iOrbit home 是否挂链待后续批次决定。

## 2026-09-21 边界改为 B（屏级替换）+ 路由归并

- 全量回归（2026-09-20 启动，日志 /tmp/full-regression-pages-1.log）已结束：1054 项 / 1014 绿 / 35 失败 / 1 取消，35+1 全部落在文档化 6 项基线（readback 16 + guide 12 云端 PG；register-live 6 + lifecycle 1 本地测试库；recommendations 1；agenda clocks 1；portrait-browser 1），0918 分支零新增失败。耗时 2h10m，其中 readback 等云端 PG 超时 7827s——后续全量回归先排除 live-mode 文件或加 `--test-timeout`。
- 本地预览：Neon 配额耗尽期间改用本地 PG `orbit_newui_preview_20260921`（工作树 `.env.local` 指向；种子：primary-test-account 部分 + live-events + event-ops e2e + contact-fixtures）。dev server 用 `.claude/launch.json` 新增的 `orbits-newui`（绝对路径，端口 3100）；`preview_start` 只认会话原目录，`orbits` 配置会从主仓起。
- 用户 2026-09-21 检查后判定当前为融合态而非替换，拍板改边界 B，见 NEW-UI-DECISION.md；路由归并（20 删 / 10 保 / 28 对应设计屏）见 ROUTE-CONSOLIDATION.md；铃铛保留；App 端不动。
- 已批准的小改动（并入个人中心新屏一起做）：onboarding 门禁提示——落地横幅（`?onboarding=1` 且未完成时，标题下说明「完成基础资料后才能进入 iOrbit/活动/人脉」+ 缺项 + 「去填写」并默认落编辑页签）、四必填字段标记、保存后仍不完整改为琥珀色缺项提示不显示绿色成功、右侧状态框上移。根因：`features/profile/onboarding.ts` 要求 displayName/primaryIndustryId/secondaryIndustryId/birthDate 四项，表单允许缺生日保存成功且无提示。
- **Network 任务 2 完成** `55931044`：/app/contacts 换为设计稿 257–302 行重建屏；来源卡/行/阶段 chip 全真实计数；mismatch=0.0572（对齐顶栏 2px + 页签 2px 偏移后 0.0386，其中真实联系人文字 0.0323、共享顶栏 0.0013、页签字号 0.0011、浮动控件 0.0021，遮罩外仅 0.0001；页签差异源于设计稿 `<button>` 走 UA 默认 13.33px 而壳用 `<a>` 继承 15px，未改任务 1 的 `nw-tab`）。附带：page.tsx 外层容器加 `data-orbit-real-page="network"`（顶栏样式需该祖先），`app-contacts-live-route-services` 源码断言改为 `NetworkAll`。
- **Network 任务 3 完成** `3da3b5b3`：/app/contacts/pipeline 换为设计稿 173–256 行重建屏（管线总览五统计块 / AI 人脉建议 / 筛选条 / 四列看板）；任务简报误把概览屏的阶段箭头条与两张高亮卡写进本屏，经协调人确认按设计稿原样移植、测试去掉 clip-path 断言。数据：统计块与看板计数 = `stageCounts`；page 同时加载 `loadContactsAnalysis`，「最近新增 N 位」= `metrics.newContacts`（未就绪显示「—」），设计稿「↗ +25%」无数据来源不渲染（去假数据偏差）；AI 建议 = `opportunities.actions` 每页 3 条、「换一批」轮换、无数据显示 `nw-empty`「暂无建议」并禁用按钮；第四列/第五统计块文案为「已归档」。看板卡片头像/姓名/「···」均为 `<a href>` 到详情（任务 6 改弹窗），上次互动/下一步为空显示「—」，`pendingInit` 加「待设置关系」chip。mismatch=0.0905（app 8603px 全页 vs 设计 1606px；对齐后 0.0420：真实看板卡片 0.0262、真实建议文字与单行高度 0.0097、统计数字/新增数 0.0015、看板列计数与已归档文案 0.0012、共享顶栏 0.0012、浮动控件 0.0021、遮罩外 0.0000；非数据残差 0.0033 ≤ 0.005，卡片节距/分隔线/列头全部恒定偏移无版式差）。修一处：`换一批` 按钮沿用设计稿 `<button>` UA 默认 `padding:1px 6px`（首轮右移 6px）。detect-changes：low，仅 `AppContactsPipelinePage`。
- **Network 任务 4 完成** `b64d150b`：/app/contacts/dashboard 换为设计稿 66–171 行概览屏 + 393–609 行「AI 人脉分析」子页（`?tab=structure|opportunities`，既有 query 语义保留；分组下钻页换壳 `NetworkShell screen="analysis"`，`.analysis-card/.analysis-grid/.analysis-notice/.analysis-muted` 规则复制进 `NETWORK_STYLES`）。任务简报与设计稿/仓库有 17 处矛盾，经协调人逐项拍板（原则：结构 1:1、内容真实或诚实省略、文案偏差记台账）：①分布卡底行按设计「最近新增 N 位」（`metrics.newContacts`，未就绪「—」），「↗ +25%」无来源不渲染；②驾驶舱右上「更新于今天/M月D日 · 依据 N 位联系人」（`generatedAt`，UTC 日期；未就绪「分析生成中」），四卡 = `cockpit(analysis)` 真实计数（`n` 置于标题前「N 位高价值关系」），置信度 mock 换成数据来源标签；③「最近联系人」→「最近动态」：行 = `analysis.activity`（真实 `occurredAt`，标签是动态句子而非姓名：头像用 `◷`、动态/来源两列、行业「—」、状态 chip 与「···」省略、行不可点；表头「姓名/公司 · 职位」改为「动态/来源」；该列加省略号截断——设计 nowrap 是为姓名设计的），空态「还没有互动记录」；④子页头按设计「← 返回人脉」胶囊 + 副标题 + 「＋ 导入人脉」；⑤结构诊断 h2 = `analysis.summary`、分布小结 = `structure.summary`、结构洞察 = `coverage.summary`、机会 hero = `opportunities.summary`/`coverage.summary`（三枚 ✦ chip 为静态设计文案），mock 段落与「查看相关洞察/具体建议/详细分析」按钮改指 `?tab=opportunities`（洞察视图不在范围）；⑥关系健康只渲染 `structure.health` 真实行（设计第四块「决策层占比」无来源不渲染，网格规则保持设计 `auto-fit` 原样自然三列），页脚「更多关键指标」= 强关系占比（health strong %）+ 最近新增联系人，「平均近 30 天互动」无来源省略；⑦覆盖建议按设计放机会页签：行 = `coverage.gaps`（severity → 优先拓展/重点关注/持续跟进 三色；设计 coverage 数组的三组 icon/icon 色按 severity 高→低映射：high ▮ #DDDEFA/#2E3270、medium ❋ #E6F1EC/#2F6B4F、low ▦ #ECEEFB/#4B4FC7——设计里 icon 是逐条 mock，此为映射偏差），副标题由设计「基于行业趋势、你的人脉结构和业务需求，为你识别以下机会方向。」改写为「基于你的人脉结构与关系目标，为你识别以下缺口方向。」（数据来源是缺口而非行业趋势，文案偏差），「查看全部建议/动作」（洞察）省略；⑧目标覆盖：表盘 = `coverage.score`（未就绪「— —」）、行 = 高价值关系 `metrics.highValue` + 核心关系 health strong count，「引荐路径」无来源省略，提示行显示当前目标文本，「⟳ 刷新机会」= 旧 workspace `refresh(true)`（POST recompute + 重拉），「◈ 设置关系目标」→ 复用 `AnalysisGoalEditor`（不可编辑时链 /app/profile「完善个人资料」）；⑨建议动作卡 = `<a href=primary>` 标题/`dueLabel` 标签/「联系人 · 判断」，evidence/steps/secondary 不渲染；新增同样式「待唤醒关系」卡 = `opportunities.dormant`；⑩人脉分析报告卡 = `analysis.report`（生成于 M月D日 / 需要重新分析 / 尚未生成），按钮沿用旧 `requestAnalysis`（stashAgentPrefill → /app/agent），「预计 2–3 分钟」mock 省略。像素：概览 raw 0.1295（design 1773 / app 1663；顶栏 +2、首行卡片以下恒定 −55、末尾少一行；对齐后 0.0947：分布卡数据 0.0194、驾驶舱数据 0.0276、最近动态行 0.0099 + 页高 0.031、高亮/阶段数字 0.0027、共享顶栏 0.0011、浮动控件 0.0018、文案偏差 0.0005、卡底边 0.0005；非数据残差 0.0039 ≤ 0.005，无版式差）；分析-结构 raw 0.0813（对齐后 0.0573，非数据 0.0045：顶栏 0.0012、浮动 0.0019、洞察卡按钮随真实段落高度居中 0.0010、页脚 1px 亚像素 0.0003）；分析-机会 raw 0.0977（非数据 0.0039 + 已批准省略 ETA 导致 CTA 右移 0.0049）。首轮修两处：驾驶舱 meta 文案过长换行（改「今天」相对日期）、动态句子溢出姓名列（加省略号）。`app-contacts-dashboard-account-scope` 改断言新页面结构（actor 作用域/匿名重定向/无账号 fail-closed 意图不变）。detect-changes：6 文件 5 符号，low。
- **Network 任务 4 修订** `f692f2c7`：评审修一轮——覆盖建议行改用专属 `.btn.nw-cov-row`（设计 L565 `padding:14px`，此前复用 `.nw-hl` 的 `14px 16px`，图标左缘偏 2px）；severity→设计三组 icon 色映射（见 ⑦）；目标覆盖两行改静态 span（设计为 div，不加链接）；两屏 donut 中心统一 `people.length`；`distributionRows("source")` 增 `language` 参数（en 用英文标签，ja 回落 zh）；dashboard page 视图模型映射提到 JSX 外；`.analysis-content` 规则复制进 `NETWORK_STYLES`（任务 8 删 workspace 后下钻页保留 max-width）。`.nw-recent-row` 的 `cursor:pointer` 按像素规则保留设计声明。复跑机会页签：raw 0.0974（前 0.0977），对齐后覆盖建议带 0.0144（前 0.0149），非数据残差 0.0039 不变（+ 已批准省略 ETA 导致的 CTA 右移 0.0049）。测试 26/26、typecheck 净。
