# Orbit_0918 Web UI 替换 — 会话交接（2026-09-20，交给下一棒）

> 本文件取代 `HANDOFF-2026-09-19.md`（该文件中的 WIP 与「剩余批次」已全部完成，见下）。
> 接手前请先读 `docs/development/2026-09-17-web/EXECUTION.md` 的 0918 批次记录（权威台账）。

## 环境

- **工作树（唯一工作区）**：`/Users/li/work/orbit-web-newui-batch0-20260918`，分支 `newui/batch-0-shell-landing`
- **Web 代码**：`repos/orbits/`（Next.js，App Router）
- **主仓库** `/Users/li/work/orbit`：**勿动**
- **设计稿**：`/Users/li/work/orbit/docs/designs/Orbit_0918/`（设计稿不提交进工作树）
- **台账（每批必落库）**：`docs/development/2026-09-17-web/EXECUTION.md`

## 已交付（最新 → 最早，0918 共 27 提交）

| 提交 | 内容 |
| --- | --- |
| `0a36fc4d` | 台账：5a/5c/5d 批次记录 + AI 能力待办 |
| `f3147fc6` | **批次 5a**：/app/today 收窄纯重定向（裸进→/app/agent；?entry=→/app/agent/actions?entry=）；/app/schedule、/app/followups 改指 /app/agent/plan |
| `c1550914` | **批次 5d**：strategy 屏（先联系谁=followups.current；下一步去哪=D17 推荐四态；缺人/准备=显式等 W4 空态） |
| `c6cab88c` | **批次 5c**：plan 屏（本周重点=followups.current+执行中账本项；本周日程=appointments；概览真实计数；4 周手风琴无接口未做） |
| `3cf47882` | 台账：批次 5b 记录 |
| `4493846a` | **批次 5b**：actions 屏（三档复用账本既有归属：awaiting→需要你决定、approved/executing→建议今天做、deferred→可稍后；今日进度环=账本真实计数） |
| `30d77c55`…`866b90de` | 上一棒交接文档 + 批次 0–4c 共 22 提交（落地页/导航/Events/个人中心/Network/报名/connect/home 四卡/agent 控制台换肤） |

**上一棒遗留的未提交 WIP（actions 三文件）已由批次 5b 收尾**：补 VM 测试 10/10 → ratchet → 截图核验 → detect-changes → 提交，无丢失。

## 批次 5 验证口径（dcb3cd38 之后的基线之上）

- typecheck 净、ratchet 8/8（新屏按钮全部带 `.btn`）。
- 三屏（actions/plan/strategy）均 playwright 截图核验 0918 渲染（夹具用后已删，符合「夹具提交前必删」）。
- detect-changes：5b/5c/5d 纯新增无变化；5a 为 8 文件 4 符号 low。
- 定向回归 409/411；2 个失败均为下方基线第 2、4 项。
- **全量回归（tests/pages 152 文件）2026-09-20 已启动**，日志 `/tmp/full-regression-pages-1.log`；结果未落台账前以该日志为准。

## 剩余事项（全部是决定类 / 环境类，无编码遗留）

### 等用户决定 / 审批

1. **plan「4 周 AI 计划」手风琴**：无服务端接口，静态版已交付（c6cab88c）；AI 版需模型预算审批后立项。
2. **strategy「四段策略 AI 版」**（缺什么人/准备什么）：依赖 W4 策略生成能力（W4 逻辑层未交付），当前为显式等 W4 空态（c1550914）。
3. **plan/strategy 的 iOrbit home 挂链**：现入口仅 /app/schedule、/app/followups 重定向 + 直接 URL；home 是否加导航入口待拍板。

### 环境阻塞，恢复后补跑（均为基线自带，干净 HEAD 377609b6 复现，不顺手修）

1. **云端 PG 配额**：event-registration-readback（15 项）+ app-event-registration-guide（13 项）是 mode:"live" 真实库集成测试；免费档云库 `data transfer quota exceeded`，配额重置后补跑（或配本地 PG）。
2. **本地测试库**：app-register-live-route-services + app-registered-event-lifecycle（7 条）需 `npm test` 的 `assertLocalTestDatabases()` 前置（本地 PG 测试库）；直跑 `node --test` 必失败。
3. **sibling playwright**：event-registration-portrait-browser（1 条）写死 `createRequire('../orbit-app/package.json')` 借 orbit-app 的 playwright Chromium；该依赖当前未就绪。
4. 另两条既有基线：app-agent-contact-recommendations（本地化 fixture 漂移）、app-events-live-route-services（agenda clocks 演示目录日期过期）。manifest 11 条 P1 全部在锁区文件（agent-chat-history-organization 9 + orbit-real-agent 2），无一条来自 0918 新屏。

### 可选收尾

- Bridge 记录：/app/today、/app/schedule、/app/followups 改重定向 + 新增 /app/agent/{actions,plan,strategy} 属客户端可见行为变化，如需跨端台账可记入 `bridge/handoffs.md`（尚未记）。
- 「四段策略 AI 版 / 4 周 AI 计划」已记入 EXECUTION.md 待办段。

## 工作法（必须复用）

- **所有 Bash 命令显式带工作树 cwd**；默认 cwd 是主仓库，已踩过。
- 测试：`node --test --import tsx <files>`（cwd=repos/orbits）；typecheck `npm run typecheck`；ratchet `tests/ui/orbit-button-ratchet.test.ts tests/ui/orbit-scale-ratchet.test.ts`。
- 提交前：`node .gitnexus/run.cjs detect-changes --scope all --repo .`（工作树根）；编辑既有符号前 impact（AGENTS.md 强制；Next 路由 default export 的 impact 必为 UNKNOWN，用文本搜索确认无代码调用方即可）。
- 截图夹具法：tests/tmp-XX-harness/（entry.tsx createRoot + build.mjs esbuild ESM + alias（"use server" action / service-factory）到 stub + banner `globalThis.process = ...` + SSR 输出 OrbitReferenceStyles 进 html head + 本地 http 服务 + playwright；file:// 会被 ESM CORS 拦）；**夹具提交前必删**。5b/5c/5d 的 stub 与入口写法已验证可复制。
- 0918 token 值：--text #0E1225 / --text-2 #3B3F7A / --text-3 #6B6F99 / --text-4 #9FA3C4 / --bg #FBFBFE / --surface #FFF / --border #E8E9F6 / --accent #4B4FC7 / hover #2E3270 / serif `'Noto Serif SC','Songti SC','SimSun',serif` 900。
- plan/strategy 数据通道：facts 快照经 `refreshHomeDashboardAction`（运行时动态 import + window 守卫，SSR/测试零副作用）；账本经只读 `GET /api/agent/ledger`；D17 推荐在 snapshot.recommendations。
