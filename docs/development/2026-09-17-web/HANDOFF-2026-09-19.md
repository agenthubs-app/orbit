# Orbit_0918 Web UI 替换 — 会话交接（2026-09-19，交给 Kimi Code）

> **已作废（2026-09-20）**：本文档列出的 WIP 与「剩余批次」（5a/5b/5c/5d）已全部完成交付。
> 当前状态请以 [HANDOFF-2026-09-20.md](HANDOFF-2026-09-20.md) 为准。
> 以下为原始交接内容，保留备查。

---

# Orbit_0918 Web UI 替换 — 会话交接（2026-09-19，交给 Kimi Code）

## 环境

- **工作树（唯一工作区）**：`/Users/li/work/orbit-web-newui-batch0-20260918`，分支 `newui/batch-0-shell-landing`
- **Web 代码**：`repos/orbits/`（Next.js，App Router）
- **主仓库** `/Users/li/work/orbit`：HEAD `377609b6`（chat-agent 分支），干净，**勿动**
- **设计稿**：`/Users/li/work/orbit/docs/designs/Orbit_0918/`（`iOrbit.dc.html`、批次 4 提案 `iorbit-batch4-proposal-2026-09-19.md` 等；设计稿不提交进工作树）
- **台账（每批必落库）**：`docs/development/2026-09-17-web/EXECUTION.md`（工作树根，docs-only 单独提交）

## 已交付（22 个提交，最新 → 最早）

| 提交 | 内容 |
| --- | --- |
| `d847d2f1` | 台账：回归收尾 + 4d 范围结论 |
| `42cc1434` | 回归修正（connect 占位改非交互元素；测试色值随 0918 更新） |
| `dcb3cd38` | **批次 4b/4c**：agent 控制台 0918 换肤（CONSOLE_STYLES 末尾追加作用域 token + 根节点 inline --text-3/4 改色值） |
| `acb4c249` | **批次 4a**：iOrbit home 今日四卡（今日日程/月历微件/联系人机会/继续对话，消费 D25 home-facts，真实四态） |
| `82cb3f80` | connect 连接屏「即将开放」占位（四张集成卡，无假 OAuth） |
| `d3e6fd8d` | **批次 1c**：报名屏保留 0066 逻辑、UI 换 0918 |
| `866b90de…d167f209` | 批次 0–3e：落地页/顶部导航/Events/个人中心/Network 等 16 提交 |

## 用户最新三项拍板（2026-09-19 12:00，提案 §四）

1. actions 三档分档规则：**「做了」= 复用操作账本既有状态归属**，不发明新规则
   （awaiting_confirmation→需要你决定；approved/executing→建议今天做；deferred→可稍后）
2. plan/strategy：**先静态聚合**，AI 能力版写入待办
3. 入口：`/today`、`/home` 重定向到 iOrbit home（`/app/agent`）。**`/home` 已是纯重定向（不用动）**；`/app/today` 待做

## 未提交 WIP（工作树未跟踪，勿丢）

`repos/orbits/app/(app)/app/agent/actions/`（批次 5b，typecheck 已过，**尚未写测试、未截图、未提交**）：

- `actions-route-view-model.ts` — 三档分组 + 今日进度环计数，复用 `createAgentLedgerService().listEntries`
- `orbit-agent-actions.tsx` — 0918 样式 server component；写操作复用 `OrbitTodayDecisionForm`（待确认条目）与 `OrbitAllActionsControls`（撤销/重试/取消）
- `page.tsx` — auth + actor + ledger service，壳同 today/page.tsx

**接手第一步**：给 composer 写测试（模式照抄 `tests/pages/app-all-actions-route-view-model.test.ts`）→ ratchet（新按钮必须带 `.btn` 类，否则提上限+注释）→ 截图核验（夹具法见下）→ detect-changes → 提交 → 台账。

## 剩余批次

- **5a 重定向**：`/app/today` → 裸进 `/app/agent`；带 `?entry=` 深链 → `/app/agent/actions?entry=`（决策深链落 actions 屏）；`/app/schedule`（现重定向 `/app/today#arrangements`）→ 建议改指 plan 屏；`/app/followups`（现 → `/app/today?view=day`）同步更新。26 个测试文件引用 `/app/today`，路由级断言需改为断言重定向；VM 单测不受影响
- **5c plan 屏**：本周重点任务（followups.current + executing 账本项）+ 本周日程（home-facts appointments）+ 概览计数；「4 周推进手风琴」无接口 → 不做，记待办
- **5d strategy 屏**：先联系谁（followups）+ 下一步去哪（D17 活动推荐）；「缺什么人/准备什么」无接口 → 显式「等 W4」空态，不放 mock

## 工作法（必须复用）

- **所有 Bash 命令显式带工作树 cwd**；默认 cwd 是主仓库，已踩过
- 测试：`node --test --import tsx <files>`（cwd=repos/orbits）；typecheck `npm run typecheck`；ratchet `tests/ui/orbit-button-ratchet.test.ts tests/ui/orbit-scale-ratchet.test.ts`
- 提交前：`node .gitnexus/run.cjs detect-changes --scope all --repo .`（工作树根）；编辑既有符号前 impact（AGENTS.md 强制）
- 截图夹具法：tests/tmp-XX-harness/（entry.tsx createRoot + build.mjs esbuild ESM + alias next/navigation、next-auth/react、next/dynamic 到 mock + banner `globalThis.process = ...` + SSR 输出 OrbitReferenceStyles 进 html head，浏览器 bundle 不渲染它 + CONSOLE_STYLES 正则提取注入）；playwright 需本地 http 服务（file:// 会被 ESM CORS 拦）；**夹具提交前必删**
- 0918 token 值：--text #0E1225 / --text-2 #3B3F7A / --text-3 #6B6F99 / --text-4 #9FA3C4 / --bg #FBFBFE / --surface #FFF / --border #E8E9F6 / --accent #4B4FC7 / hover #2E3270 / serif `'Noto Serif SC','Songti SC','SimSun',serif` 900

## 基线自带失败（非本分支引入，不顺手修）

1. event-registration-readback 15 项 + app-event-registration-guide 13 项：云端 PG「data transfer quota exceeded」，配额恢复后需补跑
2. app-agent-contact-recommendations 1 条：本地化 fixture 漂移（干净 HEAD 复现）
3. app-events-live-route-services agenda clocks 1 条：既有
4. audits 13 条 P1（与主仓库 377609b6 完全一致）
5. app-register-live-route-services + app-registered-event-lifecycle 7 条：需本地测试库环境（npm test 前置 assertLocalTestDatabases）
6. event-registration-portrait-browser 1 条：需 sibling orbit-app 的 playwright
