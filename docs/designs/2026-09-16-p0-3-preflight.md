# Orbit P0-3 正式切换前置检查

## 当前结论

用户已确认没有真实历史数据，只有 mock/fixture，并最新授权将 Demo 数据作为 Production 运行数据供测试，正式上线前统一清理。Production 空库核验、完整事务复制和正式部署均已完成；保留 `workspace:orbit-demo-fixtures` 以区分测试来源。历史源库连接、旧备份恢复和历史业务迁移均不适用。下方 schema-only / 禁止导入结论均为本次授权前的历史记录，不再限制当前操作。当前执行顺序以 [修订计划](2026-09-15-data-and-launch-readiness.md#当前执行计划2026-09-16-用户确认后修订) 为准。

## 本地 mock / seed 数据盘点（2026-09-16）

### 最新 Production 导入结果（覆盖下方历史空库状态）

- 导入前实际只读确认 Production 无业务数据；比较两端列定义、约束、五个迁移账本完全一致。
- 2026-09-16 从 Neon Demo 的 repeatable-read 快照复制到 Production，同一 SERIALIZABLE 事务先锁定并检查 56 张业务表为空；按外键顺序插入，未禁用约束，事务末核验逐表 SHA-256 后提交。
- 已提交 9,217 行，其中 `orbit_records=8,703`、canonical events=16、公开活动=13、membership heads=70（64 活跃 / 6 取消）、membership versions=76；账户、归属、版本及审计保持原值。未导入或覆盖迁移账本。
- 源快照和逐表摘要位于本机受限目录 `/Users/li/.config/orbit/production-test-2026-09-16/`，不进 Git；不含数据库连接串，但含测试账号密码哈希，按敏感备份保护。
- Vercel Production 的 `ORBIT_WORKSPACE_ID` 已设置为 `workspace:orbit-demo-fixtures`，数据库连接与 `AUTH_SECRET` 未改变。正式站发布及三类测试账号登录已验证；主测试账号看到 66 位联系人。
- 本次为一次性空库导入，不能向已有测试写入的 Production 重跑；未来清理以整个测试 workspace 为边界，清理动作不在此次执行范围。
- 后续已完成：13 个主办方静态身份对齐到 credentials 登录身份，26 条旧 account/profile 可恢复停用，active accounts=84；新增未来测试活动 `DEMO20261018`，参与者报名/刷新/取消/刷新通过。19 项相关回归通过。
- 最新正式发布：`dpl_5JaVX9ko5phE3T4cdFTGcmmcMFxs`，别名 `https://orbit-puce-kappa.vercel.app`；已包含 Web 取消请求 JSON/版本修复。

本地确实存在大量可重建的伪造数据，但它们不等于历史源库：

- `repos/mockdata/exports/local_seed.json` 与 `demo_seed.json` 各包含 132 用户、13 活动、500 报名、132 联系人、900 关系、1,056 互动、400 消息、240 条 AI 分析和 350 条匹配推荐，并附带 golden/negative/dirty 测试案例。
- Web runtime 的 `defaultMockFixtures` 标记为 `mock_fixture_generated_relationship`，生成时间为 2026-07-28；在补齐主办方账号、活动运营生命周期和 canonical Event Core 投影后，独立 Demo 实际校验为 8,365 条，覆盖既有生成 fixture 与活动演示记录；仓库已有 `seed-live-generated-fixtures.ts` 和验证脚本。
- 生成说明明确这些数据来自 deterministic mockdata generation / MiniMax plan，不是从用户历史数据库导出的业务快照。

处理结论（最新授权）：这些数据可进入 Production 供测试，使用真实 live 读写链路，但不声称其为真实用户、联系人或活动历史。导入前 Production 业务表为空；测试期保持独立 workspace，正式上线前清理。

### 独立 Neon 演示分支验收（2026-09-16）

- Neon 项目：`orbit`；分支 `orbit-demo-fixtures-2026-09-16`，分支 ID `br-cold-term-b31l12kv`，父分支为 production，设置了 7 天后过期。
- 目标 workspace：`workspace:orbit-demo-fixtures`。导入使用仓库已有的 `defaultMockFixtures`，没有从 Production 读取或臆造历史业务数据。
- 导入结果：8,365 条记录；仓库自带 verify 逻辑返回 `success=true`，各 collection 数量和账号归属校验通过。另完成 14 个 organizer 账号/profile 投影、16 条 published canonical event rows、13 条公开活动目录记录，以及 `event_signup_01` 的 64 个活跃 membership 和 6 条取消历史校验。
- 导入范围：只写入该演示分支；Production 分支保持 61 张 schema 表、业务数据为 0。
- 安全边界：连接串只在本地进程内存中用于导入和部署级变量注入，没有写入文档或提交仓库。演示分支到期后应删除或重新建立，不得当作正式数据源。

## 2026-09-16 检查结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| Neon production 分支 | 可访问 | 项目 `orbit`，production 分支 ID 为 `br-shy-rain-b3281a9u` |
| production 数据库 | 可只读连接 | 数据库 `neondb`，schema `public` |
| production 非系统业务表 | 61 | schema-only 初始化后只读核对；所有非迁移账本业务表均为 0 行 |
| production schema apply | 已完成并通过幂等验证 | 用户已明确批准；执行 `scripts/migrate-web-runtime.ts`，未执行 seed、未导入业务数据；幂等重跑 exit 0 |
| production 迁移账本 | 通过 | `event_ops_schema_migrations=15`、`event_ops_experience_schema_migrations=1`、`event_analytics_schema_migrations=1`、`appointment_schema_migrations=4`、`bc_ingest_schema_migrations=5` |
| 隔离演练分支 | 仍存在 | `p0-2-migration-rehearsal-2026-09-15`，schema-only；用于迁移验证，不是生产数据源 |
| Neon 独立演示分支 | 已完成 | `orbit-demo-fixtures-2026-09-16` / `br-cold-term-b31l12kv`；8,365 条 Demo 记录及 canonical Event Core / organizer 校验通过；与 Production 隔离 |
| 预览 Web/API health | 通过 | 已登录浏览器访问 `/api/health` 返回 `success=true`、`service=orbit-runtime`、`status=ok`、`mode=live`；Vercel inspect 显示绑定到 Ready Preview deployment `dpl_CU83LWci1csY869M9kma84X2RYNW` |
| Vercel 项目 | 已确认 | 项目为 `liqys-projects-33c8ddec/orbit`；Production 已配置运行时环境变量；本次演示 Preview 使用 deployment-level 变量注入 Neon 演示分支，未改 Production |
| Vercel production alias | 已通过 | 正式 alias 为 `https://orbit-puce-kappa.vercel.app`；deployment `dpl_7bYm7voeNXYambJq7XGZMxKFch1w` 状态为 Ready，`/api/health` 返回 `success=true`、`status=ok`、`mode=live` |
| Vercel demo Preview | 已通过部署检查 | `https://orbit-19l2tw77g-liqys-projects-33c8ddec.vercel.app`；deployment `dpl_GonZxG6jitQL78axr6oWwz5wWYM8` 状态为 Ready；最新 canonical event / organizer 代码已构建成功；该 Preview 只用于 Demo，不代表 Production 已导入业务数据 |
| Web 类型检查 | 通过 | `npm run typecheck` exit 0 |
| Web 全量测试 | 未完成 | `npm test` 输出大量通过，但进程长时间无退出，已中止；不能记为全量通过 |
| 迁移单测 | 部分通过 | 迁移清单/SQL 断言通过；需要真实 PostgreSQL 的用例因当前命令的 DNS 网络限制未完成，不能替代隔离分支证据 |
| 2026-09-06 备份归档 | 未找到 | 旧文档记录过 `.artifacts/db-backups/20260906T104748487Z/public.dump`，但当前工作区和 `/Users/li` 可读范围未发现该文件；不能假设备份仍可恢复 |

### Production schema-only apply 验收（2026-09-16）

- 目标：Neon `orbit` production 分支 `br-shy-rain-b3281a9u` 的 `neondb.public`。
- 执行：仓库官方 `scripts/migrate-web-runtime.ts`；不执行 seed、不导入业务数据、不启用本地/云端双写。
- 结果：61 张 public 非系统表；5 个迁移账本计数为 15、1、1、4、5；关键表 `orbit_records`、`event_ops_events`、`event_ops_membership_heads`、`event_ops_experience_versions`、`event_analytics_roi_snapshots`、`appointment_outbox`、`bc_ingest_batches` 均存在；所有非账本业务表为 0 行。
- 幂等性：完成后再次执行同一入口，返回 `Web runtime schemas migrated; no demo data seeded.`，exit 0。
- 边界：这只证明 production schema 已就绪，不证明历史业务数据已迁入，也不证明线上 production alias 已可用。

## 配置安全修复

发现工作区 `.env.local` 的 `ORBIT_EVENT_DATABASE_URL` 行末存在异常字符，导致运行时迁移测试报 `Invalid URL`；该行已恢复为有效连接格式，并将文件权限收紧为 `600`。没有更换 Neon 数据库、没有输出或提交凭据。临时恢复文件已删除。

由于本轮早期诊断曾让连接凭据片段进入工具错误上下文，正式上线前应轮换 Neon role 密码，并同步更新本地、预览和生产部署环境。该轮换尚未自动执行；当前 Production 已部署并验证，轮换需要随后同步更新 Vercel Production 与本地配置。

## 历史 Demo 验收缺项（已由顶部 Production 结果及修订计划取代）

- 数据语义：继续校验账号/owner、跨域引用、canonical membership 激活证据和活动时间窗口，不能只用条数判断完整。
- 登录与角色：未登录跳转已验证；主办方凭据登录成功、进入本人后台及另一账号拒绝访问尚未完成。
- 跨端：尚无当前 Neon Demo 同账号 Web→App、App→Web 业务写入与刷新回读证据。
- 环境：确认运行时实际选中的数据库变量、workspace 与 App API 地址一致；Demo 分支有到期时间，使用期间需留意。

旧源库快照、旧备份恢复和历史迁移 manifest 已撤出本轮。未来接收真实用户数据之前安排备份策略，不阻塞当前 Demo。

## 下一步执行顺序

1. 核对现有 Demo seed 的账号、权限、canonical event/membership 和时间语义，修复已证实的问题。
2. 验证 Demo 主办方登录、活动后台访问与跨账号隔离，再验证参与者报名流程。
3. Web 与 App 连接共同 API，以同账号同记录完成两方向写入和刷新回读。
4. 记录验证版本、结果和精确未完成项；只有实际跑过的业务链路才标记完成。
