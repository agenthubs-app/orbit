# 云端统一数据目标：最小剩余清单

更新时间：2026-09-16。范围是本轮已授权的数据整理、Production 合成数据测试和 Web/App 共同使用 Neon，不把全部产品 backlog、视觉重做或高级集成追加为前置条件。

## 已固定的边界

- Neon Production `workspace:orbit-demo-fixtures` 是当前测试事实源；Web/API 访问 Neon，App 只经同一 HTTPS API。fixture 是数据来源标签，不是运行时 mock 服务。
- 不再需要历史源库或旧备份：没有真实历史数据需要恢复。已存在的云端合成数据不重复导入，不用旧 seed 覆盖测试修改。
- 人脉生命周期任务和普通待办的完成语义不同：前者必须确认关系下一步，后者可以直接完成/恢复。不能只为让列表可见就改写全部旧记录，也不能保存两份可独立修改的状态。
- 清理合成数据留到真实上线前；当前保留主账号、主办方和交互账号供验收。

## 本轮修复

| 修复 | 代码/验收状态 |
| --- | --- |
| raw 登录主体与 canonical 业务账号混用 | `6f844ea47` 已部署；iOrbit 正确读取 66 人脉/66 跟进；原有个人日程可编辑保存并刷新回读，未重复创建。 |
| AI 搜索拿整句指令匹配标题 | `6f844ea47` 分离 searchTerms 与原始授权 query；工具已经在线命中指定任务。 |
| AI 查到结果却只回复“我来查询” | `40b63d218` 为两步执行或 synthesis 失败补有界事实回显，并保留三步摘要的状态字段；108 项回归、Web typecheck 通过。最终 Production 实际回复指定测试任务的标题、`status: open` 及已读/未读领域。 |
| 测试依赖宿主时区/旧 provider 结构 | `6597d8de8`：显式设置测试浏览器时区，更新事务 runtime 断言；Web 16/16，App 上海和 UTC 各 25/25，两端 typecheck 通过。 |
| 关系跟进在普通待办页不可见 | `105ebba4d` 独立只读展示当前跟进和历史，链接联系人上下文；缺失/冲突关联不给操作链接。27 项组合回归、Web typecheck 通过。不转换 Production 记录、不改变任务写入契约。联系人详情当前关系阶段为只读，完整处理入口列入 R1。 |

GitNexus impact 的 HIGH/CRITICAL、partial 与 detect-changes 的 low/零流程相互不一致，且曾 SIGSEGV/UNKNOWN；不认定完整图分析通过。各修复另外进行了源码调用复核、定向测试和线上验收，不用低风险输出覆盖高风险警告。

最终运行源码 `105ebba4d`，部署 `dpl_2FjxtX314B6DRojeZvFbdDF8gTNh` 为 Ready，正式入口仍为 [orbit-puce-kappa.vercel.app](https://orbit-puce-kappa.vercel.app)。从精确已提交 Web 子树发布，没有带入根目录用户改动。Production 待办页保留主账号原有普通任务，显示当前跟进 66 / 历史 14；另一主办方账号显示当前跟进 0，未泄露主账号记录。AI 查询正文已正确显示标题和 open 状态，并持久化到该 canonical actor 的会话。个人日程原记录编辑后成功回执与刷新回读通过。没有批量改写/删除 Neon 数据，没有发送外部消息。

## 完成当前测试目标仍必须做

| 顺序 | 必要项 | 为什么还需要 / 最小完成标准 | 依赖 |
| --- | --- | --- | --- |
| R1 | 人脉跟进的两端可见性和处理闭环 | Web 联系人详情实际显示“来源数据·只读”，无完成按钮；Tasks 的新增只读分区只修可见性。补 Web/App 对现有生命周期服务的完成+下一步消费；同一跟进完成后两端重开状态一致，旧跟进不再算当前，不能套普通待办 complete。 | Web 可见性修复；先核实现有 HTTP 完成边界再补消费者，不能只链接一个只读页面声称闭环。 |
| R2 | 为缺失的数据链补最小实例 | 现有数据没有 note，40 条通知也不构成待办提醒链。只补一条关联笔记、一条真实 ReminderPlan→Delivery→Notification→详情链；验证归属、点击对象、已读/刷新、去重和失败重试。再核验 AI 四域只读；空样本不能记通过。 | 当前云端服务；对应 worker 可执行。 |
| R3 | 云端活动 worker 真正执行 | 当前 Vercel 四类队列与 maintenance 未承载 event-operations；本机 worker 测试不等于云端运行。接通有界入口、持久触发、lease/fencing、幂等和重试，验收主办方生成→发布→参与者回读，不依赖本机常驻。 | 可独立于 App 先做；优先现有 Vercel，不默认新增付费平台。 |
| R4 | 原生 App 使用 Production 做双向回读 | 当前仅有自动化和部分 HTTP 客户端证据。本机实际 `xcodebuild -version` 为 26.1.1，只有 `/Applications/Xcode.app`，与当前 Expo 原生依赖不兼容。先获得兼容工具链并构建安装，再完成 Web→App、App→Web 的同记录编辑、完成/恢复、刷新、账号切换隔离。 | 原生工具链；不能用浏览器跨域绕过或 mock 替代。 |
| R5 | 用新账号贯通一次最短主流程 | 现有账号登录和报名已测，不等于新用户首次流程通过。走注册/可用登录入口→基础资料→未来活动报名→匹配→添加人脉→iOrbit→待办/日程，并逐步刷新回读；另一身份检查权限。Google 登录若保留对外入口，也需实际回调与资料初始化验证。 | R3 负责匹配生成；最终双端验收依赖 R4。 |

执行顺序：R1/R2 先补数据消费证据；R3 与 R4 环境准备可并行；最后 R5 串行贯通。R4 受工具链阻塞时，不阻塞 Web、数据或云端 worker 的独立工作。

R1 已核实到的最小缺项：App `/followups` 仅重定向到通用 Tasks 的人脉筛选；Tasks 的完成/恢复只调用 generic `/api/tasks/:id`，联系人详情没有 lifecycle completion UI。现有 `/api/connections/:id/stage` 也只接受轻量 stage 输入，不是 `completeTask` adapter。下一步应复用现有 lifecycle 读取/事务服务，补 actor-scoped HTTP 契约、两个 expected version 与幂等键，再让两端显式选择 `next_task / active / nurture / archived`；覆盖四种 outcome、重复提交、旧版本冲突和异账号拒绝。无需为此迁移 80 条旧记录或复制另一套状态。

原生依赖健康检查保留 3 类已发现项（expo-font peer、Hermes 提示、SDK 建议版本偏差）；在准备原生构建时按实际错误逐项核实，不无条件全量升级、降级依赖或修改 node_modules。

## 真实对外上线前才执行

1. 备份当前测试 workspace 并确认清理范围，包含本轮 QA 新增数据；按用户授权清理，确认正式 workspace 没有合成业务记录，停用测试凭据。
2. 落实 Neon 备份/恢复路径并做一次恢复验证；这是未来真实数据的保护措施，不是寻找不存在的历史备份。
3. 轮换已暴露/测试用凭据、核对生产环境变量和最小权限；敏感值继续仅保存在受限配置，不写入计划或仓库。

暂不新增：第二个数据库、业务双写、本地离线写入同步、多套队列平台、批量重造全部 fixture。实体推送、外部 Calendar、OCR 扩展等按各自已批准范围另验，不冒充本轮云端数据目标的已完成项。

## 关闭条件

当前目标只有在同一 Production API + Neon 下，两端原生/浏览器实际完成相同记录的读写、权限和主要流程，且所需后台作业不依赖本机时才可关闭。代码存在、typecheck、单元测试、一次 commit 或 Web 发布成功均不足以关闭整体目标。
