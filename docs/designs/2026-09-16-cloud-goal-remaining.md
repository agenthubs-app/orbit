# 云端统一数据目标：最小剩余清单

更新时间：2026-09-17。范围是本轮已授权的数据整理、Production 合成数据测试和 Web/App 共同使用 Neon，不把全部产品 backlog、视觉重做或高级集成追加为前置条件。

## “全部做完”后的执行增量

用户已要求完成全部剩余项；原 $1 模型验收预算的未用余额用于剩余原生流程，不另加预算。App canonical 参会者／交换／通知入口 `5ee62479`、登录5xx分类 `c5e7b124`、Web 旧状态写入旁路修复 `d0999d5f` 已提交。代码、发布和原生实测各自记录，不用一项通过代替另一项。

当前新阻塞是 **Neon Free 网络传输额度耗尽**，不是 Xcode 或密码。控制台显示本月 7.59 GB，Production SQL 返回 data transfer quota；Vercel 的 `/api/auth/mobile/credentials` 错误日志同样明确命中该额度，HTTP 返回500。未确认新原生 QA 账号创建成功，不继续重试注册、改密码、改期、重置关系或换免费项目来掩盖阻塞。

- 独立工作已完成：App全量2945/2945，Web相关297/297（均零跳过）、两端typecheck、iOS export、真实隔离PostgreSQL＋HTTP混合请求零写入/归属隔离/私有备注冷读、真实Web mapper→App decoder六状态6/6。Web `d0999d5f`已发布，Production `dpl_AYtJfqdDkAPfQiGoANYzKJ9k2e5t` Ready，正式别名不变。
- 可调查：Neon 用量及失败日志；全量集合读取可能放大传输，但尚无证据归因全部7.59 GB，不以猜测改缓存或删数据。
- 等待决定：恢复现有 Neon 项目的可用额度；升级 Launch 为独立的按量收费授权，不包含在 $1 模型费内。未购买或升级。
- 恢复后依次执行：确认现有数据可读→创建独立空测试活动（dry-run/hash/事务，拒绝覆盖旧活动）→正常报名/画像→云端生成与发布→原生目录/推荐/座位/签到→双方交换及通知直达→原生新 pending 本人初始化→Web/Neon 回读、跨账号隔离及预算结算。

以上最终源码已重验。原生真实登录的负例显示“登录服务暂时不可用”，不再误报密码错误；仍不标记 Production 原生正例完成。01:19:53Z 模型账户余额63.51CNY，原基线63.62，差额0.11CNY；本次没有新增模型调用。旧“剩余预算范围未确认”及下节代码缺项被本节覆盖，Neon 付费决定没有被替代。

## 历史：Xcode 修复后的首轮增量（剩余项以顶部为准）

Xcode27签名原生构建已成功；iOS26.1 Simulator实际接入Production5961cdde＋Neon，普通待办完成/恢复、日程双向修改、关系跟进完成并明确下一步、笔记持久化、任务提醒详情、云端AI历史及账号切换已经实测。R4的工具链和基础双向读写要求已解除阻塞，不能再列“等待Xcode”。App`3af3eefe`补本人初始化表单，`5ac0e9fd`补旧fixture的canonical读取/私有编辑边界并移除看板preview写操作；原生详情及正式跟进入口已复验，新pending的Production提交正例未验。

只保留本轮确证的缺项：活动交换通知无等价原生参会者详情，活动参会者入口实际失败；服务端旧status投影接口仍需限制，App禁用旧写入不等于服务端已修复；新pending正例需要经正常新活动/画像/发布/交换产生对象（不重置已有关系），此前$1余额能否用于native仍待范围确认。下一步先修原生活动数据/目标页面和服务端旧接口边界，再在预算范围确认后跑新活动正例。实体推送、Release商店发布、外部日历和真实上线清理不扩为此次前置条件。实际证据与全量失败/后续定向回归记录见[BR-026](../../bridge/2026-09-16-cloud-five-item-acceptance.md)。

## 五项闭环执行进度（本轮最新）

以下进度覆盖下方历史基线；详细证据见 [BR-026](../../bridge/2026-09-16-cloud-five-item-acceptance.md)。Web＋Neon＋云端worker本轮主流程已完成实际验收；整体仍未关闭，不以自动化替代原生流程。

最终App提交`5ac0e9fd`在固定测试时区`Asia/Tokyo`下全量2898/2898、0失败/跳过，typecheck通过；早期全量失败记录保留。实际原生/Web/Neon日期按Asia/Shanghai另验。此结果不关闭下面尚缺的真实业务正例。

| 项 | 已实际完成 | 仍须完成 |
| --- | --- | --- |
| R1 | Web/App共用生命周期HTTP/Schema；Web显式初始化与完成后状态回读已验。App3af3eefe/5ac0e9fd补初始化及canonical入口，实际完成旧跟进并创建明确日期下一步、Web同记录回读、详情私有编辑和任务入口复验通过。 | 新pending原生初始化Production正例；服务端legacy status投影写入边界。无须再等工具链。 |
| R2 | 真实Note→模型回读；ReminderPlan→云端唯一Delivery→收件箱详情/已读/维护去重。四域iOrbit与异账号隔离已验；本轮原生创建关联笔记并冷启动回读、打开真实云端任务提醒和已有AI笔记查询历史通过。 | 基础原生消费已验；本轮未新增原生模型调用，不能把历史回读当新调用正例。 |
| R3 | 正常报名后主办方生成，Vercel worker 5/5任务一次完成，原子发布后新账号实际读到推荐／座位／破冰问题并成功签到；81条outbox completed。具体generation/publication/worker ID见BR-026。 | 云端生成→发布→参与者回读已验，无须本机常驻。 |
| R4 | Xcode27.0签名构建成功，iOS26.1 Simulator实际登录远程Production；普通任务完成/恢复与日程同记录双向回读、关系完成/新日期、冷启动、主账号66人脉↔主办方1人脉隔离通过。未修改node_modules或弱化SecureStore。 | 基础原生运行与双向云端读写已验；剩余具体业务入口归R1/R5，不再等待Xcode。不是实体设备或自包含Release验收。 |
| R5 | Web新账号注册/资料/任务/日程、报名/画像/云端匹配发布/签到/双方交换与独立初始化已验，Production5961cdde。原生登录、账号切换隔离和既有关系/任务/日程/笔记真实读写已验。 | 原生活动参会者/交换详情入口、报名状态文案与服务端一致；修复后新活动完整原生旅程及pending本人选择正例。 |

本轮新增最高 $1 模型预算已获明确批准，14:18:20Z建立独立余额基线，16:44:18Z账户差额CNY0.11（包含并发用量，非逐请求精确账单），远低于CNY5保守停线；旧账本缺失不再阻塞本轮。最终Production两轮真实AI查询、Neon四消息/独立回复ID、刷新重开完整历史、任务及建议零新增均通过。现场交换已用独立纯测试活动验收，未修改原活动日期或绕过门禁。后续初始化修复及验收没有新增模型调用；Web规则和实际流程缺口现已关闭，原生工具链与消费对齐单列。Google登录未配置且UI不展示，本轮不适用，不扩建OAuth。

未重置/清空/重seed Production。早期只修补关系任务缺失元数据字段；最新双方初始化和任务完成均经正式UI/API，允许的状态/版本变化详见BR-026，不再称其“未改变”。修改前0600快照及hash已记录；原生验收使主办方普通任务增加完成/恢复轨迹，最终仍未完成，不能沿用上轮“三条普通任务payload未变”的结论。原生新增测试日程、笔记和一个明确下一步，未删除既存记录。未来真实上线前清理仍需单独执行。

## 已固定的边界

- Neon Production `workspace:orbit-demo-fixtures` 是当前测试事实源；Web/API 访问 Neon，App 只经同一 HTTPS API。fixture 是数据来源标签，不是运行时 mock 服务。
- 不再需要历史源库或旧备份：没有真实历史数据需要恢复。已存在的云端合成数据不重复导入，不用旧 seed 覆盖测试修改。
- 人脉生命周期任务和普通待办的完成语义不同：前者必须确认关系下一步，后者可以直接完成/恢复。不能只为让列表可见就改写全部旧记录，也不能保存两份可独立修改的状态。
- 清理合成数据留到真实上线前；当前保留主账号、主办方和交互账号供验收。

## 本轮修复

| 修复 | 代码/验收状态 |
| --- | --- |
| raw 登录主体与 canonical 业务账号混用 | `6f844ea47` 已部署；iOrbit 正确读取 66 人脉/66 跟进；原有个人日程可编辑保存并刷新回读，未重复创建。 |
| AI 搜索拿整句指令匹配标题 | `6f844ea47` 分离 searchTerms 与原始授权 query；工具已经在线命中指定任务。 |
| AI 查到结果却只回复“我来查询” | `40b63d218` 为两步执行或 synthesis 失败补有界事实回显，并保留三步摘要的状态字段；108 项回归、Web typecheck 通过。最终 Production 实际回复指定测试任务的标题、`status: open` 及已读/未读领域。 |
| 测试依赖宿主时区/旧 provider 结构 | `6597d8de8`：显式设置测试浏览器时区，更新事务 runtime 断言；Web 16/16，App 上海和 UTC 各 25/25，两端 typecheck 通过。 |
| 关系跟进在普通待办页不可见 | `105ebba4d` 独立只读展示当前跟进和历史，链接联系人上下文；缺失/冲突关联不给操作链接。27 项组合回归、Web typecheck 通过。不转换 Production 记录、不改变任务写入契约。联系人详情当前关系阶段为只读，完整处理入口列入 R1。 |

GitNexus impact 的 HIGH/CRITICAL、partial 与 detect-changes 的 low/零流程相互不一致，且曾 SIGSEGV/UNKNOWN；不认定完整图分析通过。各修复另外进行了源码调用复核、定向测试和线上验收，不用低风险输出覆盖高风险警告。

本轮开始前的运行源码为 `105ebba4d`，部署 `dpl_2FjxtX314B6DRojeZvFbdDF8gTNh`；最新版本以后续 BR-026 为准，正式入口仍为 [orbit-puce-kappa.vercel.app](https://orbit-puce-kappa.vercel.app)。历史基线：主账号 66 当前 / 14 历史；本轮完成一项并明确新增下一步后为 66 当前 / 15 历史。没有批量改写/删除 Neon 数据，没有发送外部消息。

## 初始任务基线（完成情况以顶部最新进度为准）

| 顺序 | 必要项 | 为什么还需要 / 最小完成标准 | 依赖 |
| --- | --- | --- | --- |
| R1 | 人脉跟进的两端可见性和处理闭环 | Web 联系人详情实际显示“来源数据·只读”，无完成按钮；Tasks 的新增只读分区只修可见性。补 Web/App 对现有生命周期服务的完成+下一步消费；同一跟进完成后两端重开状态一致，旧跟进不再算当前，不能套普通待办 complete。 | Web 可见性修复；先核实现有 HTTP 完成边界再补消费者，不能只链接一个只读页面声称闭环。 |
| R2 | 为缺失的数据链补最小实例 | 现有数据没有 note，40 条通知也不构成待办提醒链。只补一条关联笔记、一条真实 ReminderPlan→Delivery→Notification→详情链；验证归属、点击对象、已读/刷新、去重和失败重试。再核验 AI 四域只读；空样本不能记通过。 | 当前云端服务；对应 worker 可执行。 |
| R3 | 云端活动 worker 真正执行 | 当前 Vercel 四类队列与 maintenance 未承载 event-operations；本机 worker 测试不等于云端运行。接通有界入口、持久触发、lease/fencing、幂等和重试，验收主办方生成→发布→参与者回读，不依赖本机常驻。 | 可独立于 App 先做；优先现有 Vercel，不默认新增付费平台。 |
| R4 | 原生 App 使用 Production 做双向回读 | 当前仅有自动化和部分 HTTP 客户端证据。本机实际 `xcodebuild -version` 为 26.1.1，只有 `/Applications/Xcode.app`，与当前 Expo 原生依赖不兼容。先获得兼容工具链并构建安装，再完成 Web→App、App→Web 的同记录编辑、完成/恢复、刷新、账号切换隔离。 | 原生工具链；不能用浏览器跨域绕过或 mock 替代。 |
| R5 | 用新账号贯通一次最短主流程 | 现有账号登录和报名已测，不等于新用户首次流程通过。走注册/可用登录入口→基础资料→未来活动报名→匹配→添加人脉→iOrbit→待办/日程，并逐步刷新回读；另一身份检查权限。Google 登录若保留对外入口，也需实际回调与资料初始化验证。 | R3 负责匹配生成；最终双端验收依赖 R4。 |

执行顺序：R1/R2 先补数据消费证据；R3 与 R4 环境准备可并行；最后 R5 串行贯通。R4 受工具链阻塞时，不阻塞 Web、数据或云端 worker 的独立工作。

R1 代码缺项已补齐：`GET/POST /api/connections/:id/lifecycle`、`GET /api/relationship-tasks` 及 Web/App 独立关系任务详情。完成必须明确 `next_task / active / nurture / archived`，携带两类 expected version、幂等键及须解除的确切旧任务 ID；不走 generic complete。Web 的处理入口在 Tasks 当前跟进分区，联系人来源资料继续只读。原生运行时验收仍开放。

原生依赖健康检查保留 3 类已发现项（expo-font peer、Hermes 提示、SDK 建议版本偏差）；在准备原生构建时按实际错误逐项核实，不无条件全量升级、降级依赖或修改 node_modules。

## 真实对外上线前才执行

1. 备份当前测试 workspace 并确认清理范围，包含本轮 QA 新增数据；按用户授权清理，确认正式 workspace 没有合成业务记录，停用测试凭据。
2. 落实 Neon 备份/恢复路径并做一次恢复验证；这是未来真实数据的保护措施，不是寻找不存在的历史备份。
3. 轮换已暴露/测试用凭据、核对生产环境变量和最小权限；敏感值继续仅保存在受限配置，不写入计划或仓库。

暂不新增：第二个数据库、业务双写、本地离线写入同步、多套队列平台、批量重造全部 fixture。实体推送、外部 Calendar、OCR 扩展等按各自已批准范围另验，不冒充本轮云端数据目标的已完成项。

## 关闭条件

当前目标只有在同一 Production API + Neon 下，两端原生/浏览器实际完成相同记录的读写、权限和主要流程，且所需后台作业不依赖本机时才可关闭。代码存在、typecheck、单元测试、一次 commit 或 Web 发布成功均不足以关闭整体目标。
