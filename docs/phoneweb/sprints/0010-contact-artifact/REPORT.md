# PW-0010 / run-01 delivery

Final parent registration: SC1–5 scoped verified; run-01 closed after ROOT's actual exact original-server/Appscope restoration. Overall PhoneWeb business coverage remains partial; the full backend failures/skips remain disclosed below.

The shared App/PhoneWeb chat now displays validated contact artifacts below their saved assistant turn. Existing reliable requests are recovered through the ordinary authorized history read, without modifying old messages or resending questions. When model synthesis is skipped or fails, the runtime uses the actual ready artifact for a bounded deterministic reply and preserves execution diagnostics.

## Delivery versions

- Functional commit: `58e4840169625460e06a2d69769e95552878ebb6`, based on `b802805c243ac1fb3b299ecdb641c0147251e847`;25 whitelisted files,973 additions/13 deletions.
- Phone integration: `bbe8af4ff33ff600365d426a605370267288166c`. ROOT consumed the exact25 paths into the main App runtime, reported as MAIN224/f2a.
- Parent verifies every added/deleted feature line across these25 paths matches commit58 versus MAIN224, and current main source blobs still match MAIN224. Complete-file comparison is22 equal/3 with existing Phone profile-error or Web-viewport differences; this is verified feature-delta equivalence, not a claim that all25 complete files are identical. `native-acceptance-receipt.json` records `nativeFeatureDeltaVerified:true`.
- Actual Phone backend: BUILD_ID `8sXNW8nifgU1rD0IF79lc`, PID17635; frontend PID21340,89 exported HTML files, static tree digest `688cd5587722ce3e53eb818a85aab05990fdb02c105d3f7c0ebe0f89c7bf1762`. Runtime identities describe receipt time, not perpetual availability.
- No push, Vercel deployment, history migration, new AI request or business-data write by A. Parent/ROOT own integration and runtime restoration.

## Acceptance

| Criterion | Evidence and outcome |
| --- | --- |
| SC1: actual candidates | Real shared chat renders all eight stored candidates, roles, reasons, original bodies and permitted evidence metadata in original order. Synthetic schema/view-model/render tests cover malformed, unsupported, pending, failed and empty results. No question-keyword or all-contact ranking substitutes for actual artifacts. |
| SC2: turn-bound history | Real public and native history clicks, reopening and refresh retain the same original turn and eight results. Deterministic tests cover duplicate questions, late requests, missing bindings, deleted sessions, scope changes and bounded reads. Final original request/messages hashes are exactly unchanged. |
| SC3: navigation/isolation | Explicit real public/native existing-contact navigation reaches correct details with the full encoded namespace. Public anonymous session/request/run/contact reads are denied and UI shows no candidates. Automated actual-consumer tests cover unknown actions, actor changes and failed-refresh withdrawal. Native device was not reset or signed out for a redundant anonymous test. |
| SC4: truthful bounded summary | Actual runtime tests verify loop2 makes no synthesis call, successful nonempty loop3 synthesis is preserved, failed/empty synthesis falls back to actual artifacts, and absent-ready results do not claim candidates. Original loop2 skipped receipt and text remain unchanged. |
| SC5: same-version delivery | Fixed-version public Chromium/WebKit390 independent login/history/refresh/detail/isolation passes, pageErrors0 and attemptedBusinessOrAiPosts0. ROOT-released integrated native Simulator actual login/history/eight-name scroll/detail/back/reopen/pull-refresh passes. Existing service log records matching full original session/contact paths200; attribution is time/path correlation, not exclusive device/UA tracing. ROOT confirms exact original server and original Appscope account restored; restoration gate passed. |

## Verification and retained failures

- Final App affected complete files:107/107 pass,0 fail/skip. Earlier App once-only full suite:3018/3018 pass,0 fail/skip, before the final people-fallback repair; it was not rerun or claimed as the latest complete full suite. Final App/backend typechecks exit0. Sanctioned contracts were synced; no hand-edited client copies.
- Backend once-only full suite:3546 tests,3282 pass,58 fail,206 skip; exit1, **not green**. Failures included36 DB-required cases with intentionally unavailable credentials,4 outbound-guard child-output cases,17 unchanged audit/manifest/contract/contact/home baseline failures, and1 real runtime guardrail regression subsequently fixed.
- The17 unchanged failures were reproduced at the exact clean baseline with236 tests/219 pass/17 fail/0 skip. The final actual affected runtime/provider/trace/profile/method complete files pass61/61,0 fail/skip. Full suites were not repeatedly rerun to conceal failures.
- ROOT's separately authorized two-key offline-read metadata alignment removes the five AI findings; its full14-test file still reports13 pass/1 fail for the unchanged schedule orphan. A did not expand that correction or commit ROOT files.
- Source upstream impacts were LOW for indexed runtime/handler/mapper consumers. New private-history reader was UNKNOWN in the graph and conservatively treated as HIGH, with explicit source-consumer and isolation checks. Exact postcommit GitNexus comparison maps only the25 expected files; graph gaps are not zero-risk claims.
- Native tool diagnostics retained: successful HID commands initially produced incomplete controlled input; RAM-only verification plus targeted repair resolved it. ORICO simctl screenshot permission denial was bypassed using a dedicated temporary screenshot directory, without changing ownership or permissions. Parent's initial nullable-message-user filter diagnostic was corrected through the established actor-scoped workspace and exact original hashes matched.

## Evidence and remaining closeout

Local verification and actual native observations: `/Volumes/ORICO/Dev/phoneweb-pw0010-validation/` (test TAP/typecheck logs, native actual label/frame snapshots, independent comparison receipts, eight visible viewports and safe screenshots). Parent actual public receipt and final native request/messages/budget integrity: `/Volumes/ORICO/Dev/phoneweb-runtime-20260916/`.

The original request and two saved messages match their pre-QA full SHA256 values; the budget digest is unchanged. No new model sends, result injections, invitation/edit actions, credentials output or password screenshots occurred.

A released the single DA Simulator UI to ROOT with the QA server `http://127.0.0.1:32100`. ROOT subsequently confirms a real UI save and AX verification of the exact original `http://127.0.0.1:3000` server, with the original main Appscope authenticated account naturally restored. No logout, reset, key clearing, business/AI send, Metro change or Phone service change was used. The restoration gate is passed; run-01 is ready for parent registration and closure. No further source implementation is pending; full baseline suite failures remain explicitly disclosed. ROOT retains device UI ownership.
