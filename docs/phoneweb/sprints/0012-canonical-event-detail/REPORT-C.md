# PW-0012 C / run-01 — local implementation, live acceptance still open

Baseline `ffdb6ec7130140fbe026d0e3de45efdad38f954b`, branch `codex/phoneweb-c-0012`; Planner SHA-256 `16212d8f4560fd4245d0d630095d014c5d7c71dccfb74126a331a15b4ef2f640`. ROOT exact App whitelist release and necessary strict-footer symbol confirmation reused within this same run. No new Generator/Reviewer/Evaluator line.

## Delivered behavior

Validated public `event-core-postgres` source explicitly selects the canonical consumer. Other public/private legacy sources keep existing readiness, recommendations, review and mutation flows. Canonical has one fixed `registration?questions=false` GET and a strictly validated shared footer state; no second legacy eligibility request. The fixed bottom CTA is retained, with an opt-in action check that leaves the legacy default expression unchanged.

Unavailable eligibility is visibly unavailable, not ordinary unregistered. Preparation shows only this event's persisted registration/profile facts, with absent answers explicitly unfilled. No readiness score, generated goal or fabricated recommendation is introduced. Only an active validated registration mounts the existing attendee operations GET. The consumer strips unused graph/contact-request properties and displays only referenced published attendees, persisted reasons and icebreakers; locked/not-generated/processing/failed/ready remain separate.

Artifact qualification uses validated server `eligibility.evaluatedAt` and public event end time. Missing/invalid time fails validation and prevents downstream reads. Future events display “活动结束后可用” without requesting an artifact. Ended registered events use only the existing artifact GET and distinguish queued/running/ready/failed/unconfigured. No task request, summary generation, automatic registration, outbound message, goal/opening-line write or old review confirmation occurs. The old batch candidate review/confirmation capability is not implemented by this read-only branch.

## Evidence and limits

- Affected baseline before implementation: 176/176 PASS, exit 0.
- TDD: 33 initial consumer/adapter missing-behavior RED, then GREEN; additional backend-permitted empty citation list RED then GREEN. Before coordinator feedback, consumer/adapter 34/34 and screen 20/20 passed. Latest combined three new test files: 60/60 PASS, exit 0, 9,991ms, zero skipped.
- Initial full-screen canonical RED: 5 missing-behavior failures / 1 existing pass after setup correction. Tests check exact questions-disabled URL, protected read gates, fixed footer actions, own published results, status/error visibility, refresh revocation and actor/service/route/focus changes. Coordinator feedback added six actual RED cases: reject even internally consistent foreign operations unless `me.participantId` matches the validated registration profile, and render Chinese artifact failure explanations instead of raw diagnostic codes. Both fixes are covered by the final 60/60 run. An intermediate 59/60 run exposed a test wait missing the newly accurate unavailable-state label; the wait was corrected before the final passing run.
- Existing + initial new affected run: 215/215 PASS, exit 0. App-wide run across 264 test files: exit 1, 3,075 tests / 3,059 PASS / 16 FAIL / 0 skipped, 302,875ms. All 16 failures belong to the unchanged `tasks-unification-interactions.test.ts` setup importing backend `next/server`; this worktree had no backend node_modules. The coordinator-authorized existing Phone backend node_modules directory was linked locally (ignored, no install or source edits). The failed file rerun passed 16/16, exit 0, 9,677ms. The original App-wide run is not reported as passing and was not repeated.
- Final-source TypeScript `tsc --noEmit`: exit 0. Final-source production web export: exit 0, 89 static routes, 1,435 web modules. Bundle `entry-5d4f6cab1a70398657c43cee407bc2c4.js`, SHA-256 `9bfb506360d1da1c98d80379d23ba0da3c2859b9448deb27e0e1edf69dd5b74c` (generated dist not committed). Export retained platform warnings about web notification listeners and conflicting color flags; these were not reported as warning-free validation.
- Verified zero-outbound preload selfprobe: fetch/http/https/socket 4/4 rejected before transport. Test/type/export processes block real environment and provider keys. This C run made no true business GET, DB read/write, provider/paid call, seed/date change, service lifecycle change or device action.
- Synthetic 390px visual inspection shows explicit unavailable/future states and the fixed disabled bottom CTA without clipping. This is not real-account/public/native evidence.
- Coordinator's prior true GET receipt: canonical `event_signup_03` registration200, eligibility unavailable/actions empty/registration null; operations/artifact403. Actual screenshot's legacy failures were readiness404 (stored DTO shape incompatibility), recommendations404 and post-event503 (explicit provider:null). This run does not convert those providers to success.
- Required fixed-commit public current-account state-chain acceptance remains with Phone coordinator. Real registered/ready/ended samples have not been supplied or exercised; no automatic registration/seed/date manipulation to manufacture them. Native acceptance remains ROOT-exclusive and unverified by C.

## Scope audit / handoff

Changes are only five Planner-listed App source paths, three new tests and this Sprint's C checkpoint/report. No backend/shared/auth/storage/registration mutation files changed. Fresh graph `EventDetailView` and new Phone symbols are UNKNOWN, treated conservatively HIGH with actual source consumer chains reported; existing footer impact LOW, 2 direct callers / 3 affected. The registered ROOT graph is not an exact Phone/C branch graph.

Pre-commit `detect_changes(repo: orbit-phoneweb-main, scope: staged)` was executed and returned NoChanges against that registered coordinator checkout, not this C worktree. It is not evidence that C's staged patch has no changes. Exact staged whitelist diff is manually audited here; coordinator must audit the exact cherry-picked patch against its registered integration tree before its commit. Integrate this run's fixed commit only by cherry-pick; no merge, push or deploy authorized for C. Phone server/window and ROOT native/window ownership remain unchanged.
