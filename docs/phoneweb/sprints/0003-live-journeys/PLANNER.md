# PW-0003 — 本地真实业务体验

模式：existing-codebase / single-generator。Owner：phoneweb-C，默认 `gpt-5.6-sol` / `medium`。用户已批准方案/本地实施/独立工作树，并明确稍后自行部署，本Sprint不发布公网。

## 进入与依赖

读phoneweb README/DESIGN/runtime/route-inventory、PW-0001/0002实际交接和本端AGENTS。运行登记run-01、起始SHA、Planner hash和文件锁。A/B未集成前可调查既有路由/API与准备独立行为场景；运行整套浏览器旅程必须消费A固定的生产构建/同源服务器接口，截图以B集成版本为准。全局最多两个执行Sprint，协调者释放一个槽位后才启动本线。

后端由主协调持有 `127.0.0.1:32100`，Node22生产进程、专属本地DB/workspace。C的浏览器入口用32113，自己的独立Playwright browser/context。不得用MCP共享活页面，不修改其他线/主协调的进程、账号状态或数据库。为写入和退出场景经现有注册/用户service创建本线合成账号，或由协调者移交当前QA账号的独占验收窗口；不能与A同时退出同一账号。

## 文件与操作归属

- 可新增：`repos/orbit-app/scripts/phoneweb-journey-smoke.mjs`、直接行为辅助模块与相应测试；本Sprint checkpoint/REPORT及 `docs/phoneweb/route-inventory.md` 的真实结果列。
- 本线只在自己创建的合成账号/对象中做业务写入，通过正式API/服务，不绕过权限/幂等/发布检查向表里造成功状态。允许对专属本地演示DB使用既有正式迁移及种子，但先确认目标和必要性，保留部分失败记录。
- 不改：A的代理/认证/构建、B的公共布局/视口、Next业务实现、共享契约、总登记表。发现实际phoneweb缺口先报告精确源码/场景/影响；由协调者划定单一修复owner后再实施，不两线同改。
- 运行产物/截图/临时storageState在忽略的build/phoneweb或受限临时目录，不提交token/密码，不在日志打印返回cookie或个人记录全文。
- 所有运行进程显式清空OpenAI/DeepSeek/Google/Gemini/Anthropic keys；真实AI/OCR费用未获可用账本，不调用、不用预设回答假装生成。

## 操作链与验证

- [ ] 构建当前Expo Web生产产物，启动自己的同源入口指向32100；录入实际git/build/API版本。启动前检查空闲，不杀未知进程。
- [ ] 登录后访问五入口，检查可见主标题/内容、导航、资源加载和console/pageerror；刷新保持有效会话。错误页或只有200/index.html不能记可用。
- [ ] 用正式API/UI准备本线笔记和事项，浏览器创建/编辑/完成后刷新或另一个context回读同ID/同版本；覆盖至少一个可控失败和输入保留/重试，不自动重复写入。
- [ ] 浏览联系人列表与真实详情、笔记关联、日程、收件箱、资料；做必要的联系人/资料编辑回读。活动发现/详情可读；报名仅在本线明确发布的未来活动和合成账号上验收，否则保留报名配置缺项。AI会话列表/历史/输入与未配置provider错误分别验，不能标真实生成通过。
- [ ] 逐入口填81项清单。动态路由选择实际合成记录；邀请码/导入/OCR等没有样本时列精确缺项，不跳过成通过。管理员/运营路线按该账号实际角色验证拒绝或可用，不提升权限来消除403。
- [ ] 运行Chromium与本地可用WebKit的最小核心链；窄屏采用390宽主体验和360/430边界。截图需目视检查，记录浏览器引擎与模拟视口，不声称实体设备。
- [ ] 实际脚本/新增行为代码先TDD（不为静态清单写镜像断言）；修改既有符号前impact。脚本须非零退出未通过的必需场景，同时保存已通过部分；不能catch所有错误后exit0。
- [ ] 定向验证、必要typecheck；detect_changes后显式路径commit，报告固定SHA、SC证据、部分失败及主协调集成说明。

## SC

| SC | 行为 | 必需证据 |
| --- | --- | --- |
| 01 | 本地生产入口登录、五主页面、刷新与退出行为正确 | 浏览器操作/关键状态、控制台与版本 |
| 02 | 真实笔记和事项创建/编辑/完成后刷新回读一致 | 正式UI/API的同记录ID与版本，不记录私密正文 |
| 03 | 人脉/资料/日程/收件箱及活动/AI路径有实际结果 | 页面证据与写入回读；provider/报名阻塞单列 |
| 04 | 81入口逐项记录状态，无遗漏或伪造通过 | 入口清单+可追溯证据/精确缺项，汇总总数一致 |
| 05 | Chromium/WebKit主要链路与手机宽度不出现新增阻断 | 实际引擎结果与目视截图；缺引擎如实报告 |

源码或数据缺口只阻塞依赖场景，其余继续。最终本地体验仍有必需功能未通过时报告partial/blocked并交出已验证代码，不能靠标“不适用”完成。是否新增修复Sprint由协调者按实际原因决定，不自动重开Generator循环。
