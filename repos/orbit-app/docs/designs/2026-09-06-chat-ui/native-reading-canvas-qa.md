# 原生 Reading Canvas 验收

2026-09-06。用户追加「加入。」后，将已选 Reading Canvas 迁入原生 `AiConversationScreen`。此前的 Sites 原型与其验收报告保持独立。

## 实现范围

- 单层紧凑导航、引用式用户消息、开放式 AI 正文和固定底部多行输入框。
- 快捷入口默认收起；沿用原有主题 token、真实历史、发送 API、活动/人脉/待办数据和确认流程。
- 当前服务的活动、人脉、待办、日历记录 URL 提供对应原生详情入口；行动保留带 entry 的当前服务网页地址。未知地址和模块首页不伪装成详情。
- 入口可见文字包含真实 ID，区分同类记录；明确说明可跳转范围。待办卡片编码服务器 ID，接受建议后使用响应的 canonical task.id，不拿 suggestionId 代替。
- 没有修改其他原生页面、后端、账号逻辑或部署产物。保留本工作区已有主题改动，未提交或推送。

## 已验证

- 新增渲染与解析回归均先观察失败，再观察修正后通过。覆盖紧凑导航、消息正文/换行、输入禁用态、深浅色渲染语义、可见入口身份、同源限制、普通路径/嵌套 URI 不误识别、中文紧邻 URL、task ID 编码与 accept 响应提取。
- 全量 `npm test`：752 通过，0 失败；`npm run typecheck`：通过；`git diff --check`：通过。
- iPhone 17 Pro / iOS 26.4 Simulator，402 × 874 pt：已观察真实历史使用新原生布局；闭合键盘时输入框 y=731/h=48，发送按钮 y=783/h=44，均在屏内。
- 已目视检查软件键盘弹起后的布局：输入与发送仍可见。此系统的键盘无独立 AX Keyboard 节点，因此几何脚本的 keyboard=null 不能当作自动键盘遮挡验证。
- 截图仅在忽略的 `.tmp/visual-qa/` 保存，不加入版本控制。早期 HMR 过渡截图不作为最终验收依据。

## 未完成的手动检查与限制

- 发现用户正在共享 Simulator 中输入并发送「接下来有什么工作」后，暂停界面操作。原生详情点击往返、菜单往返和原生深色截图尚未完成，不能报为通过。
- 没有替用户发送 AI 请求、接受待办建议或新增业务记录。截图中的既有余额错误属于历史消息，并未修改。
- 接受建议后的真实写入流程和外部链接失败提示尚无端到端点击测试；已有测试覆盖响应解析与渲染，不等同完整交互验收。
- 没有 Android 或实体设备验收，也没有重新生成原生安装包；当前 Expo/Metro 原生运行实例已加载更新。
