# 收件箱本地 QA

final result: passed (RNW visual / controlled-boundary checks)

Independent code review: APPROVE after two P2 accessibility fixes. Whole-package and Simulator acceptance: **not passed**. The user explicitly requires Simulator validation before final delivery; see [current native status](2026-09-12-native-status.md).

## Reference and comparison

- Selected source: `design_handoff_orbit_ink_signal/screenshots/3a-收件箱.png`; exact HTML lines 93–102. No alternative concept was used.
- Implementation: `/tmp/orbit-ink-signal-inbox-list.png`; same 390×844 CSS / 2× / 780×1688 pixels. Source and current list opened together. Five real-shaped message fixtures, two unread; fixture copy differs from source notification examples because the product's inbox is messages plus reminders/signals, not the source's unsupported activity/task/contact notification feed.
- Source device frame, system status bar and bottom chrome excluded; existing runtime owns them. The app has a compact 48pt header, 16pt inset, two actual tabs, working search and open rows. Source imagery contains no app-owned raster assets. Actual Ionicons supply back/search/unread/confirmation icons.

## Findings and resolutions

- Initial RED: `/tmp/orbit-ink-signal-inbox-red.log`, 3/10 pass, seven expected failures: oversized title, blue tabs/search ordering, 126pt rows and inconsistent unread gutter, old no-history parent, reminder loading/failure shown as empty, long text clipping at 1.6×/2×. Implemented source geometry, source home fallback, visible partial states/retry, and full wrapping without changing API/VM semantics.
- all1: 50/50 pass; typecheck1 pass. Visual comparison confirmed row density and alignment; source/date/copy differences are deliberate functional differences below.
- Signal failure review: header summary still claimed there were no email/calendar signals while reading failed. Additional test RED 0/1 (`signal-red.log`), then summary hidden for loading/error along with false empty content.
- all2: 51/51 pass, but typecheck2 **failed** because `exactOptionalPropertyTypes` rejects explicit undefined for DataCard.detail. Replaced it with an empty string, which follows the existing truthy display guard. No shared prop contract was changed. This failed check is not counted as passed.
- Bottom screenshots initially returned to the tabs after control checks. Capture now explicitly scrolls the content to its end and asserts the header stays fixed; all bottom states were re-opened. This was a capture correction, not a production scroll defect.
- all3: **51/51**, 0 fail/cancel/skip, 7.559316584 seconds, exit 0; typecheck3 exit 0. Independent review then found two P2s: the no-history Home button still announced Back, and the 2× input placeholder clipped. Neither was waived.
- Review RED: `/tmp/orbit-ink-signal-inbox-review-red.log`, 0/2, both expected failures. The header now announces its actual Home/Back destination. Large text uses the fully visible hint `搜索消息` while retaining `搜索姓名、主题或内容` as its accessible name. The test measures the actual input font/available width and exercises name filtering.
- Final local check: all4 **52/52**, 0 fail/cancel/skip, 8.578884875 seconds, exit 0; typecheck4 exit 0; diff check clean. Source, normal list and current 320pt/2× list reopened together. Independent focused retest 2/2 and review APPROVE. No actionable P0/P1/P2 remains within this RNW scope.

## Five fidelity surfaces

- Typography: existing system font; 16/22/800 centered navigation, 14/20 ink/800 selected tabs, 15/21 700 unread and 400 read headlines, 12/18 secondary/date. Dynamic content remains readable with full system scale. Existing full-message/reply typography retained. RNW fallback font strokes are not proof of native metrics.
- Layout: 48pt fixed header, centered maximum 540pt canvas and 16pt inset, 8pt before tabs, 22pt tab gap, 14pt row padding, 8pt indicator plus 12pt gap. Read and unread text both start at x36. Search is an additional 44pt-minimum control below tabs. Large text moves dates below content instead of shrinking text beside it. All controls ≥44pt high and reachable by scrolling.
- Colors: selected white/ink/signal-blue, `border2` row separators and existing muted text; read/unread weights and actual unread dots distinguish state. Existing dark palette retained and checked; the source provides no selected dark inbox mockup.
- Assets: real icon library, no painted custom assets or placeholder avatars. Source unread dots are represented by the library ellipse and are absent for read messages; the gutter remains reserved.
- Copy: exact existing name/subject/preview/date, real reminder counts, source-aligned home parent and own write-message action. No invented all-read, category filtering, recent-30-day claim or production fixture data. Reminder failure and retry use the existing sanitized error/helper style; local ignore still explicitly lasts only for this view.

Full-view 2× images were clear enough to inspect headline weights, dots, separators, timestamps and action labels directly; no extra crop was required. Source hierarchy maps to available product functions, not invented backend support.

## Verification scope

Command:

`APP_STYLE_SCREENSHOTS=1 node --import tsx --test tests/ink-signal-inbox.test.ts tests/relationship-inbox-interactions.test.ts tests/relationship-inbox-mail-content.test.ts tests/relationship-inbox-screen-source.test.ts tests/relationship-inbox-view-model.test.ts`

- `/tmp/orbit-ink-signal-inbox-all4.log`: 52/52, including 12 new rendered cases; `/tmp/orbit-ink-signal-inbox-typecheck4.log`: `npm run typecheck`, exit 0.
- Existing four inbox suites unchanged: actual search, dedicated encoded thread navigation, compose/cancel, failure draft preservation, pending input/submit lock, local previews, empty-body history, privacy and native HTTP wiring remain covered.
- New tests cover fixed header/tab/row geometry, real read/unread identity, no implicit writes, partial failure/loading/empty distinctions, retry refresh, signal confirmation's full encoded endpoint/body, actual local dismiss counts, and text/control bounds through list/alerts/composer/detail.
- Screenshots viewed: list, alerts, empty, failure, reminders-failure, reminders-loading, signals-failure; each 320pt/1.6×, 320pt/2× and 820pt/dark list/alerts/composer/detail top and bottom. Prefix `/tmp/orbit-ink-signal-inbox-`. Source/list pair re-opened together; dark and scaled variants checked for resilience, not pixel equivalence to nonexistent source variants.
- Actual screens, VM, theme, RNW handlers and production callback bodies execute. Device, router, auth-independent GET/POST boundaries are controlled; no live network/DB mutation or real delivery claim. All browser pageerror assertions empty.

P3/deliberate: full dates rather than fabricated relative times; an extra search row; real signal evidence/permissions/confirmation and reminder priorities/local-ignore controls; standard font/icon differences. Dynamic long headlines may end with a short wrapped line at 2×; no text is suppressed.

Not verified here: iOS bundle startup, native keyboard, VoiceOver, live dynamic type changes, device permissions/security storage, actual HTTP/cross-client writes, remaining pages and final full regression. No commit, deployment or dependency changes.
