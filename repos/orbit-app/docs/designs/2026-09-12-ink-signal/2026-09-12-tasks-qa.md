# 待办列表与详情：本地验收

final result: passed

当前视觉、88 项定向回归与最终独立代码复审已通过，本地待办批次关闭。本文不代表整包完成、原生验收或跨端业务联验。

## 对照依据

- 源：`design_handoff_orbit_ink_signal/screenshots/2a-待办.png`、`2a-待办详情.png`；原 HTML 第 298–331 行。
- 正常实现：`/tmp/orbit-ink-signal-tasks-list.png`、`/tmp/orbit-ink-signal-tasks-detail.png`。
- 两组源图与最新实现截图已在同一比较输入中打开。均为 390×844 CSS / 2×，780×1688 像素；排除源设备框、系统状态栏及原生系统指示器，不在 App 重画它们。
- 同为未完成标签、三条今天／两条之后／一条已完成，以及有截止时间的开放详情。测试时钟固定东京 2026-09-11 14:20；任务标题、类别、关系 ID 和备注使用完整测试记录，不复制源稿人名进生产。日期分类和原有排序由真实任务字段决定。
- 320×844、1.6×／2× 字号和 820×844 深色另行检查。对应 `/tmp/orbit-ink-signal-tasks-{narrow-large,narrow-double,wide-dark}-{list,detail-top,detail}.png`；宽屏图为 1640×1688 像素。详情 top / 滚动到内容两态均已查看。
- 原有详情交互夹具仍替换主题/容器，仅证明行为；新测试保留真实 Screen、VM、AppScreen、主题和 RNW，替换资源、client、路由与设备 I/O。没有将测试夹具渲染说成真实 HTTP 或 iOS 原生截图。

## 五项视觉检查

1. 字体：系统字体、列表 15/22、辅助 12/18、分组 15/22 + 22/28 计数；详情标题 24/32、正文 15/24。标题和备注保留原行内编辑，通过内容高度自适应，不裁切放大文本。字号放大后保持完整段落与可滚动阅读。
2. 间距／结构：16pt inset、黑色下划线 tabs、22pt 分组间隔、分隔列表、26pt 详情勾选框、开放元数据和底部 50/46pt 双按钮；固定动作区与宽屏正文同一列。没有新增卡片装饰或伪造顶部导航。
3. 颜色：浅色白底、墨黑 `#0B1220`、蓝 `#0A5CFF`、边线 `#E6E8EE` / `#EEF0F4`。完成勾选蓝底、标题划线；必要辅助文字沿用可读 text3，非源中更浅的装饰色。深色保留既有主题，不声称对齐不存在的深色稿。
4. 图像／图标：标准 add / chevron / check / edit 使用既有 Ionicons；复选框是语义控件。任务契约没有真实联系人姓名／头像，详情只显示实际关联 ID 对应的“查看关联人脉／活动”跳转，不生成虚构人物图像。
5. 文案／内容：真实状态与计数；分类／提醒和变更历史仍可访问；来源和创建时间来自 task 字段，无字段不展示。标题、备注不改写。参考中人名和时间不是生产默认值。

全图中的文字、控制和细线在 2× 输入中可直接读清，本批不需要另外放大裁剪；多尺寸顶部和正文截图补充了重点区域核对。

## 比较历史与修正

- [P2，已修] 原列表单行截断大字号标题。RED 新测试列出被截标题；去除截断，保留实际标题和分类／日期。最新 narrow-large / narrow-double 列表完整换行。
- [P2，已修] 双倍字号下固定 44pt 宽“编辑”拆为两行，72pt 元数据 label 拆开“创建于”。补真实几何断言复现；大字号顶部使用有完整读屏名称的编辑图标，元数据标签独占一行。最新 top / detail 图已复核。
- [P2，已修] 为贴近参考 34pt 标题起点而使用负 margin，导致详情触控覆盖复选框 10pt。几何回归先失败；去除负 margin，保留独立 44pt 点击宽度。正文相对参考右移 10pt 是明确的触控适配。
- [P2，已修] 820pt 固定底部动作比正文列每侧多 16pt；断言实测 x=140 与 156。内层 maxWidth 扣除正文两侧 inset 后对齐，最新 wide-dark-detail-top 再次查看。
- [行为，已修] 同一轮事件双击会发出两条完成请求，抛错会留住禁用状态。独立 RED 分别得到 2≠1 与缺失错误反馈；同步锁／finally 恢复，失败不刷新、不将记录画成完成。
- RNW 已弃用的选中映射需同时设置 `aria-selected` / `aria-checked`；保留原生 accessibilityState。新测试验证实际浏览器选中态。

## 有依据的产品差异

- 列表 `＋` 前往已有 `/today` 的真实添加待办表单；不新增无后端依据的创建页。
- `GET /api/tasks` 无 status 参数已只读核对服务端 list 支持全状态且无分页限制；从同一响应生成标签计数。逾期、未安排事项分别保留，不强塞“今天”或丢弃；取消记录不算作完成。
- 完成预览使用每条记录自身状态执行恢复，不借当前 open 标签判断动作。完成深链仍保留。
- 详情新增来源／创建时间／真实关联跳转，保留原有分类、提醒、历史、删除和版本草稿。比参考多出的分类／提醒两行属于功能保留；无推测人名或机构。
- 正常尺寸参考 22pt / 26pt 复选框分别有至少 44pt 独立热区；大字号使用自然换行，底部按钮可增长。旧失焦保存协议不变，编辑按钮仅聚焦，不产生写入。

## 验证记录

- 新 `tests/ink-signal-tasks.test.ts`：13 项；首次新布局测试 RED 9/9，后续字号／热区／点击锁／宽屏对齐均单独 RED→GREEN。
- `node --import tsx --test tests/ink-signal-tasks.test.ts tests/task-detail-interactions.test.ts tests/today-tasks-view-model.test.ts tests/today-tasks-screen-source.test.ts tests/app-wide-workspaces.test.ts`：88/88，0 失败／跳过，22.403 秒；`/tmp/orbit-ink-signal-tasks-all3.log`。
- `npm run typecheck`：exit 0，`/tmp/orbit-ink-signal-tasks-typecheck4.log`；`git diff --check` 已通过。
- 本批逐符号 GitNexus：屏幕、行状态处理、tab、样式及投影函数 LOW；两个视图类型 MEDIUM，6 个文件级直接依赖，仅新增可选字段。未修改 HTTP client、共享契约、Web、数据库或公共 AppScreen。
- 独立复审：最终 APPROVE，无剩余 Critical / Important / Minor；已核对全状态读取、恢复语义、旧详情写协议、大字号及宽屏对齐。

## 未验范围

没有跑本批全量 npm test（按当前节奏在剩余页面后统一执行）；没有真实账号／HTTP 写入、Web↔App 双向回读、原生键盘与 Dynamic Type 热切换、VoiceOver、设备通知或发布验证。已有原生依赖缺陷不因此关闭。

## 收尾清单

- [x] 两张源图和最新实现成组对照。
- [x] 字体、间距、颜色、图标、真实内容逐项检查。
- [x] 常规、窄屏大字号与宽屏深色，无未解决 P0/P1/P2 视觉项。
- [x] 定向测试、类型和差异检查。
- [x] 独立复审通过后关闭本批、更新主台账，继续联系跟进。
