# 登录批次 QA

final result: passed

Source: `design_handoff_orbit_ink_signal/screenshots/2a-登录.png`, 780×1688 pixels, 390×844 CSS points at 2×. Implementation: `/tmp/orbit-ink-signal-auth-login.png`, same viewport/density. Source and rendered screenshot opened together; device frame and system status excluded, app-owned regions compared.

## Comparison history

First comparison was blocked: hierarchy and vertical rhythm aligned, with the following corrections required:

- P2: auxiliary copy uses darker `text2` instead of source muted tone; use readable `muted` for description/footer.
- P2: at 320pt/2×, signup title leaves a single 号 and Google action leaves a single 录; shorten UI title/large-text action without changing accessible action labels or request semantics.
- Browser normalization: focus capture adds a boxed native-browser outline around a natively underlined field. Keep a visible focused underline, avoid a second box. Source HTML's declared 1.5px border also computes to 1px in this Chromium environment (measured independently).

Second comparison: description/footer use `muted`, signup title is the complete “创建账号”, and large-text Google label is “Google 登录” with unchanged full accessible name. The source Google placeholder ring is replaced by the existing real Ionicons Google glyph. OAuth button minimum height is explicitly 50pt. 70/70 tests and typecheck passed. A remaining browser-native `outline-style:auto` ring still painted despite a computed zero width; a new failing test reproduced that exact condition.

Third comparison: `outlineStyle: solid` plus zero width removes the extra browser box while focused fields retain a dark underline. The latest login and exact source were opened in the same image input again. No actionable P0/P1/P2 remains. Main agent also inspected all three modes at 320pt/1.6×, 320pt/2× and 820pt dark, including scrolled bottoms, plus login/signup failure and recovery acceptance. Top captures explicitly reset the scroll position; bottom captures demonstrate reachability rather than pretending all large-text content fits at once.

## Required fidelity surfaces

- Fonts/typography: source system font intent retained, 26/31/900 editable Orbit. wordmark, 34/40/900 heading, 14/20 description, 12/18/700 labels, 16/22 inputs, 15pt actions. Native system-font metrics and RNW platform fallback are not identical pixel proofs; no new font was installed or unselected design adopted. Source's low-contrast decorative labels are rendered with the existing readable small-copy token. Password length helper is retained. No text scaling is disabled.
- Spacing/layout: fixed 48pt close header, 24pt body inset, 40pt hero start, 28pt heading gap, 36pt form gap, 22pt field gap, 46pt underline field, 50pt actions, 12pt radius, centered bottom registration. Recovery has a real 44pt target between password and submit; the 14pt subsequent gap retains source CTA placement. Scroll content grows and is bounded to the existing 540pt maximum on wide screens.
- Colors/tokens: white, ink `#0B1220`, signal blue `#0A5CFF`, source muted `#6B7280`, border `#E6E8EE`; existing error/notice semantic colors retained. Dark mode uses the existing dark palette and is a resilience check, not alignment to an absent selected dark reference.
- Image quality/assets: no raster imagery is required by this frame. Orbit. is editable source wordmark text; close/eye/Google use actual library glyphs, not hand-drawn logo shapes. The old ring illustration is removed. Device bezel/status/home chrome remain system-owned and are excluded from comparison.
- Copy/content: source login heading/description and compact bottom registration; real existing helper/error/created-notice/provider labels remain. Signup/recovery retain their own purpose and callbacks. No invented credentials or prefilled source person enters production; filled credentials exist only in the test fixture. `no-ai-tone` review favored the short signup title rather than redundant brand repetition.

Full-view comparison covered all visible content; individual image inputs were readable at 780×1688, so separate region crops were not necessary. Focused review covered underline/focus, labels, password toggle, button icon/text alignment and footer. Ordinary pressed/disabled feedback, password visibility, ready/provider states, failure retention and all primary actions are exercised by rendered tests.

## Verification

`APP_STYLE_SCREENSHOTS=1 node --import tsx --test tests/ink-signal-auth.test.ts tests/account-auth-view-model.test.ts tests/account-auth-screen-source.test.ts tests/password-reset-interactions.test.ts tests/app-wide-account.test.ts`

- Latest `/tmp/orbit-ink-signal-auth-all3.log`: **70/70**, 0 fail/cancel/skip, exit 0, 15.575329 seconds; includes 12 new rendered cases.
- `/tmp/orbit-ink-signal-auth-typecheck3.log`: `npm run typecheck`, exit 0. `git diff --check` clean.
- TDD: `/tmp/orbit-ink-signal-auth-red1.log` caught open-field alignment, missing close behavior and unsafe scroll reachability; polish/word/focus RED logs caught source color, orphan action words and browser auto ring. Each was followed by implementation and a passing run. The 1.5px→1px rounding was independently reproduced using the source's CSS in the same Chromium environment, not silently accepted as a product failure.
- New tests keep real screen/view-model/theme/RNW and icon glyphs; only native, router and external auth/client boundaries are controlled. Existing recovery tests retain real API client/envelope parsing, malformed responses, same-turn lock and late-response isolation. Password/Google provider protocol, native storage and auth callbacks are unchanged.
- Independent read-only review: APPROVE, no Critical/Important/Minor. A tentative CTA-width concern was withdrawn after reopening the original-size screenshot; actual x/width equals the field area. Added direct bounding-box regression, `/tmp/orbit-ink-signal-auth-review-width.log`: 12/12, exit 0 (6.605788 seconds). No production change was needed. Whole-package completion is not claimed.

Screenshots: `/tmp/orbit-ink-signal-auth-login.png`, `login-error.png`, `signup-error.png`, `forgot.png` with that same prefix; and `narrow-large`, `narrow-double`, `wide-dark` × `login`, `signup`, `forgot`, with each `-bottom` variant. Native/browser focus behavior and actual native keyboard/auth round trips remain outside this RNW batch.

## Implementation checklist

- [x] Source and latest rendering compared at identical density/state.
- [x] Source layout, reachable scaled controls and existing auth/recovery actions verified.
- [x] All P0/P1/P2 findings repaired and rechecked; no further P3 polish loop.
- [x] Independent review completed.

No server/API, generated contract, real account/database, commit or deployment changes. Full test suite and current native acceptance remain part of the unfinished overall redesign.
