# 活动主页面对照 · 2026-09-12

范围：源 `design_handoff_orbit_ink_signal/screenshots/1c-活动.png` 与实际 `/events`。HEAD `8b38b4eb8618505ca4f59f20dc323159e1ad784b` 加未提交 App 工作；不含活动详情验收，不提交／推送／部署。

## 对照证据

源与最终 `/tmp/orbit-ink-signal-events-390-round5.png` 在同一输入中实际查看：均为 390×844 CSS pt、2×、780×1688 PNG，浅色、全部标签、四条相同标题／日期。只比较 App 内容；源手机外框与虚构系统时钟不复制。字体与小图标在此密度已可逐项阅读，无需另做裁剪。真实路由、RNW 组件、hooks、HTTP client、view-model 保持运行，只替换外部 HTTP／认证／导航／原生边界；加载真实 Ionicons 字体，检查 pageerror 为零。

- 字体：系统字族，30/900 页标题、15/800 行标题、14pt 标签／搜索、12pt 日期地点状态；长标题完整换行，不禁用字号缩放。
- 布局：16pt inset、92×70 图片／8pt 圆角、14pt 行间距和分隔线。筛选与搜索保留至少 44pt 热区，因此列表较源稿下移约 16pt；四条仍完整可读，属于可达性所需差异。
- 颜色：白底、墨黑标题、信号蓝状态／计数、浅灰搜索和列表分隔；深色保留原有配色，不引用淘汰设计。
- 图片：保留实际活动图片解析路径，测试读取已有 meeting.jpg；不以源稿渐变占位替换真实图片，不把整屏图贴入页面。
- 内容：名称／日期／计数来自数据。源“已报名”没有对应公开集合接口，不绘制假标签；保留推荐／全部及真实时间、地点、主题组合筛选。地点默认全部，不强制东京。真实主办方信息保留，不改成源样例副标题。

## 迭代与功能验证

1. round2 发现标签文字从 24/90pt 开始、下划线覆盖整个热区；实际失败测试复现。round3 调整为 16/66pt、文字等宽下划线，保留 44pt 热区，后续 round5 复核通过。
2. 320pt／1.6 大字号图发现 IORBIT 断词。测试在 1.6／2 倍失败；公共底栏中央项改为按内容宽度布局。RNW 中 `maxWidth: undefined` 未清除旧 60px 上限，实际测量复现后改为明确 `100%` 上限。1／1.6／2 倍品牌完整、五项都在边界内且至少 44pt。最终 `/tmp/orbit-ink-signal-events-320-initial.png` 已实际查看。
3. 820pt、390pt 深色初始图以及推荐 pending／empty／accepted 图已实际查看。路径前缀 `/tmp/orbit-ink-signal-events-`，包括 `820-initial.png`、`390-dark-initial.png`、`recommendations-{pending,empty,accepted}.png`；320pt 长文本末行可滚动到并点击，键盘隐藏底栏的公共测试通过。

公开 API 使用 imported 同时表示未来与现场活动；本页按校验后的 startsAt／endsAt 推导时间筛选，保留原显示状态。18 条实际分页覆盖 8→16→18→8，以及每项筛选重置；两个地点验证排除效果。残缺字段、垃圾日期、逆序时间、非法状态、重复 ID 不伪装为空。

推荐独立展示加载／准备中／空／失败与重试；只有显式点击才发接受请求，不额外报名。回执必须 2xx、accepted、同一 ID、有效评分／消费字段、五项安全布尔值明确 false。全部逐字段缺失／true 反例已测。完整账号／Cookie／服务器／焦点作用域、同步锁、AbortSignal 和旧回调保护覆盖 GET 与 POST；迟到 401 不退出新会话。

## 验证结果

最终定向 131/131，67.354 秒；全量 1659/1659，122.932 秒，均 exit 0、无失败／跳过／取消。typecheck8 与 diff check 通过，独立只读复审 APPROVE，无 Critical／Important／Minor。日志：`/tmp/orbit-ink-signal-events-target-final.log`、`/tmp/orbit-ink-signal-events-full2.log`、`/tmp/orbit-ink-signal-events-typecheck8.log`。

P3：行末箭头比源稿略深，品牌自适应使普通字号底栏横向分配略有变化；不继续循环微调。无未解决 P0/P1/P2。

未验：真实 iOS／VoiceOver、系统字号热切换、真实账号跨端读写与发布。既有原生依赖问题不因 RNW 通过而关闭。本批不代表全部 26 张设计完成。

final result: passed
