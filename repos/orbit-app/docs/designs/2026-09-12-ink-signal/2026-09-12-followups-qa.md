# 联系跟进本地设计验收

final result: passed

源图：`design_handoff_orbit_ink_signal/screenshots/2a-联系跟进.png`。
实现：`/tmp/orbit-ink-signal-followups-open.png`；源与实现均 780×1688，390×844 CSS、2×。源含示意手机边框和系统栏；实现仅保留 48pt 安全区，不画系统内容。使用相同四名测试联系人、三条今天和一条之后的待办；实际另有一条已完成及一条其他分类，通过产品入口保留。

## 第一轮对照

同一次查看中打开源图与实际截图，逐项比较正常首屏：14pt 下划线 tabs、15pt 姓名与分组、22pt 数量、40pt 头像、12pt 身份信息、嵌套 14pt 任务和 20pt 复选框。控件真实热区至少 44pt，分开进入联系人、任务详情与完成操作。

- [P2] 初版头像采用告警语义色，偏离源稿及现有人脉列表的柔和身份配色；一次未定义 token 导致两个头像不可见。后者由渲染测试和类型检查捕获并已修复，前者应复用现有人脉列表的源色渐变处理。
- [P2] 真实头像加载失败时没有身份回退。新测试已复现，应回到实际联系人首字，不生成虚构照片。
- 允许差异：原生安全区／系统字体抗锯齿、≥44pt 非重叠热区使任务文字略右移、真实 ID 决定头像 tone、已完成数量、其他待办入口及下方现有跟进工具。未来日期保留现有星期信息；不隐藏产品数据去模拟源图。

## 修正与再次对照

初版头像不可见由错误 token 引起；替换为已有 token 后，再复用人脉列表现有的源色渐变。真实照片优先，加载失败回到该人姓名首字。两项先出现预期失败，随后通过。完成态另发现旧截止时间覆盖完成日期，补充限定到具体任务行的 RED 后修正为真实完成记录日期。

最新源图与 `open` 截图再次同次打开比较，40pt 头像、列表层级与色彩差异已修正；`completed` 为 9月10日18:00，不再写成“今天”。无可操作 P0/P1/P2。

五项核对：

- 字体：系统字体，15/22 姓名、14/20 任务、12/18 元数据，分组 15/22 与数量 22/28；无单行截断。正常和放大后主次关系清楚。
- 间距：16pt 页边、52pt 嵌套起点、20pt 组间距；40pt 头像、20pt checkbox。点击区 ≥44pt 且与详情区域不重叠。完整 2× 输入中小字／图标可直接辨认，无需额外裁片。
- 色彩：白、墨黑、信号蓝与浅分隔线；身份色复用现有联系人列表，不使用告警红色装饰。深色保留产品主题，不声称有未提供的深色参考稿。
- 图像／图标：既有首字身份回退和实际照片、Ionicons；不将源示例人物或手机状态栏写入生产。
- 内容／交互：已保存任务与候选明确分离；候选、草稿与已发送不同。其他分类仍可进入全待办，逾期／未安排／已完成不丢失。未知联系人只显示真实关联入口，元数据失败不阻挡任务处理。

## 验证记录

主 Agent 已逐张打开 `/tmp/orbit-ink-signal-followups-{open,completed,error,narrow-large,narrow-double,wide-dark}.png`，以及后三种尺寸各自的 `-tools` 和 `-drafting` 下方截图。320px 的 1.6×/2× 字号换行和完整滚动内容、820px 深色内容列均可用。模拟请求边界下无浏览器 pageerror；这不代替原生或实际 HTTP 验证。

- 最终合并定向：73/73，0 失败、0 跳过，22.414 秒，exit 0。含新增真实渲染／交互测试 13 项、旧 followups VM／接线及 app-wide-workspaces；日志 `/tmp/orbit-ink-signal-followups-all3.log`。
- `npm run typecheck`：exit 0，`/tmp/orbit-ink-signal-followups-typecheck4.log`；`git diff --check` clean。
- 独立只读复审：APPROVE，Critical / Important / Minor 均无。已保存任务、候选分离、真实 ID、Tokyo 日期、请求负载与防重复均通过审查。
- 请求边界：完成／恢复只 PATCH 指定 task；生成候选、提醒、轻量起草、保存草稿及标记待确认仍使用原 API 和 builders，无自动外发。
- GitNexus：已有 Screen/Workspace 为 LOW（分别 1 个直接调用）；新增组件尚未索引，为 UNKNOWN，已人工核对只由本页消费。未改旧页内共享样式或共享 AI mapper。

实施清单全部完成。P3 仅原生／浏览器字体笔画、标准库图标细节，以及原有可读的未来日期星期字段。

未验：真实原生 VoiceOver、键盘、字号热切换、鉴权 HTTP 与 Web↔App 持久化、剩余页面及最终全量回归。没有提交、部署或真实业务写入；仅关闭本批本地验收。
