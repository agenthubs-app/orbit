# 2026-09-12 名片导入：三条复核流程

final result: passed

范围仅为 RNW 视觉、交互和受控边界回归；不是原生 OCR、真实联系人写入或整包 Simulator 验收。原生结论见[当前记录](2026-09-12-native-status.md)。

## 比较目标与证据

- 精确源图：`design_handoff_orbit_ink_signal/screenshots/3a-名片导入.png`，HTML 画板第 77–90 行。
- 源图及正常实现均为 780×1688 像素；CSS viewport 390×844、deviceScaleFactor 2。源图设备轮廓和状态栏不复制为 App 内容；保留现有安全区与二级页返回。
- 最新正常实现：`/tmp/orbit-ink-signal-card-review-{legacy,ingest,single}-normal.png`。三张与源图在同一比较输入中打开，逐一检查图片区、标题、字段及动作层级。状态为已选图片、已有待复核字段、尚未创建联系人；输入保持源图相同姓名、公司、职位，电话保留完整可编辑值。
- 已目视检查同前缀 `{legacy,ingest}-{loading,forbidden,completed,failed,duplicate,image-loading,image-unavailable}`；单张图片失败、保存失败、第二张权限／读取／识别失败；不把不可读取归作空态。
- 响应式证据：三条流程的 `narrow-large`（320pt、1.6×）、`narrow-double`（320pt、2×）、`wide-dark`（820pt、深色）顶部、字段中部、复核动作、底部；额外检查无历史深链的大字号返回标题。
- 重点区域：正常图 2× 的 72pt 字段列、分隔线、预览边界清晰可读；大字号字段、电话、风险／确认动作使用独立滚动位置截图检查，不仅检查首屏。
- 实际 screen、VM、theme、图片状态组件经 Chromium/RNW 渲染；只控制原生图片选择／文件／Alert、鉴权、路由和 HTTP 边界。各页面 `pageerror` 为空。

## 五项视觉检查

| 表面 | 结论 |
| --- | --- |
| 字体与层级 | 系统字体，识别结果 15pt/800，字段值 14pt/20pt、600，标签列 72pt。大字号纵排并增长多行高度，不关闭缩放。单张保留真实置信度／复核状态，辅助文字 11pt/16pt。 |
| 间距与布局 | 48pt 二级页头、16pt inset、104pt 预览、12pt 圆角、开放分隔。主操作至少 50pt、次操作至少 46pt；各 viewport 控件至少 44pt、无横向越界，长公司名完整换行。 |
| 颜色与 tokens | 白底、墨黑主动作、信号蓝图标／导航、浅灰图片及提醒表面；错误、禁用保留实际语义。深色保留现有能力，不声称匹配未提供的深色稿。 |
| 图片与图标 | 生产只显示当前实际图片，contain 保留完整名片，缺失／读取中／失败明确提示。截图白色条是验证图片管线的有效 1px PNG 测试输入，不是生产占位画或源图插画复刻；真实照片清晰度与 OCR 尚未原生验收。使用已安装 Ionicons。 |
| 文案与内容 | 保留真实字段、来源、风险、候选保存与写入区别、批次状态和重复提示；无虚构 4/5、工作区、日期或合并能力。第二张采集失败属于第二张来源操作，不污染第一张复核。 |

## 比较历史

1. 旧标题、字段边框和批次优先顺序先由真实渲染测试报错，再改为图片／复核优先、开放字段和选定标题；管理与采集入口保留在后方。
2. [P2，已修复] 320pt 大字号公司字段收到正确高度仍被 `flex: 0` 的 basis 压回单行。完整文本包容断言先失败；改为明确 grow/shrink/basis，按内容高度增长。重新检查三条流程的大字号字段，长公司名完整显示。
3. [P2，已修复] 无历史深链长返回标签使标题呈 3+1 孤字。换行测试先失败，改为大字号 2+2；返回仍去真实导入中心。
4. [P2，已修复] 初始采集 cover 裁切风险经失败测试确认后改为 contain。已识别图片绑定成功扫描 draft，选择新照片不提前替换旧结果。
5. [P2，已修复] 复核保存失败原在后方来源表单显示，增加失败草稿／alert 回归后移至复核字段后。
6. [P2，已修复] 独立审查发现共用错误状态会把第二张权限、读取或识别失败显示在第一张复核中。三条交互回归及图片失败朗读断言先失败；分离 `reviewError` 和来源 `error`，保留原图片／编辑字段。新截图确认来源错误位于选图与提交之间；图片失败提示使用 polite 朗读。
7. 最终同尺寸源图与三条正常流程再次成组比较，全部大字号、深色、重复及错误状态目视检查。当前 RNW 无待修 P0/P1/P2；独立复审 APPROVE，无未解决发现。

## 功能差异与后续细节

- 批量七字段包含认识背景、备注；单张有真实字段置信度和候选保存步骤。密度与总高度不同，不为首屏贴图删除信息。
- 动作位于完整表单后并随内容滚动，不复制固定底部动作；保留现有 AppScreen 滚动／键盘边界，额外字段、风险和批次操作可达。
- 批量重复候选仍需原生二次确认“仍然收录”，不是合并；单张原有合并建议保留，不新增无契约操作。
- 重拍位置保留真实返回，重选／采集在现有入口；示意扫描图、4/5、工作区和固定日期不当作真实数据。
- P3：平台抗锯齿、返回图标及约 4pt 图片起始位置差异保留，不扩张公共布局修改。

## 验证与边界

- 审查修复前的 11 个定向套件：354/354，91.418182125 秒，exit 0；`/tmp/orbit-ink-signal-card-review-all1.log`。不冒充修复后的重跑。
- 审查修复后：新名片测试 28 项；合并 acquisition screen／VM／风险集合 80/80，23.024569208 秒，exit 0；`/tmp/orbit-ink-signal-card-review-ownership-green.log`。独立复审另复跑 28/28。
- 随后当前整包 `npm test`：2188/2188，0 失败／取消／跳过，203.935630541 秒，exit 0；`/tmp/orbit-ink-signal-final-regression2.log`。包含上述测试，不相加计数。
- 当前 `npm run typecheck` exit 0：`/tmp/orbit-ink-signal-final-typecheck2.log`；定向 diff check 通过。
- 覆盖精确编辑请求、取消／拒绝权限、图片失败／旧回调、选择新照片不串结果、保存失败保留草稿、最终同意重置、无提前写入、重复确认和批次锁定。
- 未执行真实业务写入、相机／OCR 服务或跨端回读。Simulator 字号、键盘及非空流程按独立记录保留。

## 实施清单

- [x] 三条流程应用选定视觉，保留业务状态机。
- [x] RED/GREEN、响应式截图、源图对照及独立复审。
- [x] 当前全量回归及类型检查。
- [ ] 整包 Simulator 验收全部通过；本地报告不能替代。
