# IORBIT 视觉与功能验收

首页、会话与失败态已完成本地视觉验收、定向回归与最终独立复审。全量回归留到剩余页面实施后的统一验收，不将其记为已通过；也不代表整个 26 图设计包或原生验收已完成。

## 首页对照证据

- 源图：`design_handoff_orbit_ink_signal/screenshots/2a-IORBIT首页.png`。
- 最新实现：`/tmp/orbit-ink-signal-ai-home-390.png`。两图均为 780×1688，即 390×844 CSS、2×；主代理已将两图放在同一次图像输入内重新对照。源图手机外框、状态栏、底部指示条不作为 App 内容复制，实际实现保留 48/24pt 安全区。
- 状态：有两类真实接口形状的三条历史记录、输入框有草稿、无键盘弹出。数据仅为实际路由／HTTP 边界测试夹具，不进入产品。业务提示问题依现有 Today 状态变化；不硬编码为图中三句。
- 聚焦检查：同一组 2× 图内完整查看品牌标志／顶栏、问题行／历史行和输入框，文字及线条均可读，无需另裁图才能判断。品牌使用源 HTML 中原始标志的透明栅格导出（54×54，显示 18pt），不是近似手绘图形。
- 韧性截图：`/tmp/orbit-ink-signal-ai-home-320.png`（320×844，fontScale 1.6）、`/tmp/orbit-ink-signal-ai-home-820.png`、`/tmp/orbit-ink-signal-ai-home-dark.png`。均已打开最新版本检查；这些是滚动到“下一步”与旧对话后的长草稿状态，不与源图假装同状态比较。

## 五项视觉检查

| 表面 | 结果与边界 |
| --- | --- |
| 字体 | 系统字族、30/36pt 主标题、15pt 问题／历史标题、12pt 辅助文字，保留系统缩放。RNW 系统回退的中文笔画与源图 PingFang 回退有平台差异，不声称逐字栅格相同；原生字体仍待检查。 |
| 间距 | 16pt inset、开放分隔列表、三行问题和三条历史层级已恢复；历史标题触控区保持 44pt。输入框正常 44pt 内容高度，长草稿随内容增高至 120pt 后内部滚动；按钮保持 44pt。 |
| 颜色 | 白底、墨黑主文、信号蓝链接／发送动作。辅助文字使用已验证的较深语义 token 保证可读性；深色沿用产品既有配色，不套用淘汰稿。 |
| 图片 | 原始品牌图标导出清晰；首页没有需要捏造的业务照片。标准图标使用既有 Ionicons，历史时钟与源图历史箭头细节不同，属 P3。 |
| 文案 | 保留三项可编辑提示、最近记录和自己的下一步；原始多语言业务内容不因 live/provider 等词被过滤。不使用“不会自动执行写操作”承诺，因为现有明确创建待办请求可以写入。 |

## 迭代记录

1. P2：最初品牌使用另一版图标、最近会话距问题列表偏大、单行输入框默认两行。修复为原始源标志、紧凑区块间距和单行起始高度。对应三项测试先失败，修复后通过；最新 390 图已重新成组对照。
2. P2：320px 大字号的长输入只显示过窄的一行。增加内容高度适配、大字号最小高度向上取整与清空回缩。新用例先失败，当前普通／大字号均通过；最新 320 图中末尾完整多行和发送／更多操作可达，内部滚动裁切不是页面溢出。
3. P2：无搜索匹配误报没有历史。改为“还没有匹配的对话”，保留真实空历史提示，实际搜索回归已通过。
4. 独立评审发现的原文过滤、单源失败掩盖与畸形空结果已修复，均有先失败再通过的实际路由用例。当前首页无待处理 P0/P1/P2。

## 功能证据与限制

- `tests/ink-signal-ai-home.test.ts` 使用真实私有路由、hooks、HTTP client 和 RNW 控件；只替换认证／设备、HTTP transport、快照存储外部边界。包含完整身份／Cookie／服务器／焦点隔离、迟到 401、双击、失败恢复、删除确认及持久化回执检查。
- 新测试与兼容测试合计 136/136，0 失败／跳过／取消，30.296 秒，exit 0；日志 `/tmp/orbit-ink-signal-ai-home-all2.log`。类型检查 `/tmp/orbit-ink-signal-ai-typecheck5.log` exit 0；diff check 通过。独立最终复审 APPROVE、无剩余发现。之前的单独 green1/green2 文件存在输入框失败，不作为最终通过证据。
- 所有边界测试浏览器 pageerror 均断言为空。菜单、历史、搜索、提示填入、删除确认／取消、重试、返回／旧入口已通过真实控件交互验证。
- 设计里的 @ 没有现有消息协议支持，不放置假可用按钮；更多菜单保留名片扫描、新对话和常用入口。自己的下一步与真正旧消息保留在最近会话下方。
- 本端版本仍为 HEAD `8b38b4eb8618505ca4f59f20dc323159e1ad784b` 加 App 未提交改动。未改 Web／生成契约副本／根 bridge 台账，无真实 API 写入、提交、推送或部署。
- 原生键盘／VoiceOver、实机字号切换、同账号 Web↔App 双向持久化未验证；不能由 RNW 或类型检查替代。

## 会话与失败态对照证据

- 源图：`design_handoff_orbit_ink_signal/screenshots/1c-IORBIT会话.png`、`3a-失败态-IORBIT.png`。
- 实现：`/tmp/orbit-ink-signal-ai-conversation-reference-390.png`（真实形状的活动记录与既有活动照片）、`/tmp/orbit-ink-signal-ai-conversation-390.png`（活动集合为空）、`/tmp/orbit-ink-signal-ai-failure-390.png`。源图与实现均为 780×1688、390×844 CSS、2×，保留实际安全区，不复制手机外框和系统状态栏。
- 主代理已将源会话图和最新活动引用实现、源失败图和实现分别放入同一次图像输入对照。完整 2× 图中可以直接辨认顶栏标志、消息字号、编号与段落、引用图片和输入框，已作逐区域检查，无需裁图才能判断。
- 对照会话使用与源图一致的提问及三段编号回答；源稿的虚构依据数量、笔记及纯色活动占位不进入产品。失败状态使用实际 HTTP 503 的原因和代码，因此不复制 `NET-408`、固定时间或“重试不会产生重复待办”的承诺。根 POST 可以执行明确的创建待办请求。
- 320×844 / 1.6 字号、820×844 和深色最新截图均已打开检查：`/tmp/orbit-ink-signal-ai-conversation-320.png`、`-820.png`、`-dark.png`。这些是长草稿、滚动到对话末尾状态，不冒充源稿同状态。按钮至少 44pt，输入增长到 120pt 后内部滚动，正文可滚动而固定控件仍可达。

### 五项会话视觉检查

| 表面 | 结果与边界 |
| --- | --- |
| 字体 | 52pt 顶栏、15pt IORBIT／11pt 会话名；用户正文 16/24pt、700，回答 15/24pt、400；编号 22pt、说明 14/23pt。使用系统字族，RNW 中文回退的笔画粗细与源图不同，原生字体需单独验收。 |
| 间距 | 16pt inset、无消息气泡、用户后分隔线、蓝色编号／对齐说明和固定双行输入区；活动引用改为无卡片外框、64×52pt 图、横向文本。自己的状态／报名人数与全部活动入口保留，因此引用区域比只有两行信息的稿件更高，仍可滚动查看全部。 |
| 颜色 | 白／墨黑／信号蓝；红色真实错误；次级文字使用产品既有可读性 token。深色沿用既有主题。无装饰渐变或拟物背景。 |
| 图片 | 同一个源品牌栅格标志；标准图标为 Ionicons。活动图使用现有真实封面，不生成假封面、不把设计的纯色占位当业务图片。 |
| 文案 | 多语言原文、Markdown、真实引用／执行依据保留。复制通过原生文本选择；未新增没有协议支持的 @／反馈按钮。快捷入口、各业务内嵌面板与任务建议仍可用。 |

### 会话迭代记录

1. P1/P2：旧气泡、偏高顶栏与输入框、编号说明未对齐、引用面板插在回答之前。改为紧凑顶栏与源品牌、平面消息、编号分组、单行起始且可增长输入框，面板跟在最后一条回答后；布局用例及最新成组截图验证。
2. P1：初始请求失败后仍有“正在处理”与空活动面板，将失败恢复推下屏。失败后只显示原问题、IORBIT 错误卡和真实错误，重试／编辑均可达；先失败再通过的位置与文案用例、最新失败图确认。
3. P2：活动引用仍是旧的大卡片、64×64 图，区块头部使卡片被输入区截住。改为 64×52 真实图片的无外框横向行，并把自己的“相关活动／全部”入口移到行后。实际图片尺寸、记录路由和卡片完整位于首屏的断言先失败再通过；最新引用图已和源图重新比较。
4. 保留的 P3：系统中文回退笔画、标准库返回／更多图标的线条与源稿略有不同；44pt 触控区使输入区比稿件较高。无设计提供的原生键盘或深色目标，因此不能作原生像素一致承诺。

### 会话功能证据与限制

- 真实私有路由／hooks／HTTP client 测试验证初次提问、普通续聊、Web session 续聊、同步双击锁、新草稿保护、迟到响应／401、账号与服务器隔离、失败重试／编辑、任务建议接受／忽略以及保存失败只重试保存。
- route-owned 暂存仅在同账号／服务器／导航意图内保留；焦点和 Cookie 重建不会丢回复、草稿、未确认建议或待保存快照。URL 消费标记阻止整个路由重新挂载时自动重发，已中断请求显示明确恢复入口。
- 刷新调用原资源的刷新接口，不取消写入；新 GET 到达前保留已显示内容，到达后解除本地回复覆盖。未保存或草稿会话不因刷新被当成新的空会话。
- 保存按现有服务的 trim／标题 120／单条 12,000／最近 100 条协议核对；超限截断在输入框上方持续提示，采用服务实际保存的内容，不错误显示“尚未保存”或静默导航。截断提示的真实视口位置和切回保留有回归。
- 最终定向集合：`/tmp/orbit-ink-signal-ai-all1.log` 241/241、75.341 秒、exit 0；随后修正服务规范化 ID 后的导航，新增用例先失败，再以 `/tmp/orbit-ink-signal-ai-conversation-final2.log` 验证最终会话与源码兼容 67/67、25.018 秒、exit 0。这两个集合重叠，不相加。`typecheck13` exit 0；`git diff --check` 通过；最终独立复审 APPROVE，无剩余 Important/Critical。保存后的本地快照、会话 ID 与导航均采用确认回执。
- 无真实远端写入、数据库访问、Web 改动或发布。原生键盘、VoiceOver、实机字号热切换、跨端真实会话持久化仍未验证。

## 后续执行

接着实施日程／待办／跟进。依照用户要求加快推进，本轮统一在各页面批次结束后跑全量回归；本地定向测试与逐批截图对照仍保留。

final result: passed

passed 仅指本报告列明的 AI 本地视觉与行为范围；原生、真实跨端和全包剩余批次仍未完成。
