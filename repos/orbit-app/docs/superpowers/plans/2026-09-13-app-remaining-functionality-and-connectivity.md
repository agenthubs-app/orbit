# App 剩余功能与连通性验收计划

> 执行状态：2026-09-13 用户要求开始逐项实施，并在每个功能完成对应验证后单独 commit。沿用已批准的整体视觉、原目录与 `chat-agent` 分支；不自动推送。后端修改、付费调用、真实业务写入、新增设计与原生依赖补丁仍遵守各自审批边界。按子项目细化并使用 `superpowers:executing-plans` 执行；未另行选择，不启动并行代理。

> 原始交付说明：本文最初作为剩余任务计划与只读排查结果交付；该轮仅编制计划，不表示全部功能已完成或上述独立审批已满足。

**Goal:** 保留已认可的界面，优先定位 App AI 不可用问题，补齐剩余业务能力，并用真实 App → API → 业务服务 → 持久化／跨端回读证明功能可用。

**Architecture:** 沿用 Expo Router、Screen、hooks/view-model、HTTP client 和共享契约。服务端负责身份、报名资格、工具权限、幂等和权威数据；App 负责正确消费、交互、失败恢复与原生表现。不以页面可打开、健康接口成功或静态测试全绿替代业务验收。

**Tech Stack:** 现有 Expo / React Native / TypeScript、HTTP API、Node Test Runner、RNW/Playwright 边界测试及 iOS Simulator；相机、真实推送等能力另以实体 iPhone 验证，不预设框架升级。

**Spec:** [9 月 10 日会议任务审阅稿](2026-09-10-app-meeting-task-review.md)、[已批准的 Ink & Signal 设计](../../designs/2026-09-12-ink-signal/README.md)、本文件的剩余范围与验收条件。原稿保留追溯；本文件作为后续剩余任务入口，不抹掉原批准或未通过记录。

## 一、执行边界

- 原计划编制轮次只新增本文。后续实施现已获准按功能分步提交 App 改动；每个功能提交前运行对应测试、GitNexus 变更检查并记录验证范围，不以“已提交”代替真实业务验收。未另行授权不推送、合并或部署。
- 后续 App 实施仅编辑 `repos/orbit-app`。Web/API 与根 Bridge 台账交对应负责人处理；负责人未确认前不写成已接单。
- App 只经 HTTP 访问业务数据；不直接读写 Postgres、Supabase、`orbit_records` 或浏览器 localStorage，不新增业务双写、离线写回队列。
- 共享 contract/schema/行业语言字典仅通过 `npm run sync:contract` 同步；不手改生成副本，不扩大白名单。
- 现有 26 个设计状态不重做。新功能需要新增表面时，沿用当前视觉并审阅必要的新增页面；首页内容差异先统一规格，不直接改掉已认可布局。
- 功能修改前逐符号 GitNexus upstream impact；报告调用者、流程和风险，HIGH/CRITICAL 先提示。索引未收录不等于零风险。
- 业务修复采用 TDD；先记录可复现的失败，再实施，再验证。未知基线失败不自动放行；不删断言、造空路由、换假数据或跳过失败换取绿色。
- 原有工作树安排、设计审批、TDD 例外、真实写入、原生依赖改动及发布门槛各自保留。计划批准不等于这些操作均已获准。
- 连通性检查默认只读。登录、OAuth、AI 生成、OCR、任务创建、报名、审核、签到、通知注册与真实消息等单列副作用；执行前确认账号、环境、数据对象和授权。
- 付费 AI/OCR 调用与重复实验先约定硬预算；只读故障复现同一假设最多 3 次，无新证据则停止该循环并换证据来源，不盲目重发。不能自动提高用户设定预算。
- 日志仅留方法、脱敏路径、HTTP 状态、Content-Type、错误码、关联请求 ID 与时间；不提交 Cookie、token、密码、密钥、原始个人内容、原生截图或构建日志。业务错误页不能展示服务端堆栈。
- 笔记中心保持后期范围；旧内容和归属必须保全，新入口就绪后才移除旧互动输入。关联人脉不等于分享笔记或通知对方。

## 二、起点与本次已核实事实

### 2026-09-13 执行记录

- 实施起点已保存为 `1efc95508a43326bc6c99dea6d955e7b3f4dc5b7`。源码、测试、计划与设计说明已提交；截图、压缩包和独立 prototype 仓库仍保留本地，不加入后续功能提交。
- 本轮起始全量回归：2190/2190，0 失败／取消／跳过，175.259 秒，exit 0；日志 `/tmp/orbit-r00-baseline-20260913.log`。这是自动化基线，不是原生业务通过结论。
- R-00：已从实际 Simulator 的服务器页确认当前地址为 `http://localhost:3000`。本地 Web/API 的锁定运行依赖有六项缺失；其中 AI 会话的 `@vercel/queue` 和名片相关路由的 `heic-convert` 有实际编译失败日志。具体失败请求的 HTTP 状态与生成／保存闭环尚未采集，R-00 不关闭。
- R-01：已建立初始模块／接口／副作用清单，执行 11 个匿名 GET 探针；公开探针与私有边界的结果分开记录，未将匿名 401 算作登录后的业务通过。完整消费者盘点及 L1–L5 验收仍未完成。
- R-01 只读补充：已形成[路由与 HTTP 消费点源码盘点](../../verification/2026-09-13-app-http-consumers.md)，覆盖本轮 263 个 TS/TSX 文件的 204 处匹配、66 个路由／layout 文件，以及动态请求和原生副作用；精确角色、实际环境／样本及逐层结果仍需验收，不提前关闭 R-01。
- R-00 受控回归补充：新增 8 项客户端错误分类／状态／401 边界和 4 项真实路由错误恢复测试，相关两文件 92/92、类型检查通过；四种测试进程内的故障注入被新增用例捕获。生产逻辑未变，原始请求关联、运行环境恢复和真实 AI 验收仍开放，详见执行记录 2.4。
- 12:32 用户明确授权自行继续处理，此前提出的 Web/API 本地锁定依赖恢复／必要重启及 R-03 固定账号资料替换移除方案不再重复申请。12:35 依赖恢复完成，package/lock 哈希不变、原依赖保留备份；原设备首页三个 GET 及 AI 会话／历史 GET 均 200，实际非空内容恢复。详见执行记录 2.5；新回答／工具／保存、跨端回读和费用硬预算仍需完成，不关闭 R-00。
- R-03 的固定账号资料替换按[独立实施计划](2026-09-13-profile-server-truth.md)处理：资料卡使用服务端原文，账号／AI 入口使用登录名与通用缺名标签；不改布局、真实资料或必填规则。三语／空资料／草稿保护已有红绿证据，整项注册／OAuth／资料补全仍开放。
- 后续用户明确将本次真实 AI/OCR 验收的累计费用硬上限设为 **5 美元**，不是每个功能或每次请求各 5 美元；此前 10 美元提议不生效。用户随后要求外出期间按计划自行调查、判断并执行，不再逐项问询。继续复用既有设计、在范围内作可回滚判断、逐功能验证及 commit；不自动推送、部署、充值或提高预算，不能安全完成的独立事项保留未完成状态。
- 预算预检：本地文件配置为 DeepSeek live 会话；现有 OCR 有转录、结构化及独立高风险复核请求，不能以一次按钮点击当一次模型调用。已查公开计价，尚未发起本次真实生成／OCR；费用控制与逐调用记录就绪前不启动付费验收。证据与限制见执行记录第八节。
- R-02 显式发送子功能按[独立实施计划](2026-09-13-ai-explicit-send-intent.md)完成：旧链接只预填、空新会话可编辑，首页点击通过单次作用域意图延续；延迟跳转跨身份不带入原问题，发送前保留草稿。最终全量 2243/2243、类型检查及契约同步通过，Simulator 空新会话只读确认；两次失败轮次与修复／复跑单独保留在执行记录第九节。B3 服务端幂等、实际生成、第二轮续聊及跨端回读仍开放。
- 证据、缺失依赖版本、故障清单及下一步关闭条件见 [R-00/R-01 连通性执行记录](../../verification/2026-09-13-app-connectivity.md)。下方 2.1–2.3 保留计划编制时的历史基线。
- R-02 续聊历史按[独立实施计划](2026-09-13-ai-session-continuation-history.md)补齐：已保存 session 与新草稿的每轮续聊均使用支持 history 的根 POST，保持原八条窗口、会话身份、保存回执及冻结重试。红绿证据与全量 2246/2246、类型／同步检查通过；实际模型与跨端回读仍未执行。
- R-00 付费验收前置的[累计费用控制](2026-09-13-ai-acceptance-budget-guard.md)已完成脚本和受控验证：逐模型请求原子预留、未知结果保留额度、跨进程／重启共用账本；30 项定向和全量 2276/2276、类型／契约通过。不改 Web 或产品模型参数，实际服务启用与真实生成仍须分别记录。
- R-00/R-02 原生实际进展（14:18–14:22）：在 `60443e36a` 及同一 API/账号上，原冻结问题重试真实生成并执行内部工具读取，保存 4 条消息后重开摘要一致；再只问前文代号，模型答对并保存 6 条消息，第二次重开摘要仍一致。累计 5 次供应商调用，保守估算 $0.012780，同一 $5 账本无未结算预留。首次 503 根因未明、Web 双向联验及服务端幂等仍开放；详细请求、分层结果和交接见执行记录 11.3，不以本次成功关闭整项。
- R-04 草稿与当前页回读按[独立子计划](2026-09-13-registration-draft-and-readback.md)完成源码和受控验证：刷新保留答案／辅助问答，问卷变化显式确认，防重及回执归属校验，有效保存／取消后触发两个 GET。后续已改用公开详情，并为报名问卷增加只走网络确认、失败停用写入和重读入口；默认内容缓存不变。15:09 原生公开详情 200、问卷仍 500，但已正确显示错误且不展示旧表单。各批次测试、先前失败及纠正记录保留于子计划；完整 R-04 资格、可填问卷、跨页／跨端回读和匹配消费仍开放，详见执行记录第十二节。
- R-08 日期与截止时间按[独立子计划](2026-09-13-task-date-editing.md)接入现有待办设置：独立日期 patch、版本与幂等重试、脏稿保护、回执归属和账号隔离。最终定向 116/116、全量 2379/2379、类型／六项同步通过；先前两轮全量失败及修正／复跑证据单列保留。15:30 同一 Simulator 已有待办的详情／历史／提醒 GET 均 200，新输入布局只读复验；没有真实任务写入。地点、个人日程、清空日期、提醒联动、全局时区及跨端回读仍开放，不关闭完整 R-08。
- R-12 普通字号长标题：15:44 用既有原生检查重新确认完整标题但仅 32pt 高、末尾裁切；只将标题固定高度改为最小高度，首次更新及返回列表重开均为 64pt，截图确认全文可见。定向 60/60、全量 2379/2379、类型通过；证据见[执行记录第十四节](../../verification/2026-09-13-app-connectivity.md#14-r-12普通字号待办长标题)。未改 RN 依赖、未输入／保存任务；全局 Dynamic Type 热切换和三语范围仍开放。
- R-05 既有回复草稿：补齐当前会话／预览写权限检查、异常回执保稿、单次保存与同稿重试 ID、刷新失败停用保存、账号／会话隔离。最终定向 91/91、全量 2416/2416 和类型检查通过；首轮全量的 5 个旧夹具失败及修正记录完整保留。15:58 原生列表 GET 200 但为空，15:59 不存在会话的详情／提取 GET 均 404，正确显示错误且没有写入口。未创建会话、保存草稿、生成摘要或发信；平台绑定、有效邀请、真实收发和非空原生／跨端验收仍开放，见执行记录第十五节。
- R-11 首页角标刷新：原先下拉只重读日程／待办／联系人，现同步重读收件箱与通知；有效完成待办回执后也重读，不在本地推算减数。新增 4 项交互测试，定向 68/68、全量 2420/2420、类型检查通过。16:14 同一 Simulator 的下拉已触发 5 个 GET，均为 200 JSON，未写真实事项。已读、有效目标、推送及跨端一致性仍开放，见执行记录第十六节。

### 2.1 已有成果，不重新立项

- 已选 Ink & Signal 的 26 个页面／状态完成本地实施与 RNW 对照，用户已认可整体设计。
- 四个根页、五项底栏、独立全屏 IORBIT、默认首页和二级页返回已接入；完整原生导航矩阵仍需补验。
- 首页独立读取日程／待办／联系跟进，已有待办完成与快捷入口；活动推荐／全部、资料编辑、消息提醒、日历日周月等已有基础能力。
- AI 已有历史、草稿保护、保存失败单独重试、稳定会话地址和部分防重复机制；不是从零建设 AI。
- `/contacts/new/import/[id]` 已接入真实服务端导入进度，包含轮询、取消、重试和会话隔离；原路由缺口不再列为待修。
- 名片三条复核流程及长字段排版已完成本地改造；不等于双面合并或真实 OCR 全流程已通过。
- 原生启动时 Keyboard 接收者问题及 Hermes 日期格式差异已有修复；运行中字号热切换与待办长标题裁切仍开放。

### 2.2 当前版本与基线

| 项目 | 本次结果 | 证明范围 |
| --- | --- | --- |
| App 源码 | HEAD `8b38b4eb8618505ca4f59f20dc323159e1ad784b` 加现有未提交 App 改动 | SHA 不能单独表示未提交实现；实施起点需保留 diff 范围 |
| `npm test` | 2190 通过，0 失败／取消／跳过，189.045 秒，exit 0 | 自动化单元、渲染及受控边界回归；不是原生真实网络验收 |
| `npm run typecheck` | exit 0 | 全量类型检查 |
| `git diff --check` | exit 0 | 当时差异的空白检查 |
| 整包 Simulator／真实业务联验 | 未通过／未完成 | 保留原生裁切、非空名片／运营及跨端回读缺口 |

自动化日志：`/tmp/orbit-meeting-audit-test-20260913.log`、`/tmp/orbit-meeting-audit-typecheck-20260913.log`。上述测试不包含独立原生标题尺寸检查的失败结果，不能用总数掩盖该失败。原生历史见[运行状态核验](../../designs/2026-09-12-ink-signal/2026-09-12-native-status.md)；用户之后已反馈重新进入 App，不继续沿用旧报告的登录阻塞，具体会话有效性由本轮请求再验证。

### 2.3 App AI 不可用：已确认症状，不预判根因

2026-09-13 08:42–08:44 JST 只读检查：

- 实际 iPhone 17 Pro / iOS 26.4 Simulator 显示“这次回答没有生成”，错误为 `ORBIT_APP_NON_JSON_RESPONSE`。已查看截图 `/tmp/orbit-ai-connectivity-current-20260913.png`，未点击重新生成或发送输入框内容。
- `src/api/client.ts` 仅在收到响应、且 Content-Type 不包含 `application/json` 时产生该错误；它不同于网络请求抛错时的 `ORBIT_APP_NETWORK_ERROR`。尚未捕获失败那次请求的完整 URL、状态、最终跳转与响应类型，不能断言是哪个服务、代理或页面产生的。
- Mac 上 `http://localhost:3000/api/health` 返回 200、`application/json`、`success: true`、mode `live`。该接口源码明确不调用模型，因此不能证明 AI provider 可用。
- 不带会话的 `/api/ai/conversations` 与 `/api/ai/conversations/sessions` 均返回 401 JSON。这只证明本地路由可达及拒绝未登录访问，不证明 App 当前会话失效，也不证明发送成功。
- Web 发送代码与 App 均有 `/api/ai/conversations` 消费入口，App 续聊还可能使用带 ID 路径；“Web 应该可以”是用户提供的线索，本轮没有登录 Web 发送新问题，尚未做同环境同账号对照。
- 本地服务监听 `127.0.0.1:3000`，Metro 监听 `[::1]:8081`。默认 App 地址是 localhost，但 App 可保存覆盖值，本轮未证实其运行时实际值。Simulator 与实体 iPhone 分开核对；不能把本地监听可达推导成实体手机已可访问。
- 未重放付费请求、读取密钥、重启服务、改地址或写业务数据。当前结论是“AI 发送链路出现非 JSON 响应，根因待定位”，不是“已修好”“没有连接”或“额度不足”。

## 三、剩余任务与原计划映射

P0 优先解决可用性、信任和关键闭环；P1 完善业务与原生体验。以下任务全部保持开放，子步骤已有证据的可以独立关闭，不能将整个任务提前标为通过。

| 新编号 | 优先级 | 剩余任务 | 原任务 | 依赖／进入条件 |
| --- | --- | --- | --- | --- |
| R-00 | P0 | 定位并恢复 App AI 真实请求链路 | APP-00、APP-04、APP-15 | 当前故障证据；必要的同环境请求授权 |
| R-01 | P0 | 全功能连通性清单与分层验收 | APP-00、APP-15，覆盖全部模块 | 从只读开始；写入／AI/OCR 另核授权 |
| R-02 | P0 | AI 显式发送、幂等与会话恢复收口 | APP-04 | R-00；B3；发送语义确认 |
| R-03 | P0 | 注册与基本资料完成流程 | APP-03 | B1；D2 必填范围确认 |
| R-04 | P0 | 报名资格、答案保留与完整回读 | APP-06 | B2；资料缺项处理与 R-03 对齐 |
| R-05 | P0 | 平台身份、聊天资格与邀请绑定 | APP-09 | B4；授权测试账号 |
| R-06 | P1 | @联系人与主动 AI 入口收敛 | APP-05 | R-02；B3；D3 |
| R-07 | P1 | 双面名片合并与真实导入闭环 | APP-10 | B5；相机／OCR／测试对象授权 |
| R-08 | P1 | 待办与日程的日期、时间、地点编辑 | APP-11 独立部分 | B6 中事项编辑协议；不依赖笔记 |
| R-09 | P1 | 首页规格差异、时区、活动发现与导航验收 | APP-01、APP-02、APP-07 | 首页内容取舍；B7；R-04 |
| R-10 | P1 | 人脉分析简化与 Pipeline 入口决策 | APP-12 | D3/D4；B7；R-06 |
| R-11 | P1 | 收件箱已读、目标跳转与角标一致性 | APP-13 | R-05；消息／通知现有契约 |
| R-12 | P1 | 三语、原生裁切与设备一致性 | APP-14 | D6；原生补丁独立审批 |
| R-13 | P1／后期 | 笔记、关联人脉、旧输入切换及笔记建议 | APP-08、APP-16，APP-11 后期部分 | D7；B8/B6；独立设计与迁移审阅 |
| R-14 | P0 收口 | 真实业务回归、跨端回读与交接 | APP-15 | 当前批准范围内任务及 R-01 矩阵通过 |

建议顺序：R-00 与 R-01 的只读盘点先行 → R-02/R-03/R-04/R-05 核心闭环 → R-06 至 R-11 功能补齐 → R-12/R-14 收口。R-12 的裁切定位与语言准备可提前做，不等全部功能结束才发现问题。R-13 单列后期，未实施时交接必须明确排除，不能宣称整个原会议计划已完成。

## 四、逐项执行与关闭条件

下列路径相对 `repos/orbit-app`；列出的“拟新增”文件只描述后续产物，本次不创建实现。接口未知部分先交付确认记录，不凭空定义字段；后端就绪后形成该子项目的可执行规格和失败用例。

### R-00：AI 请求链路，第一优先级

**触点：** `src/api/base-url.ts`、`ApiBaseUrlProvider.tsx`、`client.ts`、`src/hooks/useOrbitApiClient.ts`、`src/screens/settings/ApiSettingsScreen.tsx`、`src/screens/ai/AiConversationScreen.tsx`。只读对照 Web 的 AI route、鉴权边界和 `features/orbit-ai/service-factory.ts`，不在 App 任务里改 Web。

**测试／证据：** 扩展 `tests/api-client.test.ts`、`tests/ink-signal-ai-conversation.test.ts`；拟新增 `docs/verification/2026-09-13-app-connectivity.md`，保存脱敏的场景和分层结果。

- [ ] 记录失败时间、设备、App 构建／diff、运行时 base URL、Web URL、API 实际版本、账号角色及会话来源；保留当前草稿，不用更换账号或清缓存掩盖问题。
- [ ] 捕获一次明确授权的失败发送：方法、路径、HTTP 状态、Content-Type、最终 URL／跳转链、错误码和请求关联 ID。不能只记录屏幕上的通用错误；不得保存认证头和整段个人对话。
- [ ] 对照 Web 同环境、同账号、同类问题，分清根 POST、带 ID 续聊与 session 保存的差异；分别定位地址／传输、鉴权、响应封装、模型／工具执行、保存阶段。
- [ ] 对已确认根因补失败回归：HTML 登录页、HTML 500、非 JSON 错误响应、正常 JSON 业务错误分别断言；保留原状态与失败草稿，不把所有情况归为网络断开。
- [ ] 若根因在 API/部署/provider，提供带证据的交接并暂停依赖该修复的步骤；不改成 mock、不关闭鉴权、不绕过解析规则制造成功。
- [ ] 修复后在 Simulator 以新发送证明回答真实生成、按需工具读取成功、保存确认、离开再进入仍可回看；实体设备问题必须在原设备复验。旧历史能展示不算本项通过。

### R-01：全功能连通性，不止健康检查

**触点：** 现有路由、`src/api/endpoints.ts`、HTTP client、各 Screen 和 `docs/api-gaps.md`。检查记录使用上述拟新增验证文档；不默认新增产品内调试页或自动批量写入工具。

- [ ] 从实际路由及 HTTP 消费点列出每个功能的方法、路径、读取／写入／外部副作用、认证角色和环境；第三方原生能力单列。
- [ ] 按第五节矩阵先执行只读检查，确认不是缓存旧数据、空占位或静态页面；保留请求证据和数据版本，不清除真实数据制造空态。
- [ ] 每个模块分别填写传输、认证、协议、业务、持久化、原生结果；健康检查通过时其余列仍可失败或未执行。
- [ ] 对当前获准写入的模块使用隔离测试对象验证创建／修改／失败恢复／回读；清理或撤销也需明确授权，保留被测 ID 的脱敏映射。
- [ ] 每批功能完成立即复跑对应连通性场景，不等最终收口才首次访问真实 API。
- [ ] 形成故障清单：现象、最先失败层、责任端、复现步骤、影响范围、依赖、关闭证据。没有可用样本或权限时写受阻，不写通过。

### R-02：AI 显式发送与持久化稳定性

**触点：** `app/ai/[id].tsx`、`src/screens/ai/AiScreen.tsx`、`AiConversationScreen.tsx`、`src/view-models/conversations.ts`、`agent-history.ts`。

**测试：** 扩展 `tests/ink-signal-ai-conversation.test.ts`、`tests/conversation-view-model.test.ts`；拟新增 `tests/ai-conversation-persistence-interactions.test.tsx`。

- [x] 先确认新首页发送、业务上下文草稿、旧 `initialMessage` 深链的不同意图；后者默认仅预填，无初始问题的新会话也能输入。源码与真实路由受控回归完成，原生空会话已只读复验。
- [x] 用真实路由交互复现首次挂载自动 POST；修改后只由显式发送生成回答，普通挂载／刷新／返回不发模型请求。此项为受控 HTTP 边界的红绿结果，付费生成仍见下方独立验收。
- [ ] 与 B3 确认请求 ID、网络超时重试及服务端幂等语义；双击、重挂载、前后台和重复请求不产生意外重复回答／业务事项。组件锁不是服务端幂等证据。
- [x] 回归已实现的仅重试保存、回复／草稿保护和稳定会话地址；账号／服务器切换后旧结果不得进入新会话。相关自动化完成，未代替真实持久化验收。
- [ ] Simulator 新会话与 Web 创建历史分别续聊；断网恢复、保存失败和超时结果未知时均可解释并恢复，最终两端回读一致。

### R-03：注册与基本资料

**触点：** `src/screens/profile/AccountAuthScreen.tsx`、`ProfileScreen.tsx`、`src/api/AuthSessionProvider.tsx`、`mobile-auth.ts`、`src/view-models/account-auth.ts`、`profile.ts`、`mobile-profile.ts`。

**测试：** 现有 auth/profile 套件；拟新增 `tests/profile-completion-interactions.test.tsx`。

- [ ] B1/D2 确认服务端资料完成判定、必填字段、行业关联职位来源；姓名＋行业是原建议，不把未批准的公司／职位要求写成强制门槛。
- [ ] 普通注册和 Google 登录读取真实资料：缺项补全、已完成不重复拦截，完成后返回原目标；本次活动目标只在活动上下文填写。
- [ ] 区分基本资料与可选“提供／寻找／话题”，保留自填；资料提取先确认再保存，不自动写成事实。
- [x] 移除按特定账号 ID 替换姓名／公司／简介的展示规则，原文来自服务端；不删除真实资料，回归已有保存失败和刷新草稿保护。源码、受控回归与原设备资料预览见执行记录第七节；不代表下项新用户／OAuth／跨端写回验收通过。
- [ ] 用授权的新用户及已有用户在 Simulator 验证，两端回读姓名、行业及可选字段；Google OAuth 必须走实际系统回跳，不能只测按钮路由。

### R-04：报名资格、问答与回读

**触点：** `src/screens/events/EventRegistrationScreen.tsx`、`EventDetailScreen.tsx`、`src/view-models/event-registration.ts`、`events.ts`。

**测试：** `tests/event-registration-view-model.test.ts`、`tests/event-registration-screen-source.test.ts`；拟新增 `tests/event-registration-interactions.test.tsx`。

- [ ] B2 明确活动生命周期、报名窗口、本人报名状态、服务端时间、问卷版本与允许动作；不以客户端日期比较代替资格判断。
- [x] 先复现刷新覆盖答案／辅助问答和提交只刷新报名资源的差距；保护未提交答案，版本改变时明确提示及恢复方式。源码与受控回归通过，原生可填问卷仍被接口失败阻断，见执行记录第十二节。
- [ ] 覆盖未开放、截止、结束、取消、满额、待审核、已报名、取消后重报和临界时间；重复提交防重，失败保留输入。
- [ ] 成功后回读报名状态、活动人数、详情、相关列表及首页／日历中应体现的记录；未存在的同步能力先登记，不只刷新一张卡。
- [ ] 授权账号提交个性化答案，并确认后续匹配确实消费同一账号、同一活动的答案；接口接受但匹配未使用不通过。

### R-05：身份、聊天与邀请

**触点：** `src/screens/contacts/ContactDetailScreen.tsx`、`ContactIntrosScreen.tsx`、`src/screens/chat/RelationshipChatScreen.tsx`、`RelationshipChatDetailScreen.tsx`、`src/screens/inbox/RelationshipInboxScreen.tsx`、相关 view-model。

**测试：** 保留 relationship inbox/chat 回归；拟新增 `tests/contact-communication-eligibility.test.tsx`。

既有草稿写入的 App 保护已单列实现和受控验证，见[执行记录第十五节](../../verification/2026-09-13-app-connectivity.md#15-r-05回复草稿的写入边界)。不把此子功能当成 B4 平台资格或两人聊天完成证明；以下整项要求继续开放。

- [ ] B4 提供已验证平台绑定及聊天资格；未注册、匹配中、冲突、无权限、已确认分别呈现，未知资格默认不允许站内发信。
- [ ] 区分联系人私有记录、草稿预览与真实发送会话；不能用手填姓名、照片或联系人 ID 代替接收方平台身份。
- [ ] 使用有效服务端邀请链接，用户主动预览／复制／系统分享；打开入口不得自动发送邮件或短信。
- [ ] 使用两名授权测试用户完成邀请→注册／绑定→刷新资格→聊天，以及撤销资格后的拒绝；不得向真实陌生人测试发信。

### R-06：@联系人与统一主动 AI

**触点：** AI 两个 Screen、联系人详情／分析页、收件箱；拟新增 `src/screens/ai/ContactMentionPicker.tsx`。

**测试：** 拟新增 `tests/ai-contact-mentions-interactions.test.tsx`，回归已有会话和首页指导测试。

- [ ] B3/D3 确认类型化引用 DTO 与工具权限；按部分姓名／公司检索、区分同名，并传稳定 ID，不把整个联系人库拼入 prompt。
- [ ] 业务页进入 IORBIT 仅携带待发送上下文，逐一移除或转接重复的主动分析／写作入口；名片 OCR、报名辅助问答等已明确例外保留。
- [ ] 删除、无权限、跨账号引用明确失败；历史回看仍对应同一 ID。App 能读联系人不等于 Agent 已获工具权限。
- [ ] 验证联系人问题与不关联人脉的一般任务；权限不足、工具不可用、服务失败、真实空结果分别呈现。邮件仍为可编辑草稿，不自动发送。

### R-07：双面名片与真实导入

**触点：** `ContactAcquisitionScreen.tsx`、`BusinessCardIngestScreen.tsx`、`BusinessCardBatchScreen.tsx`、`BusinessCardImportScreen.tsx`、`src/view-models/business-card-ingest.ts`、`business-card-batch.ts`。

**测试：** 现有 ingest/batch/import/review 套件；拟新增 `tests/business-card-two-sided-interactions.test.tsx`。

- [ ] B5 确认同卡分组、正反面来源、字段冲突、一个联系人确认及去重协议；不把第二张图片当成另一张独立卡。
- [ ] 实现正面必选、反面可选、单面独立完成；冲突人工复核，姓名／公司保留源语言，原图片与来源可回看。
- [ ] 回归 MIME／字节／大小校验、权限拒绝、取消、重拍、图片删除／过期、重复导入和切账号隔离。
- [ ] 复验已实现的进度路由离开再进入、后台恢复、取消与重试；优先查明原生批次读取失败，非空复核必须有真实证据。
- [ ] 实体 iPhone 在授权 OCR 和隔离对象下完成拍摄→上传→识别→双面复核→一次创建→Web/App 回读；Simulator 选图不冒充真实相机验收。

### R-08：待办／日程独立编辑

**触点：** `src/screens/today/TodayScreen.tsx`、`src/screens/tasks/TaskDetailScreen.tsx`、`src/screens/schedule/ScheduleScreen.tsx`、`src/view-models/today-tasks.ts`、`schedule.ts`。

**测试：** `tests/task-detail-interactions.test.ts`、`tests/schedule-view-model.test.ts`、`tests/today-tasks-view-model.test.ts`，新增对应日期地点控件交互用例。

日期／截止时间子功能的实际范围、红绿结果与原生只读证据见[子计划](2026-09-13-task-date-editing.md)。目前服务器 update 不能清空已有日期，没有地点字段，schedule-items 只有 GET；这些缺口与真正跨端写回单列，不用已完成的日期界面替代下列完整要求。

- [ ] B6 确认无 contactId 的个人事项创建、日期／时间／地点更新、版本冲突与提醒语义；现有标题／备注保存能力直接复用。
- [ ] 日期、时间和地点从展示补成可编辑；失败保留输入，冲突不覆盖他人新版本。无明确时间、跨日和跨时区用例单列。
- [ ] 首页、待办、日历对同一记录修改后回读一致；公开活动预览不自动变成私人日程，待办不伪装成已确认会面。
- [ ] 只使用 App 内云端事项和现有提醒，不新增 Google／Apple 日历写权限。笔记建议由 R-13 处理，不阻塞本项。

### R-09：首页、活动发现与导航剩余项

**触点：** `HomeDashboardScreen.tsx`、`src/view-models/home-dashboard.ts`、`EventsScreen.tsx`、`EventDetailScreen.tsx`、`src/components/OrbitTabBar.tsx`、`AppScreen.tsx`、导航 view-model 与对应路由。

**测试：** 现有 home-dashboard、ink-signal-shell/events、initial-route、mobile-route-access、route-parity 套件。

- [ ] 记录首页规格选择：现有已批准稿为日程／待办／联系跟进，原会议稿要求推荐活动。用户确认是否仍需要首页推荐及位置，再更新对应规格；不默认牺牲跟进区或插入新模块。
- [ ] 确认账号时区与设备回退策略，替换当前固定东京口径并回归首页／待办／日历／活动跨日一致性；保留原有 Hermes 日期差异回归。
- [ ] 核实公开活动 API 返回集合是否完整、搜索筛选与分页的服务端范围；仅当存在缺口才扩展消费，不能把前端 8 条切片当全库搜索证明。
- [ ] 回归无推荐仍可浏览全部、推荐依据、真实主办方运营／审核／签到入口及普通用户权限拒绝。
- [ ] Simulator 冷启动、四根页进入／关闭 IORBIT、深链、登录回跳、通知直达及无历史返回逐项验证；原计划 Android 返回行为单列 Android 设备验证，未测不算通过。

### R-10：人脉分析与 Pipeline

**触点：** `ContactsDashboardScreen.tsx`、`ContactsScreen.tsx`、`src/view-models/contacts-dashboard.ts`、`contacts-analysis.ts`、`app/contacts/dashboard.tsx`、`app/contacts/pipeline.tsx`。

**测试：** 现有 analysis/dashboard 套件；拟新增 `tests/contacts-analysis-refresh-interactions.test.tsx`。

- [ ] 保留无需 AI 的行业／来源统计和钻取，区分当前用户长期目标与某个人脉的关系阶段。
- [ ] B7 提供生成时间及数据／分析版本：数据改变仅提示重新分析，进入页面不自动付费生成；执行按 R-06 汇入 IORBIT，原页可看已存报告。
- [ ] 修改长期目标仅写经确认的目标字段，不因一个目标覆盖整份资料；失败保护草稿。
- [ ] D4 明确是否隐藏独立 Pipeline；若批准，只收敛前台入口及旧链接落点，保留生命周期、归档、历史和后端任务。

### R-11：消息、提醒和未读一致性

**触点：** `RelationshipInboxScreen.tsx`、`src/components/OrbitNotificationsCoordinator.tsx`、`src/hooks/useRelationshipInboxBadgeCount.ts`、首页与相关 view-model。

**测试：** relationship inbox、notification model、`tests/notifications/merged-notification-lifecycle.test.ts`、`notification-registration-races.test.ts`。

首页下拉及有效待办完成后的角标重读已单列修复，见[执行记录第十六节](../../verification/2026-09-13-app-connectivity.md#16-r-11首页角标刷新)。原有角标口径、缓存回退和页面样式保持不变，不把此子功能当成整项未读／推送一致性完成。

- [ ] 复用当前统一入口与分区，保留消息／待办提醒／活动通知的真实类型；当前仅本次查看有效的忽略不冒充服务端已读。
- [ ] 验证点击真实目标、已删除／无权限反馈、已读写入及返回首页角标一致；新消息到达后的更新方式明确。
- [ ] 退出、换账号、权限撤销和前后台变化不沿用旧计数、不重复注册；通知偏好及设备 token 的注册／解绑单列真实设备验证。

### R-12：三语与原生一致性

**触点：** 现有主题／组件与主链路 Screen；如复查仍无基础设施，拟新增 `src/i18n/OrbitLocaleProvider.tsx`、`messages.ts`、`zh.ts`、`ja.ts`、`en.ts`。原生依赖只在专项方案获批后处理。

**测试：** 现有触控／键盘／长内容测试与 `tests/native/task-title-layout.mjs`；拟新增 `tests/app-locale-render.test.tsx`、`tests/app-long-content-render.test.tsx`。

- [ ] D6 明确中／日／英主链路及账号语言／设备回退；UI 翻译与姓名、公司、OCR、邮件和聊天原文严格区分。
- [ ] 覆盖首页、登录资料、人脉、名片、活动报名、IORBIT、待办日历、收件箱与设置；遗漏页面明确列出，AI/OCR 输出语言按现有或批准契约传递。
- [x] 修复普通字号待办长标题裁切，用已存在的原生失败检查加最新 AX／截图证明全文可见，不仅让 RNW 通过。2026-09-13 原同一任务先 RED（32pt）后 GREEN（64pt），返回列表重开复验及截图全文可见；证据见执行记录第十四节，不代表其他原生／字号项通过。
- [ ] 对运行中 Dynamic Type 裁切单独审批可回滚补丁方案；不禁用缩放、不整屏重挂载丢草稿／焦点。双向、多次热切换及返回后复验。
- [ ] 实际 Simulator 验证浅／深色、普通／大字号、键盘、安全区、底栏及触控；VoiceOver 单列。非空名片／运营需有效样本；隔离只读装配需批准，且仍不能替代真实业务联验。

### R-13：后期笔记与人脉切换

**触点：** 笔记新页面先审阅路由／设计；既有 `ContactDetailScreen.tsx`、`ContactNotesSection.tsx`、`src/view-models/contact-notes.ts`、`contact-detail-editor.ts` 与人脉资料编辑协同切换。笔记持久化由 B8 服务端提供。

**测试：** 笔记列表／编辑／多人关联／失败恢复／账号隔离交互；保留 contact-notes 和资料编辑回归，并增加旧入口不再写入的断言。

- [ ] B8 先确认独立笔记身份、多人关联、私密权限、版本、幂等与旧备注分类保全；由对应负责人提供迁移方案，本文不批准迁移或删除。
- [ ] 一篇笔记只保存一份正文，可关联多人；不同人脉详情打开同一原文，移除一个关联不删正文和其他关联，所有编辑发生在笔记界面。
- [ ] 完成新笔记读写／关联回读与旧内容访问后，移除详情备注录入及资料编辑中的互动摘要等残留写入口；旧链接安全返回，不能长期双写。
- [ ] 人脉资料备注、身份及授权字段仍可编辑，失败保留输入；现有只读身份字段须先获得合法编辑契约，不由 App 擅自补写。
- [ ] 清理固定联系人／会话 ID 的事实替换特判，保留原文和所有者；不把所有旧备注强制当互动笔记或全部删除。
- [ ] D5/B6/B8 单独确认笔记转建议：笔记 ID／版本、相关人脉、日期歧义、建议类型、接受幂等；用户确认后才创建，多人笔记不自动复制事项，原文变更使旧建议重新校验。

### R-14：最终业务验收与交接

**产物：** 更新 `docs/superpowers/plans/2026-09-08-app-wide-native-qa-matrix.md`、本计划的执行状态及 App 内验证记录；按 `bridge/templates/handoff.md` 提供交接内容，由 Bridge 协调者更新根台账。

- [ ] 普通用户：注册／Google → 补资料 → 首页 → 搜索活动 → 报名 → 首页／日历回读；核对资料、问答答案、状态、人数。
- [ ] 人脉：双面名片 → 一个联系人 → 授权资料编辑 → @提问 → 历史回看；笔记及笔记建议只在 R-13 获准完成后加入，否则明确未验范围。
- [ ] 身份：外部联系人不可聊天 → 主动分享邀请 → 授权账号绑定 → 刷新资格 → 可聊天，撤销后拒绝。
- [ ] 主办方：同一真实账号正确角色进入运营／审核／签到；普通用户执行同入口受控拒绝，不用演示身份替代。
- [ ] 同环境、同记录 Web 写→App 回读和 App 写→Web 回读；至少覆盖资料、联系人授权字段、AI 会话、任务／日程、报名；笔记随 R-13 加入。
- [ ] 全套测试、类型、同步副本、真实连通性、Simulator、实体设备和跨端写入各报结果；失败及未运行项保留。
- [ ] 交接包含 App/API 实际版本、未提交范围、设备、账号角色、另一端影响、剩余缺口及授权范围；无共同运行版本或接口负责人配合时，业务联验保持未完成。

## 五、全功能连通性验收矩阵

### 5.1 分层口径

| 层 | 要证明的事实 | 不足以替代的检查 |
| --- | --- | --- |
| L1 地址与传输 | 当前设备连接预期 API，路径／TLS／跳转／超时正确 | Mac 可打开网站；Metro 正常 |
| L2 身份与协议 | 正确账号／角色，JSON/envelope/schema 符合消费要求 | 不带会话的 401；仅 HTTP 200 |
| L3 功能执行 | 模型、工具、业务服务／worker 确实处理该操作 | 健康检查；读取旧历史；仅按钮可点击 |
| L4 持久化与跨端 | 保存确认、重开回读、两端同记录一致 | 乐观本地显示；接口接收但异步任务未完成 |
| L5 原生体验 | Simulator／必要实体设备上的权限、键盘、前后台等真实行为 | RNW 渲染；浏览器文件选择器 |

### 5.2 模块场景

以下是待执行矩阵。除第二节明确列出的只读探针外，不能预填通过。每行按 L1–L5 分别记录；无写操作的场景可将对应项标为“不适用＋理由”。

| 模块 | 只读／基础检查 | 授权后的真实操作与回读 | 必测失败／设备场景 |
| --- | --- | --- | --- |
| 服务地址与健康 | App 运行时地址、实际 API 版本、健康 JSON；Simulator/实体机各查 | 显式改地址后身份及缓存隔离，需授权才操作 | 错地址、服务停机、HTML 页面、重定向、超时；不能自动改配置 |
| 账号／OAuth | 当前会话、资料、角色 | 注册／登录／Google 回跳／登出／账号切换 | 过期、撤销、Cookie 与原生 jar 冲突、安全 next、切号不泄漏 |
| 个人资料 | 当前资料及可选字段，确认无特定账号替换 | 补全、编辑、保存后两端回读 | 未填必填项、版本冲突、刷新不覆盖草稿、保存失败 |
| 首页 | 待办／日程／跟进分别真实 GET，不自动触发 AI | 完成事项后列表／首页／日历一致 | 一个模块失败不掩盖其他模块，跨日／跨时区 |
| IORBIT 生成与工具 | 会话／历史、权限与实际请求类型 | 新发、续聊、通用任务、联系人／活动等工具读取；真实模型结果与来源 | 当前非 JSON、401/403、429、服务错误、工具拒绝／空数据、超时结果未知；费用预算 |
| AI 保存与恢复 | 指定 session 回读 | 保存失败只重试保存；Web↔App 续聊与重开 | 双击、返回／重挂载、切账号、断网；不能重复生成 |
| 人脉列表／详情 | 搜索／筛选／详情及原文 | 授权字段修改后两端回读 | 同名、删除、跨账号、长公司名、旧内容保留 |
| 名片／导入 | 真实批次／进度／图片授权读取 | 实体拍摄、双面 OCR、复核、一个联系人、取消／重试／恢复 | 权限拒绝、超限、过期图片、重复卡、后台／换账号；OCR 预算 |
| 活动发现／详情 | 推荐和全部公开集合、搜索范围、状态／时间 | 推荐选择等现有显式操作回读 | 无推荐、访客访问、分页、取消／结束、服务失败 |
| 报名 | 服务端资格、问题版本、本人报名状态 | 提交／取消／重报，答案、人数、列表、匹配消费 | 未开放／截止／满额／待审，重复提交，问题版本改变，失败保留答案 |
| 活动运营／审核／签到 | 真实角色、运营状态、非空申请队列 | 授权测试活动审核／签到后状态回读 | 普通账号拒绝、重复操作、并发冲突、入口与业务权限一致 |
| 待办／日程／提醒 | 同一事项在各页的 ID、日期、状态 | 创建、编辑日期地点、完成／取消／提醒；两端回读 | 无联系人事项、时区边界、冲突、网络重试、弱网草稿 |
| 身份／聊天／邀请 | 平台绑定与资格、既有消息真实来源 | 分享邀请、测试账号绑定、发送／接收、资格刷新 | 未注册、冲突、无权限、撤销；草稿不能冒充发送 |
| 收件箱／推送／设置 | 消息／提醒类型、未读、当前通知偏好 | 已读、正确目标、角标更新、实体推送接收与点击 | 目标失效、权限撤销、重复注册、登出／换号、前后台 |
| 人脉分析 | 确定性统计、目标、已存报告及版本 | 显式分析、目标保存、数据变化后的重算 | 进入不付费生成、部分失败、旧分析提示、资料不被整份覆盖 |
| 笔记与关联（后期） | 列表、原文、按人脉回读、私密权限 | 多人关联、修改／解除、旧内容保全、确认建议 | 一份正文、失败草稿、失效建议、重复接受、旧入口关闭 |

### 5.3 每条记录的必填项

场景 ID；关联 R/APP 编号；执行时间；App/API 实际版本和环境；设备与系统；脱敏账号角色；前置数据和授权；方法／路径；HTTP 状态／Content-Type／错误码／请求 ID；L1–L5 结果；写入后回读；Web↔App 结果；失败责任端；下一步和客观关闭条件。

结果只用“未执行／通过／失败／受阻／不适用（附理由）”。服务停机、无账号／角色、无非空样本、provider 未就绪、worker 未完成和另一端未配合必须显式记录，不能折算成通过。

## 六、接口协作与必要决策

| 编号 | 需要确认的内容 | 影响 |
| --- | --- | --- |
| B1 | 权威资料完成判定、必填边界、行业／职位／标签来源 | R-03 |
| B2 | 三类活动状态、资格／原因、服务端时间、问卷版本、匹配消费与回读 | R-04/R-09 |
| B3 | 发送幂等、保存恢复、类型化联系人引用、当前账号工具权限及错误语义 | R-00/R-02/R-06 |
| B4 | 已验证身份绑定、冲突／候选、聊天资格与有效邀请链接 | R-05/R-11 |
| B5 | 同卡正反面、字段来源／冲突、一次创建、导入进度与旧批次兼容 | R-07 |
| B6 | 独立事项编辑／提醒与版本；笔记建议的来源、接受和去重 | R-08；笔记部分 R-13 |
| B7 | 公开发现完整范围、推荐依据、统计／分析版本和目标字段归属 | R-09/R-10 |
| B8 | 独立笔记与多人关联、私密权限、原文版本、幂等、旧内容保全／兼容 | R-13 后期 |

这些是确认清单，不表示所有 API 都不存在。先核对现有服务与契约，再列真实缺口；提供方和消费方各记录 source_ready/consumer_ready，双方真实验收后才是 verified。

沿用已批准视觉与 D7 后期方向；仍需分别明确：首页推荐活动是否保留及位置、D2 必填资料、D3 主动 AI 入口边界、D4 Pipeline、D5 笔记建议机制、D6 三语主链路、原生补丁／隔离 QA 方案，以及真实写入和费用预算。已批准且未改变的部分不重复索要批准。

## 七、执行时验证命令与交接

在 `repos/orbit-app` 执行；前两项为当前已有命令，针对性用例按被修改任务加入：

```sh
npm run typecheck
npm test
node --test --import tsx --import ./tests/helpers/register-render-hooks.mjs tests/contract-sync.test.ts tests/api-schema-sync.test.ts tests/domain-sync.test.ts
node --test --import tsx --import ./tests/helpers/register-render-hooks.mjs tests/api-client.test.ts tests/base-url.test.ts tests/ink-signal-ai-conversation.test.ts
git diff --check
```

同步检查不等于执行 `sync:contract`；后者仅在源契约变更批准并发布后进行。上述命令不触发真实 AI/OCR，不替代第五节运行时矩阵。修复当前 AI 前后使用同一获准场景验证，不改成无关问题来掩盖故障。

### 本次文档交接

- App：新增本文，既有源码和视觉保持不变；上一轮 2190 项及类型检查基线如第二节。
- Web/API：本轮只读源码及不带会话的探针；未改代码／配置，未验证登录后真实 AI 生成，也未确认用户所指 Web 环境。
- 另一端影响：后续按 R-00 失败层与 B1–B8 清单交接，不把计划当成 API 负责人已接单。
- 验证范围：本轮核对任务覆盖、文件锚点、审批边界、Simulator 现有错误截图及本地只读 HTTP；没有实施上述功能、发送付费 AI、执行 OCR、写业务数据或进行跨端业务联验。
- 完成口径：每个子项目按实现、自动测试、真实连通性、原生与跨端证据分别关闭；计划写完只表示规划交付完成，不表示功能完成。
