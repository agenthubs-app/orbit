# Sprint 0065 — Web 取消报名真正显示确认并提交

修复 Phone Web 活动详情和报名资料页的取消确认：点击后确实出现确认，保留报名不写入，确认取消才调用正式接口并回读；原生仍使用现有原生确认方式。

本项承接用户已批准的活动取消／再次报名目标及0064真实验收。独立原因是当前真实 Web 依赖 `react-native-web/dist/exports/Alert/index.js` 的 `static alert() {}` 空实现，两取消入口都直接调用原生 Alert；既有 mock 测试不足以证明 Web 弹窗可用。验收必须包含真实 Web 依赖对应的 RED，以及配置修复后同账号真实页面取消和再次报名。只修这两个活动入口，不替换所有页面的 Alert。
