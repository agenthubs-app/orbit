# Sprint 0047：Web 历史 AI 会话可以继续提问

从 Web 历史列表打开已有 AI 会话后，用户能看到唯一的输入框，并把追问发送到同一会话；刷新及原生 App 重开仍能读取新增轮次。

发送期间不能重复提交，失败保留可恢复请求和输入，不自动另建会话或再次调用模型。此项修复 0044 实测的 Web composer 缺口，不替代 0036 的源版本、笔记工具接线或外部 provider 验收。

具体范围与验证见 [PLANNER](PLANNER.md)。
