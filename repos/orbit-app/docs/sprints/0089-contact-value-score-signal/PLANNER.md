# Sprint 0089 — 联系人列表那个"价值分"要么有区分度，要么别占位置

**Plan revision:** 1。**模式:** existing-codebase / single-generator。运行状态只在登记表。
**原需求:** TODO.md「2026-09-19 全量功能与 UI 复核」第 3 条。
**单一目标:** 查清价值分为何几乎恒为 84，据此修算法或换掉列表右侧内容；分数不可信时不显示。
**易读目标:** [GOAL.md](GOAL.md)。
**基线:** `chat-agent` = `ededaa6ac`。未启动，run_count = 0。
**进入条件:** 本地库演示账号 78 联系人可查（分布统计需要真实数据）。

## 已查明的事实

| 事实 | 位置 |
| --- | --- |
| 观测分布 | `/contacts/list` 78 人：76 人 84 分，2 人 72 分 |
| 读取点 | `src/view-models/contacts.ts:319-321` `valueScore()` 从 `contact.value.score` 取数 |
| 渲染文案 | 同文件 `:449-450` `valueScoreLabel = 价值分 ${score}`；`null` 时不渲染（已有的 null 分支是可用的落点） |
| 数据来源 | 种子生成的 `value.score`（TODO 第 3 条原始记录） |

**判断 1：先查算法还是先查数据，取决于 `value.score` 是被算出来的还是被写死的。** 第一步是查明 `value.score` 的产生路径；84 这种整齐的众数更像常数或被钳制的默认值，但不预设结论。
**判断 2：不用假值填充最显眼的位置。** 现有 `null → 不渲染` 分支是对的；问题在于上游给了一个看似有效的常数。判定不可信（如全量方差为 0）时宁可不显示。
**判断 3：若分数本身无法在本 Sprint 内变得有信息，则换内容而不是保留一个没信息的数字。** 换成什么由列表已有字段决定（最近互动时间／未完成待办数），不新增数据依赖。

## 范围与文件

- 调查产物：分布统计脚本与报告（scratchpad + `docs/audits/`）。
- orbits：`value.score` 的产生处（算法或种子，待第一步确定，按 RULES 第 0 节登记）。
- App：`src/view-models/contacts.ts`（`valueScore`／`valueScoreLabel`）、联系人列表屏幕与其测试。
- 排除：引入新的评分数据源或模型调用；改联系人详情页的评分展示（除非同一符号）。

## 验收契约

| SC | 可观察行为 | 必需证据 |
| --- | --- | --- |
| SC-0089-01 | `value.score` 的产生路径被写明（算法／种子／默认值） | 源码位置 + 分布统计 |
| SC-0089-02 | 修前修后分布对比，修后不再由单值占压倒多数（或该位置已换成有信息的内容） | 分布报告 |
| SC-0089-03 | 分数缺失或判定不可信时不显示假值 | view-model 单测 |
| SC-0089-04 | 列表截图前后对照 | 截图 |
| SC-0089-05 | 无回归：联系人相关定向集通过；两端 typecheck 0 | 摘要 |

## 一次 Generator 的执行顺序

1. 登记 run-01；分支 `codex/sprint-0089-contact-value-score`。2. 查产生路径 + 跑分布。3. 决定修算法 vs 换内容并登记。4. RED→GREEN。5. 截图对照 → 收口。

## 最小测试与检查

- 档位：App L；若改服务端评分则 orbits L。定向集：`contacts` view-model 测试 + 列表屏幕测试。

## 失败与交接

若 `value.score` 由尚未实现的评分系统占位，本 Sprint 只落"不显示假值 + 换内容"，把评分系统单列并登记，不假装分数已有意义。
