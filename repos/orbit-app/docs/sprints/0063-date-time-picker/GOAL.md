# Sprint 0063 — 点选日期与时间即可创建个人日程

用户已明确批准此设计，并要求开始实现。运行状态由主协调线登记，不在本目标页复制。

用户明确要求改善新建个人日程的日期时间输入：点击日期直接出现日历，点击时间直接选择小时和分钟，无需手动填写 YYYY-MM-DD 或 HH:mm；参考成熟日历产品，作为独立新 Sprint 推进。

ROOT 已为此需求保留编号0063。当前 PhoneWeb 公网固定来源 b3562f4de8936d783cda097e1a30e1e3554208c7；设计依据为该版本的 PersonalScheduleTimeBlock、个人日程草稿／校验与既有底部弹层。正式实施基线及文件归属由ROOT与0061／0062交付协调后冻结。

获准体验：日期行打开月历底部弹层，可切换月份、选择今天／明天和具体日期；开始／结束时间分别打开小时分钟选择器。保留30分钟／1小时／2小时与全天快捷操作。弹层确认才更新草稿，关闭或取消保留原值；选完仍须用户点击页面保存。

验收应覆盖：无需键盘完成新建；已有任意分钟数回显不被取整；跨月、闰日、跨天和全天日期边界正确；取消不改变草稿；编辑保存后正式回读与详情一致；中文／日文／英文及手机触摸／键盘访问正常。原时区、DST歧义拒绝、重复系列／单次范围、关联对象和版本冲突保护仍有效。

执行仍采用一个主任务协调、有限副线并行。副线保留 phoneweb- 前缀，默认 GPT-5.6 Sol / medium。设计批准后由 ROOT 协调空闲执行线和文件／QA／构建窗口，避免与现有A／B的0061／0062冲突；未经批准不创建副线或开Generator。公网现版本保持运行。

方案详见同目录 DESIGN.md。本目标页说明已批准的预期结果，不作为测试通过或Sprint完成的证据。
