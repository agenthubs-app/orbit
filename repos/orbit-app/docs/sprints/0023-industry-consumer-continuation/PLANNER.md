# Sprint 0023 — 已批准接续方案的执行契约

**Revision:** 1。**模式:** existing-codebase / single-generator / cross-client。
**基线:** 根 `chat-agent`，`d531c42d4`；产品部分实现 `5355f0c6a`，原 0020 的 blocked 历史不变。
**批准:** 2026-09-14 用户在明确收到“8 个新增文件及接续 Sprint”的确认说明后表示全部同意并要求直接持续执行。本页只登记该批准方案，不增加新的功能设计或审批停点。

## 目标、输入与范围

- 规格与技术细节：[0020 冻结 Planner](../0020-secondary-industries-self-profile/PLANNER.md)及[已批准接续方案](../0020-secondary-industries-self-profile/CONTINUATION_PROPOSAL.md)。原目录、字段语义、工具白名单和数据验收不变。
- 源码白名单：上述原 Planner 的完整文件表，加接续方案列出的 8 个生产文件和 3 个测试文件；各文件仅按所列用途修改。数据生成源／原子写路径的独立部分按接续方案办理，不直接运行旧生成器。
- 承接原 RED 与 WIP 实现，不删除重做，不把已获批准的接线再送审。现有资料 adapter 已在原白名单，新增的是其页面模型类型文件。
- 当前主代理作为唯一 Generator，原地执行；无第二实现者、Evaluator、自动再生成、push／merge／部署。协调者负责根 Git 和本目录登记。
- 必要付费调用已允许，累计 USD 5 上限保留，实际费用逐次核对。真实数据库写入／迁移／部署仍按明确对象确认，不从未来未知事项的泛化批准推导任意副作用。

## 验收契约

完整继承原五项 SC，不缩减分母；本编号只是获准的接续执行，不改变原 0020 结果。

| 本 Sprint SC | 对应原 SC | 必需结果 |
| --- | --- | --- |
| SC-0023-01 | SC-0020-01 | 14／79 字典、父子验证、旧字段兼容、同步与类型成立；保持原已验证行为。 |
| SC-0023-02 | SC-0020-02 | Web/App 本人和授权联系人选择、HTTP 保存与重开、失败保稿、双用户隔离、真实同记录双向及必要原生证据。 |
| SC-0023-03 | SC-0020-03 | 三种 HTTP 搜索输入落实结构化过滤；profile.getSelf 经真实 registry/runtime/observation/synthesis 工作，并取得必要真实模型证据。 |
| SC-0023-04 | SC-0020-04 | 工具参数不接受他人身份，空／失败／账号切换正确；完整 trace 脱敏，资料读取不写业务记录。 |
| SC-0023-05 | SC-0020-05 | 完整正常测试数据清单、源和投影一致、获准测试库补齐／版本保护／幂等回读，无未解释遗漏或失败；未具备精确环境时保留 blocked。 |

## 执行顺序与检查

按接续方案的任务 1→2→3 执行；数据源／真实验证独立处理，不阻塞已就绪接线。

1. 联系人 provider/HTTP：解除两个既有测试 TODO，确认预期 RED；补四参数传递、错误类型／配对拒绝、持久化与旧三参数兼容；再接两端详情交互。
2. 搜索 HTTP 与资料页面投影：增加三种请求的实际服务过滤测试，RED 后实现；验证旧五领域过滤、空值、非法 ID 与两端资料重开。
3. 本人资料工具：真实注册器／runtime 的受控 RED，增加 artifact、认证上下文、结果进入 synthesis、整条 trace 脱敏；回归旧工具及恶意输入，预算核对后做必要真实调用。
4. 数据维护按原 DATA_SCOPE 和接续方案核实精确源与版本条件写，保留全部正常对象与反例。真实副作用未具备条件时继续其他独立工作。
5. 每个符号先 upstream impact；HIGH/CRITICAL 报告并覆盖传递消费者。TDD、完整相关测试、两端 typecheck、契约变更同步，H 档提交前全量及必要业务验证；失败不记作通过。阶段性提交后继续，不因报告或提交停止整体工作。

已有基线：App 全量 2581 pass；Web 2932 pass／51 fail／168 skip／2 TODO。用户已允许保存部分实现并继续，已知失败不阻止范围内修复，也不豁免最终验收。新意外失败按原两个本地修复轮次限额处理，只暂停依赖部分。

执行证据使用 App 忽略目录 `build/harness-state/evidence/sprint-0023/run-01/`，日志放 `build/harness-logs/`。结束后才创建实际 REPORT；每个提交记录真实 SHA、对应行为和残留验收，不提前生成完成报告。
