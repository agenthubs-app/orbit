# 实施顺序与减少返工

2026-09-14。用户已要求完成 0022 文档后按计划逐步实施，并在离线期间优先推进不需要新决定的工作、不再提问。本文是调度与技术准备计划，不替代各 Sprint 的 PLANNER，不自动批准新接口、迁移或真实写入。

## 顺序

不按编号硬排，也不同时修改共享文件。0001／0002 已完成，不重跑。

| 执行序位 | Sprint | 为什么放在这里 |
| --- | --- | --- |
| 1 | 0020 | 先统一行业 ID、父子校验、本人资料工具；后续资料表单、名片和 AI 消费同一字段，避免先接一级再重接二级。 |
| 2 | 0003 | 使用 0020 字段补全注册／资料与返回路径；同步落实已确定的生日私密规则。先补 B1 的跨端执行边界。 |
| 3 | 0009 | 先统一设备时区、全天日期和写入转换，任务编辑、首页、日历不再分别修时间。旧 Planner 的账号优先描述须按已确认设备策略修订。 |
| 4 | 0005 | 先处理 B3 稳定 ID、幂等、保存与恢复边界，再做会话组织和模板；B3 的 Web 文件必须先补入审阅后的跨端计划。 |
| 5 | 0021 | 在可靠会话之上接入口来源、分组和管理；同时冻结元信息版本与首次保存接口，后续入口只接一次。 |
| 6 | 0006 | 使用 0020 联系人行业和 0021 会话入口协议，实现 @ 与已确认模板；向 0022 发布工具入口。 |
| 7 | 0010 | 使用 0009 时间规则与 B6 真实协议完成事项编辑／清空／提醒，不让列表统一承担新写入协议。 |
| 8 | 0022 | 用已稳定的任务动作和模板统一列表、筛选与旧路由；首页及语言工程直接消费最终入口。 |
| 9 | 0004 | 资料基础已定，再接 B2 报名／问卷／取消重报，避免重复改完整性拦截。 |
| 10 | 0007 | 行业字段已定，再接 B5 双面 OCR 与单联系人保存；设备／真实样本不齐则不领取。 |
| 11 | 0008 → 0012 | 先 B4 身份、邀请和真实聊天，再做已读、前台更新与通知目标；摘要取消按已确认决定接入 0008。 |
| 12 | 0011 | 日期、任务、人脉入口、AI 行为已经稳定，再落首页活动区、最多五条待办和分析／目标保存。 |
| 13 | 0013 → 0014 → 0015 | 先语言基础与账号同步，再翻译已定型的页面；分类已有中日英 ID 标签，不再复制字典。 |
| 14 | 0016 → 0017 | 先原生导航／输入／辅助功能，再做当前主链路的同版本跨端集成验收。 |
| 15 | 0018 → 0019 | 按原已登记后期边界完成笔记及其 AI 建议，再做原需求最终收口。迁移仍保留独立审批。 |

这是优先队列，不是让就绪工作等待无关阻塞：0020 未批准时，0009 的无副作用技术盘点可先做；0007 缺 OCR 样本时，0008 的文档／就绪核查不必停下。实际 Generator 只领取依赖和批准均齐全者，不用代码 stub 绕过依赖。

## 只改一次的共享边界

| 共享位置 | 生产者 → 消费者 | 执行约束 |
| --- | --- | --- |
| 行业字典／profile／contact 契约 | 0020 → 0003／0006／0007／语言 | 先发布 ID／schema，再同步 App；不手改生成副本。 |
| 会话保存／历史／发送意图 | 0005 → 0021 → 0006 → 0022／0019 | 先可靠性，再元信息，再模板，最后迁移入口；保持同一 session／message ID。 |
| 日期解析／序列化／展示 | 0009 → 0010／0011／语言 | 一套设备时间来源、一套纯日期语义，调用方不再补固定东京偏移。 |
| 任务列表与跳转 | 0010 的动作 + 0006 的模板 → 0022 → 0011／0015 | 不把日期编辑、任务筛选、AI 生成三个职责混在一个 Sprint。 |
| 首页 | 0011；0018 最后接笔记按钮 | 0011 改布局和内容区，0022 不改首页；笔记真实路由存在后才接按钮。 |
| 语言／原生验收 | 页面稳定 → 0013～0016 | 不先翻译即将移除的跟进独立页面；历史兼容入口仍测。 |

### 笔记顺序的明确取舍

0018 当前契约明确依赖 0017，且笔记迁移／权限协议尚未审阅。为省一次首页按钮接线而提前实施整套笔记，会改变这个阶段边界。本轮保留原依赖，不默默倒置它。

因此允许两次各有必要的首页变更：0011 完成活动区和待办布局；0018 在创建路由真实可用时，把旧快捷位换成“记笔记”。两次不重复重写同一功能。过渡期间不放空笔记按钮，不把首页最终入口已完成写进 0011 报告。此项最终由 0018 验收。

## 开始实施前的技术准备

以下准备不消耗 Generator run，不创建假 REPORT，也不要求离线用户即时答复。

- [x] 0020 入口核查：已核对 14／79 目录、同步通道、资料写入链和 seed 副作用；[核查记录](0020-secondary-industries-self-profile/READINESS_AUDIT.md)区分候选扫描与实施期完整 inventory。代码方案审阅和精确测试库写入仍待批准，不把目录认可扩大为全部批准。
- [x] 0003 技术补充：[B1 提案](0003-profile-completion/TECHNICAL_PREPARATION.md)区分旧六项完整度与注册完成、列出生日私密存储、版本冲突及跨端文件／反例；已确认产品规则直接复用，旧账号强制补全策略随技术审阅处理。
- [x] 0009 技术准备：已核对日期函数、现有设备读取可用 API 与 Hermes 回归；[技术准备](0009-timezone/TECHNICAL_PREPARATION.md)列出全天、DST、前台变化和脏稿提案，现有 68 项基线通过。尚未修改 Planner／产品，书面审阅仍保留。
- [x] 0005／0021／0006 协议准备：[B3 提案](0005-ai-session-reliability/B3_PROTOCOL_PREPARATION.md)明确稳定 ID、首次持久化、请求恢复、元信息及引用的先后归属；隔离内存复现旧快照将四条消息覆盖为两条。原 503 仍未关闭，未操作真实历史或付费调用。
- [x] 0022 文档准备：新增规格、工具能力与消费者矩阵并完成登记；等待 0006／0010 真实交付及书面审阅，不表示功能已实现。
- [x] 0010/B6 核查：[技术准备](0010-task-schedule-editing/B6_TECHNICAL_PREPARATION.md)确认个人待办 POST 已存在，清空／地点仍被拒绝；新内存存储复现同版本并发编辑均成功但只留一次修改。现有 15 项测试通过，不代表并发安全或真实验收完成。
- [x] 0004/B2 核查：[技术准备](0004-registration/B2_TECHNICAL_PREPARATION.md)确认既有截止时间／admission／题库版本能力，隔离验证三类依赖异常逃出 GET；留存日志定位原问卷响应附近题库表不可见的 42P01，仍需精确库／schema／迁移核查及当前复验，未连接数据库。
- [x] 0007/B5 核查：[技术准备](0007-two-sided-cards/B5_TECHNICAL_PREPARATION.md)确认 manifest 不保留卡片分组／正反面字段，首次运行时包含迁移；明确卡片级确认和图片生命周期的待审增量，没有上传或 OCR。
- [x] 0008/B4 核查：[技术准备](0008-identity-chat/B4_TECHNICAL_PREPARATION.md)区分邀请文案暂存、草稿持久化与真实绑定／投递；摘要入口移除可独立交付，但仍须原 Planner 明确要求的范围审阅。

## 启动门槛与离线处理

执行指令已收到，不能再把“用户尚未要求实现”当成阻塞。不同性质的缺项分开记录：

1. 已批准产品规则：直接复用，不再提问。
2. 可通过源码／本地测试确定的技术事实：由代理核实；相关测试只作为基线，不能冒充新功能验收。
3. 明文保留的书面设计／跨端白名单审批：记录待审版本；离线时继续其他安全准备，不默认通过。
4. 数据库、迁移、真实样本写入、服务重启或额外费用：没有适用授权不执行。

每个就绪 Sprint 沿用原地 chat-agent、唯一 Generator、TDD、符号 impact、最小必要验证和逐功能 commit。全局登记表是唯一可变运行状态来源；本文不另造 ready／completed 状态。阻塞项不会因离线、排序完成或文档写完自动变成 ready。

## 当前动作队列（2026-09-14，本轮持续推进）

这里记录动作，不另设 Sprint 生命周期。已勾选的技术准备不等于对应 Sprint 完成。

- 可执行：本轮 B6/B2/B4/B5 核查文档均已提交；0020 代码方案获批后按已核目录开始 Task 1 的 TDD，真实写入保持独立门槛。
- 可调查：本轮已列出的源码／隔离／留存日志核查完成；B2 下一段是具体数据库／schema／migration ledger 核对，需要适用环境授权，不能以“只读”擅自访问未知目标。B4/B5 下一段涉及新存储／身份／图片生命周期设计，须先审阅相应提案方向；不重复已完成基线填时间。
- 等待决定：0020 现有书面代码方案已发出一次非阻塞审阅请求；0003/B1、0005/B3 和 0009 的新增技术策略尚未批准；真实数据库、迁移、设备写入各自按精确对象授权，不默认通过。

本轮不因等待决定结束整体任务；先完成可执行和可调查项，不把这些准备伪装成已开始 Generator。

## 后续共享入口的补充核查

2026-09-14，源码基线 `18a8627b9`。这里只记录影响实施顺序的事实，不另建三套未经批准的实现计划。

- **0011/B7：** Web `features/mobile/contacts-dashboard-service.ts` 每次组装响应使用 `generatedAt: now()`；其 schemaVersion=1 是响应格式版本，不是数据或分析报告版本。不能把这两个字段直接显示成“报告刚生成／分析未过期”。App `ContactsDashboardScreen.tsx` 的 saveContactDashboardGoal 确实调用 buildProfileUpdateRequest 后 PUT 整份资料。提案应复用 0003 的字段级版本写入基础，只提交 relationshipGoal；报告需另带持久化生成时间、所用源数据版本和分析版本，进入／刷新不能生成报告。首页位置／五条上限／Pipeline 决定不再询问；代码仍有 0006／0022 消费依赖及书面边界审阅。
- **0013 语言：** 已有共享 zh／ja／en 字典与 Web `shared/i18n/orbit-language.ts`；App 尚无自己的界面语言 provider，不能新增另一套枚举。ManualProfile.preferredLanguage 是必有三语值，Web manualProfileFor 对缺值默认 zh，不能从响应 zh 区分“从未手选”与“明确手选中文”。落实已批准的账号／设备策略需单独表达 system/manual 来源或可空手动偏好，并定义旧数据兼容；不能把所有既有 zh 自动判为手动，也不能擅自清除真实偏好。复用 0003 的版本写入基础，读取失败保留该账号已知偏好并显示未同步，换账号清除前账号作用域；来源字段与旧值策略仍需审阅。
- **0018/B8：** 已有 `shared/api-schema/contact-notes.ts` 是 contact.notes 子集（noteId/body/createdAt/privacy），App contactNotesToView 仅展示 private 项；没有在这份消费契约中提供独立笔记版本与多人关联集合。现有 private 标签不等于已验证新笔记权限。应先制定旧资料备注／互动记录的分类与可回退迁移方案，再实施一份正文、多条关联；不得把现有备注批量猜测成同一种笔记。0017 前置、正式 D7 路由／B8 存储设计与迁移批准均保留，不能只为首页少接一次按钮就提前运行迁移。

以上属于已完成只读核查，没有新语言偏好、目标或笔记写入；无新增测试／完成声明。

## 进入代码阶段的实际缺项

| 对应动作 | 具体门槛及证据 | 可独立部分的处理 |
| --- | --- | --- |
| 0020 代码 | 现有 PLANNER 的跨端方案尚未得到书面审阅；已认可的是行业目录，已有执行指令不再算缺项 | 目录、写链、数据入口及同步核查已完成；代码审批与真实数据更新分开，不要求为代码先批准生产库。 |
| 0003／0005／0009 基础 | B1/B3/时区技术提案已有具体字段和反例，仍保留审阅；0005 原真实恢复与 503 证据未关闭 | 已做资料／日期基线和会话丢写隔离复现，不再靠重复运行证明新功能。 |
| 0004/B2 恢复 | 已取到历史 relation 不可见证据；当前库／schema／迁移状态需精确环境检查，适用批准未齐 | 不执行已有迁移、不把捕获异常当修复；资格／版本源码核查已完成。 |
| 0007／0008 新能力 | B5 卡片级协议和 B4 身份／站内投递是跨端设计增量，不是现有预览接口接线 | 双面 Schema 与邀请／草稿边界已核；摘要移除可独立，但要补齐明确要求的范围审阅。 |
| 0006／0010／0011／0021／0022 消费 | 依赖上述行业、时间、发送／任务协议；各 Planner 明确保留书面审阅 | 入口、共享边界和具体消费差异已准备，不用 stub 或旧不安全写接口抢跑。 |
| 0012～0017 | 0012 依赖真实双用户消息及通知环境；三语依赖账号偏好／最终页面；原生与跨端验收需对应已提交功能及授权设备／对象 | 不能提前跑空会话／旧页面／路由存在检查冒充后续 SC；语言来源差异已查明。 |
| 0018／0019 | 原后期前置、旧内容分类与迁移／笔记权限及建议接受协议未批准 | 新建入口／AI 模板规则已记录；无分类方案时不盘点或迁移真实私密正文，不生成空实现。 |

下一次推进优先复用对 0020 当前版本的明确审阅结果，开始唯一 Generator；不会要求重新批准行业目录、原地 chat-agent、逐功能 commit 或已确认产品选择。其他提案的审阅可分开进行，不要求先一次性批准全部 Sprint。
