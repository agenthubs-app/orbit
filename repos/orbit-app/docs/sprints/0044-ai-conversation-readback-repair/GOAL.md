# Sprint 0044：AI新会话发送后能真正打开

新建AI会话并发送问题后，回答和用户消息能在当前页、历史列表、返回重开及同账号Web中读取；重试不会重复建会话或丢掉第一条内容。

存储或provider失败必须明确显示，保留可恢复输入，不能“发送成功”后跳到不存在的会话。AI是否实际调用任务、跟进、笔记等工具另外留证，不只看HTTP200。

具体边界见 [PLANNER](PLANNER.md)。
