# Data authority registry

> Generated from `features/data-authority/registry.ts`. Edit the registry and regenerate this view; do not maintain a second authority table.

| Domain | Canonical store | Owner key | API contract | Projections | AI policy | Migration |
| --- | --- | --- | --- | --- | --- | --- |
| notes | orbit_records/notes | user_id = accountId = ownerUserId | /api/notes and /api/notes/:id | Notes Web and App clients | actor-scoped notes.query allowlist; body only for authorized get; untrusted text | canonical; legacy: none; done when: all note readers use NoteService |
| tasks | orbit_records/tasks | user_id = accountId = ownerUserId | /api/tasks and /api/tasks/:id | Tasks and Today views | actor-scoped tasks.query allowlist; taskSuggestions denied | canonical; legacy: taskSuggestions; done when: suggestions remain explicitly separate from confirmed tasks |
| relationship_followups | orbit_records/tasks | user_id = accountId = ownerUserId | internal Followups live graph filtered to Relationship Connection | confirmed Relationship followups; followups.reviewQueue derived recommendation | actor-scoped followups.query allowlist; message bodies denied; evidence summarized | canonical; legacy: none; done when: persistent facts and derived review queue remain separately labelled |
| schedule | orbit_records/personal_schedule_items | user_id = accountId = ownerUserId | /api/schedule-items and /api/schedule-items/:id | Today, Schedule, Event action, and Agent action; legacy orbitScheduleItems reader (compat: remove after migration parity evidence and one compatibility release) | actor-scoped schedule.query allowlist after canonical projection; provider tokens denied | consolidating; legacy: orbit_records/orbitScheduleItems; done when: dry-run reports zero conflicts/orphans and all writers use PersonalScheduleService |
| push_devices | SecureStore/orbit.pushDeviceId + orbit_records/pushDevices | server-injected actorId + installation deviceId | /api/devices/push-tokens and /api/devices/push-tokens/:id | App notification lifecycle; legacy /api/devices/push-token compatibility adapter (compat: remove after upgraded-client telemetry shows no legacy calls for one release) | denied; device ids and push tokens are never model-visible | consolidating; legacy: AsyncStorage/orbit.notifications.device-id.v1, orbit_records/devicePushTokens, /api/devices/push-token; done when: legacy id is migrated/revoked and lifecycle traffic uses plural endpoint |
| ai_provider_context | ephemeral Orbit AI request context | server-injected authenticated actorId | Orbit AI live runtime provider-input contract | provider synthesis prompt | message/history/memory/outcomes are purpose-bound, bounded, redacted, and never authority | canonical; legacy: none; done when: all provider inputs appear in the AI visibility manifest |
