# 名片大文件上传：问题与实现约束

状态：线上问题已复现，原文件持久化、服务端授权接口、受限读取、队列清理、V2 消费接口及 V2 网页直传已实现；V1 图片/PDF 消费与客户端尚未接入，不能视为全部上传问题已修复。

## 已实现的基础与验证

新增第 4 号迁移与 `business-card-upload-sources.ts`：上传记录先于授权持久化；许可绑定工作区、用户、服务器路径、大小和固定有效期；处理租约支持接管并拒绝旧租约提交；目标写入与消费回执使用同一数据库事务。删除在许可过期并经过一分钟余量后执行，先提交 deleting 状态，删除中断可重试，删除后的消费回执仍支持幂等查询。

真实 PostgreSQL 的 31 项针对性测试通过，覆盖新记录的归属隔离、并发租约、事务回滚、唤醒失败、许可过期、删除中断，以及已有 V1/V2 图像日志和 V2 仓储回归。

后续实现增加 `POST /api/contact-drafts/business-card/uploads` 上传预约与 `/uploads/token` SDK 令牌入口，要求当前会话、同源 Origin、最多 16 KiB JSON 元数据。签发前按持久化记录校验用户及路径，保持原许可的大小、类型和有效期；不请求完成回调，也不接受客户端回调作为消费凭据。SDK 内部使用部署凭证，不导出配置值。

`business-card-upload-source-reader.ts` 只读取当前工作区、用户和记录绑定的私有路径，限制实际流量、验证精确字节数及 SHA-256，并在整个打开/读取过程使用 60 秒超时。它仅验证字节完整性，解码是否有效仍由后续转换流程负责。到期记录及删除重试已接入两条队列和独立扫描的待办查询；未来未到期记录不触发空转，过期后的活动租约与删除退避仍保留待办。

这一阶段的 22 项针对性测试通过，包括 6 MiB 图片与 50 MiB PDF 字节流读取、超限/摘要不符/超时、真实 PostgreSQL 的扫描恢复与删除退避、HTTP 会话/同源/归属和授权约束。本地合成字节流不代表合法图片/PDF 已通过线上转换。

V2 新增 `POST /api/contact-drafts/business-card/uploads/consume-v2`，接受 sourceId、batchId、itemId、operation，以及替换必需的 expectedVersion。首次上传核对原文件记录与 manifest 的摘要、大小和类型；原文件读取和真实图像归一化受现有并发门限控制。仓储上传/替换支持借用消费事务连接，通过 savepoint 保留原有批次锁与版本规则，不提前提交外层事务。衍生图先进入持久化写入日志；目标变更与原文件消费回执原子提交后才发送工作队列消息。回执绑定目标、操作及替换版本，失败重试不会再次替换同一项目。

V2 消费阶段的 33 项相关测试通过：27 项既有仓储、worker、衍生图日志回归，以及消费集成与 HTTP/授权测试。集成使用超过 4.5 MB 的真实 JPEG（尾部填充至 6 MiB），验证实际归一化、目标写入后的回执失败回滚、提交后的发送失败、重复消费、替换版本、旧图清理任务和已取消目标。该阶段完成 V2 服务端消费；V1 消费与后续客户端、线上验收分别记录进展。

V2 客户端现由登录后的 `GET /uploads` 查询服务端存储模式。私有存储模式只向应用接口发送元数据，文件通过 Blob SDK 直传；本机文件系统模式保留原上传路径。会话缓存仅保存预约标识及对象路径，不保存文件或上传令牌。重复操作先查询消费回执，只有服务端明确返回缺失原文件或过期时才补传或重新预约；网络超时、未知错误不丢弃已有回执。上传与替换共用这一恢复流程，替换回执仍绑定原版本。上传队列遇到失败后停止当前循环并保留文件，避免无限自动重试。

客户端接入阶段 23 项针对性测试通过，覆盖预约/上传/提交响应丢失、并发重复调用、刷新后恢复、过期预约、本机路径、请求超时、HTTP 恢复状态以及真实数据库消费回归。类型检查无新增诊断，仍有原有 109 项错误。实际浏览器仍受 Vercel 登录保护，浏览器完整旅程需单独验收。

## 已取得的证据

2026-09-07 在部署 `orbit-4uzy83c1n-liqys-projects-33c8ddec.vercel.app`，使用既有隔离 QA 账户上传合成 JPEG：原文件 6,291,456 字节，multipart 请求 6,291,667 字节，返回 413，响应包含 `FUNCTION_PAYLOAD_TOO_LARGE`，未创建批次。记录 `/tmp/orbit-large-upload-probe.log`；探针 `/tmp/orbit-large-upload-probe.cjs` 不打印账号凭证或配置密钥。

现有 V1 将全部图片或 PDF 放进一个 FormData 请求，图片上限 10 MiB、PDF 上限 50 MiB、批次上限 500 张；V2 按单项 PUT 上传，原图上限 10 MiB、批次上限 100 张。两条通路仍受函数入口限制，即使单个文件不大，多张图片的总请求也可能超限。

[Vercel 函数限制](https://vercel.com/docs/functions/limitations)规定请求体上限 4.5 MB；[Blob 客户端上传](https://vercel.com/docs/vercel-blob/client-upload)允许浏览器直接传文件到私有 Blob，由服务端授权单次上传。项目已安装的 `@vercel/blob` 类型声明支持 private upload、路径授权、maximumSizeInBytes、validUntil、allowOverwrite 和签名完成回调。无需读取或导出已部署的 Blob 配置密钥。

## 保持的产品范围

- 保留 V1 多图、50 MiB PDF 和现有每页一张名片的解析流程；不能只修 V2 或把 PDF 功能删除。
- 保留 V2 manifest、单项上传/替换、摘要匹配与续传，不改变 10 MiB 原图上限来绕开平台限制。
- 文件字节不再经过浏览器到应用函数的大请求体。应用接口只处理有明确数量和大小限制的上传元数据与文件标识。
- 保留本机开发路径；旧 API 的小文件兼容不能被当作大文件功能完成。

## 原始上传的持久化边界

原文件与供识别/复核使用的 JPEG 衍生图必须分开管理。原图或 PDF 在转换前也可能遇到断网、取消、函数重启，不能成为无记录的对象。

1. 服务端先创建上传记录，再签发有期限的上传许可。记录包含工作区、actor、服务器生成的对象路径、预期 MIME/字节数/摘要、目标通路、过期时间和处理状态。对象路径不得直接使用客户端提供的 URL 或文件名。
2. 令牌生成入口逐次校验当前会话、上传记录归属、路径和状态，限制大小与内容类型并禁止覆盖。完成回调必须通过 SDK 签名验证；回调不依赖浏览器登录 Cookie，也不能独自作为业务写入成功的依据。
3. 消费原文件时，从记录中的私有对象路径读取，按实际流量限制字节数，验证摘要和解码结果。不可根据客户端声称的大小直接无限读取，也不可接受任意远端 URL。
4. 消费需要持久化租约。目标批次/项目写入与上传记录的消费回执必须在同一事务中完成，或使用可证明等价的幂等目标标识；不能先创建批次再在另一个事务标记 consumed，否则超时重试会创建重复批次。
5. V1 的 PDF 解析和大批量转换需要验证函数执行时间与内存，而非只证明传输突破 4.5 MB。若必须移到后台，需持久化分页/项目进度、失败重试和取消，不用一个无进度的长请求替代。

## 删除与许可重放

不能在原文件消费后立即删除对象、却让同一路径的上传令牌继续有效：对象删除后，旧令牌可能重新上传并留下孤立文件。清理时间必须晚于该记录所有已签发许可的最大有效期及明确的时钟余量，或者采用经过验证的等价重放防护。

清理先提交不可再消费的 deleting 状态，再执行物理删除，最后写删除回执；崩溃后可重复删除。尚未完成上传的记录也要到期回收。待办判定、延迟唤醒及独立扫描应覆盖原文件，OCR 不可用不能阻止清理。原文件处理成功与衍生图回收记录的存在不能相互替代。

## 验收必须覆盖

- 真实 Preview：超过 4.5 MB 且不超过 10 MiB 的图片、超过 4.5 MB 的合法 PDF、多张图片合计超过 4.5 MB，均可进入正确批次/项目；随后取消并核对清理状态。
- 10 MiB 图片与 50 MiB PDF 的边界；超出应用限制时在上传授权前拒绝；实际对象大于声明时仍拒绝消费。
- 匿名、其他 actor、其他工作区、伪造路径/回调、过期许可、取消后的消费与重复提交。
- 上传成功后浏览器中断、转换失败、消费事务回滚、写入后响应丢失、删除后状态回写前重启。
- PDF 和批量转换的资源限制、部分进度及恢复；V2 替换/续传；本机小文件兼容。
- 浏览器成功、失败、进度、重试和重新选择文件的实际操作；不能用一次小图片 API 成功代替这些证据。

现有阻塞条件仍独立存在：Vercel 浏览器登录、有效 SMTP、真实模型/OCR、独立调度和目标用户试用。直传完成也不代表这些验收完成。


## V2 实际 Preview 验证

部署 `orbit-g69rfx967-liqys-projects-33c8ddec.vercel.app` 完成实际 API/Blob SDK 验证，记录 `/tmp/orbit-direct-upload-live-smoke.log`。隔离 QA 账户创建批次后：

- 6,291,456 字节 JPEG 通过私有 Blob 直传，消费返回 200，衍生图可读取并正确解码为 96×64 JPEG。
- 7,340,032 字节替换图片同样直传成功，消费返回 200，衍生图为 128×80。
- 两次消费回执重放均返回 reused，项目版本未再次增长；改变替换回执版本返回 409。
- 上传前缺失原文件返回可恢复的 404；匿名模式查询及消费返回 401；取消批次后图片读取返回 404。

测试批次 `bcb2:ce4689f7-b13f-49de-a817-9fce0e869c66` 已取消。原文件记录为 `3bf2a2f7-0417-4196-a8d8-1027010882a6`、`d065b0ec-872c-4fa5-9834-17d5a3795d45`。此验证未证明延迟原件物理删除已执行，也未完成受 Vercel 登录保护的真实浏览器全流程。

实际 V2 React 组件的受控浏览器验收另行通过：第一次模拟 503 后停止在失败状态并出现“重试上传”；点击后只再发送一次上传，状态变为“已上传”，识别按钮可用。受控服务最终计数 attempts=2、uploaded=true。失败提示区分网络问题和无效图片；页面已改为说明原件临时保存并安排自动清理，不再声称原件从不保存。

## V1 PDF 分段转换基础

`renderBusinessCardPdfPageRange` 支持指定起始页和本次页数，逐页等待 `acceptPage` 完成，再生成下一页；返回总页数、下一页及是否结束。画布在等待页面保存前释放，PDF 解析任务在成功、失败、超出批次上限和取消时销毁。原 `paginatePdfToCardImages` 通过这一入口保留数组返回值，兼容现有小请求接口。

12 项 PDF/批次接口测试通过，包括实际 500 页 PDF、包含 49 MiB 合法流对象的 PDF、分段结果与完整转换一致、保存失败后的起始页恢复和取消。它们证明本地转换与资源边界，尚未证明 V1 云端大批量导入完成。

下一步 V1 后台集成必须持久化每页接受结果与进度，不能仅在内存保留 `nextPage`。现有批次创建仍接收完整图片数组，不能直接用它在单个函数中完成最大批次。原文件有效期、处理中租约及衍生图写入日志必须覆盖准备阶段；取消必须阻止迟到的页面写入，完成时目标批次和源文件回执必须原子提交。当前分段转换函数不创建任务、不发送队列消息，也不改变 V1 网页仍走旧上传路径的事实。

## V1 后台准备任务存储

`business-card-v1-preparation/repository.ts` 沿用 V1 的通用记录表和工作区事务锁，新增任务、原文件绑定和请求幂等记录三个集合。任务记录在同一 JSON 中保存页面索引、下一文件/页、租约、失败次数、过期时间及最终批次 ID；单次保存原子更新索引和进度，页面不包含图像字节。一个原文件只能绑定一个任务，重复请求或同源列表的新请求键返回既有任务，改变源列表顺序会发生冲突。

处理租约为 15 分钟，过期后另一连接从已提交页接管；失败采用 60 秒退避，连续五次失败终止，成功提交页面会清零失败计数。任务期限取所有原文件期限的最早值，不延长原件保存期。取消会拒绝迟到的页面提交，并让原文件进入到期清理；仍保留原件与衍生图日志，不能声称物理删除已立即完成。准备页面要求独立的、仍为 pending 的衍生图写入日志，页面 ID 不可复用，图片摘要必须匹配原文件。

最终完成入口只接受已经打开的原文件消费事务，借用其连接写入目标批次和任务完成状态；外层消费回执提交之前不会独立提交。原文件行锁先于 V1 工作区锁，取消和完成遵守同一顺序。

15 项相关 PostgreSQL 测试通过，其中 7 项覆盖新任务仓储，其余回归原文件与 V1 图像日志。测试涵盖发布失败、并发去重、租约接管、进度回滚、取消、混合文件页序、退避、过期，以及模拟目标记录与任务/原文件回执一起回滚。此为任务存储阶段的验证；实际正式批次写入见下节。

## V1 准备任务转正式批次

`storage/business-card-new-batch.ts` 为旧上传和新准备任务提供同一正式记录写入入口。旧上传仍逐项保存图片；`business-card-v1-preparation/finalize.ts` 引用任务已提交的图片路径，不重新上传字节。正式批次使用任务 ID，保留文件顺序、PDF 页码及原有摘要语义，初始状态为 processing，项目为 pending，可被现有 OCR 领取逻辑处理。

最终提交先确保 V1 图片引用触发器已安装，再由原文件消费事务锁定源记录、任务和仍为 pending 的图片写入记录。所有图片必须匹配工作区、任务和项目路径。正式批次、项目、图片 attached 状态、任务 completed 状态以及原文件回执共同提交；排队发生在提交之后，发送失败通过回执重试，不删除已经提交的图片。取消任务、已进入删除的图片和已存在的目标批次均不能覆盖创建。

25 项测试通过，包含 4 项真实 PostgreSQL 最终提交测试、准备任务测试及旧上传接口/服务/事务/图片日志回归。验证了实际批次可读和 OCR 可领取、混合 PDF/图片顺序、提交后队列失败重试、最终状态写入失败时全部回滚、删除防护及并发/取消。图像字节在这些最终提交测试中不走真实 Blob；云端大文件、自动转换 worker、任务 API 与网页流程仍待接入和验收。这一阶段尚未部署。

## V1 准备 worker 与队列装配

`business-card-v1-preparation/worker.ts` 每次领取一个准备任务，校验并读取一个原文件，转换一张图片或至多五页 PDF。PDF 分段计算整个批次剩余容量，保留最多 500 项和 50 MiB 原 PDF 上限；图片保留 10 MiB 原件上限，解码为 JPEG 后引用原件摘要。每页使用新的图片 ID，独立写入日志先于上传，页面索引提交先于下一页。PDF 转换设 120 秒取消信号，同进程只允许一个 V1 转换任务进入读取/解码阶段。

原文件租约在本轮结束时释放，进程退出未释放时等待过期；任务接管从持久化进度继续。图片上传后若取消或租约改变，迟到的页面不能进入任务，图片日志仍承担清理。无效图片/PDF 和超出批次容量为终止失败；可恢复处理失败按原有 60 秒退避和五次连续失败规则执行。完成阶段失败单独退避 60 秒，让其他任务继续，在原件期限内重试；完成扫描避开原件仍持有活跃租约的任务。

`configured.ts` 装配生产原件读取器、图片存储/写入日志、任务仓储和最终批次提交。现有 V1 队列先处理准备工作，有进展时把 OCR 留到后续消息，避免单次函数同时承担完整转换和 OCR 等待。待处理扫描覆盖 pending/processing/ready 准备任务，因此活跃租约和退避期间不会丢掉后续唤醒；OCR 未配置不妨碍文件准备。

20 项 PostgreSQL/队列回归通过，其中 5 项 worker 测试使用真实 PDF 渲染与图片解码，覆盖混合文件分段续跑、页面提交失败后的新图片 ID、上传中取消、无效/超大文件、完成退避和租约等待。测试的对象存储为受控适配器，尚未证明 Vercel 自动消费消息或真实 Blob 完整导入。任务 API、V1 网页直传和进度/取消入口仍待接入，本阶段未部署。

## V1 导入任务 API

- `POST /api/contact-drafts/business-card/imports` 接受 `{requestKey, sourceIds}`，返回 202 与公开任务进度。请求体限制 32 KiB，保留 500 个源文件引用上限，重复请求复用既有任务。
- `GET /api/contact-drafts/business-card/imports` 返回当前账号最近 50 个任务；`GET .../imports/{id}` 查询单个任务。
- `POST .../imports/{id}/cancel` 接受空 JSON 对象，取消任务并安排原件清理；最终批次已经创建则返回冲突，不能用准备任务接口取消正式批次。
- `POST .../imports/verify-source` 接受 `{sourceId}`，在短期处理中租约下读取实际原件并校验大小和摘要，帮助客户端处理上传响应丢失。无论验证成功或失败都会释放本次租约；不会解码文件或创建批次。此路由最长 120 秒，原件读取仍有 60 秒限制。

所有接口要求登录，写操作校验同源，未知字段、伪造账号/路径和查询参数被拒绝。响应禁止缓存；公开任务只包含进度、固定错误码和完成后的批次 ID，不返回源路径、图片路径、租约、原始页记录或字节。

10 项 API/worker 联合测试通过，包括真实 PostgreSQL 500 源文件提交与取消、排队失败后的幂等恢复、账号隔离、32 KiB 请求边界、6 MiB 对象实际摘要核验、缺失/损坏/过期原件以及后台转换回归。对象存储在本地测试中使用受控传输；V1 网页直传与任务进度界面尚未接入，云端证据另行记录。

### 首轮云端验收发现与修正

Preview `orbit-iauyvybwf-liqys-projects-33c8ddec.vercel.app` 的真实请求通过了 6,291,456 字节图片及 51,381,691 字节、12 页 PDF 的私有直传和摘要核验，任务提交与重复提交均返回 202。队列自动转换第一张图片后，PDF 转换返回 `PDF_INVALID`，测试任务已取消。因此该轮不能算完整通过。

相同 PDF 在本地成功渲染。Next.js 的原构建清单仅包含 `pdf.mjs`，缺少动态加载的 `pdf.worker.mjs`；`next.config.js` 已为后台队列和旧 PDF 上传接口显式纳入这一依赖，重新构建后的两个 `.nft.json` 均包含实际存在的 worker 文件。

首轮同时观察到成功分段后的下一次处理等待数分钟。正常分段不再以失败消息重试作为唯一续跑机制：若本轮有进展且仍有工作，先发送新的管线唤醒，成功后才确认当前消息；发送失败仍抛错保留原消息重试。无进展时保留退避，避免 OCR 未配置等状态产生新消息忙循环。16 项相关测试通过，包括续发确认顺序、发送失败、空闲退避、API、转换和配置回归；修正后的云端结果需另行确认。

### 修正后的真实云端结果

Preview `https://orbit-pthokrt1h-liqys-projects-33c8ddec.vercel.app`（代码提交 `1b0580b0`）通过完整的 V1 原件到正式批次测试：6,291,456 字节图片和 51,381,691 字节、12 页 PDF 经真实私有 Blob 直传及大小/摘要核验后，任务由 Vercel 队列自动分段推进到 13 项、两个源文件全部完成，再原子创建正式批次。全程没有手动触发 worker。重复提交复用任务，最终批次 GET 和标准化 JPEG GET 均为 200，读取图片实际解码为 96×64 JPEG。

任务与批次 ID 均为 `2829da6f-bf11-466b-8fa4-1b5052f185a3`；原件 ID 为 `a3c4d853-f60f-4026-9c59-eddaa39fb47b` 和 `a630cebc-1aa5-4a3c-b2f1-a0368cd5c597`。测试结束已取消正式批次（200）。原件延迟物理删除、实际 OCR、网页完整交互和真实用户试用不在本次通过范围；V1 网页仍待接入这些直传与任务接口。

### 2026-09-08：V1 网页接入直传与准备任务

网页入口已接入私有文件直传、上传回执恢复和幂等任务提交，保留本地存储模式的旧入口。新增准备进度页和导入任务列表，提供轮询、取消、失败重试及重新登录入口。同步锁防止连续点击重复提交，取消后的旧轮询响应不会覆盖更新状态。

8 项客户端与真实提交函数测试通过，覆盖 6 MiB 图片、49 MiB PDF、500 文件上限、响应丢失恢复、超时及文件选择器重置。生产构建通过。此处为代码及受控测试结果，尚未完成部署后的浏览器交互验收，不替代实际 OCR 或完整用户旅程。证据：`/tmp/orbit-push-web-tests.log`、`/tmp/orbit-push-web-build.log`。

部署与交互复核：`a7d52221` 已在 `https://orbit-2x2itzjtj-liqys-projects-33c8ddec.vercel.app` 完成云端构建和 Preview 部署（`/tmp/orbit-v1-web-deploy.log`）。真实 React 进度组件在本机受控 API 中呈现首次 503 提示，自动恢复到 1/2 文件、6 页进度；通过 Tab 聚焦取消按钮并按 Return 取消。服务确认取消请求一次、迟到的旧进度响应已返回后，浏览器仍显示“已取消”。这是受控交互验证，截图仅为 390px 内容宽度，不是移动设备视口验收。重新访问固定 Preview 地址仍到 Vercel 登录保护页，完整线上浏览器流程尚未通过。

### 2026-09-08：识别不可用时的手工收录退路

Preview 环境变量名称检查确认没有 `DEEPSEEK_API_KEY`、`GEMINI_API_KEY` 或 `GOOGLE_API_KEY`，因此当前云端无法执行真实 OCR。V1 复核页现在允许失败项保留原图并进入手工录入；姓名必填，提交走独立的 `manual-entry` 端点。普通确认端点仍拒绝失败项，只有明确的手工录入请求才能把失败项和联系人写入同一事务，并同步修正批次的失败、处理和确认数量。重复联系人仍走原有复核，不会提前结算项目。

31 项 V1 API、页面、请求控制、worker、服务和事务回归通过，无跳过；其中隔离 PostgreSQL 测试证明手工联系人、项目状态和批次数量原子提交。类型检查保持 109 条既有诊断且本轮无新增，生产构建通过。证据：`/tmp/orbit-v1-manual-regression.log`、`/tmp/orbit-v1-manual-typecheck-final.log`、`/tmp/orbit-v1-manual-build.log`。该退路让用户在缺少识别凭证时仍可完成核心收录，但不等于真实 OCR 已验收。

首轮线上手工退路验收进一步暴露了 worker 根因：provider 为空时 V1 worker 不领取任何待处理项目，队列虽然持续保留，批次却永远停在 `processing`，所以手工入口无法到达。现在 worker 无论 provider 是否存在都会领取项目；未配置 provider 走与请求失败相同的受控失败计数，第二次尝试后变为 `failed` 并使批次进入 `ready_for_review`。这保留了真实 provider 的超时、结构校验和重试语义，也让无凭证环境最终可手工处理。

36 项关联回归通过，覆盖无 provider 的 pending→pending→failed 转换、队列续发、清理、API、页面和真实 PostgreSQL 手工收录事务；类型检查仍为 109 条既有诊断、无新增，生产构建通过。证据：`/tmp/orbit-v1-no-ocr-regression.log`、`/tmp/orbit-v1-no-ocr-typecheck.log`、`/tmp/orbit-v1-no-ocr-build.log`。修复后的线上闭环仍需重新部署后复核；失败的首轮线上观察记录为 `/tmp/orbit-v1-manual-live-smoke.log`，不能计作通过。

### 2026-09-08：空账号线上旅程与本机真实浏览器旅程

**Preview API 级全旅程（空账号）**：在部署 `orbit-d8y1whb7q`（提交 `5f46e260`）上用全新 `@example.invalid` 账号走完 `注册 201 → 登录 200 → 私有直传（预约 201 / 令牌 200 / PUT / 核验 200）→ 导入任务 202 → 任务 completed（27 s）→ 批次 processing → 项目两次受控尝试后 failed/OCR_PROVIDER_FAILED → 批次 ready_for_review（自建批次 37 s）→ 手工录入 200 → 联系人详情字段一致 → 草稿 → 完成批次 200 → 归档联系人 200`。这证明无 provider 时批次不再永久停在 processing。草稿分两路：联系人级 `POST /api/chat/assist/email-draft` 在 Preview 返回 422 `MODEL_API_KEY_MISSING`（Gemini 未配置，干净失败）；通用 `POST /api/message-drafts` 规则草稿返回 200。同一脚本在部署 `orbit-cbnyba82i`（提交 `aa2ef096`）上再次全程通过。匿名访问受保护路由均 401。任务在 `completed` 前会经过中间态 `ready`，脚本不得把它当终态。证据：`/tmp/orbit-fresh-account-live-journey.log`、`/tmp/orbit-fresh-account-live-journey-2.log`；脚本在会话 scratchpad `orbit-fresh-account-live-journey*.cjs`。账号文件未打印。

**本机真实浏览器旅程（Playwright + 系统 Chrome，`localhost:3000`，真实数据库与 DeepSeek）**：8/8 步通过，但范围有限：本机没有 `VERCEL=1`/私有 Blob，`GET /uploads` 返回 `directUpload:false`，浏览器走旧 multipart 入口，准备进度页 `/app/contacts/new/import/{jobId}` 本机不可达；本机没有队列消费者，批次由脚本外部调用 `runConfiguredCardQueueTick('v1')` 推进。`business-card-tiny.heic` 经真实 DeepSeek 返回 `extracted` 但所有字段为空，因此走的是"复核确认"而非"手工录入"分支。上传首个请求被拦截为网络失败时出现 `上传失败，请重试。` 与 `重试上传`，重试成功。联系人详情正确显示手填姓名/公司；`/api/chat/assist/email-draft` 本机约 15 s 返回可编辑主题与正文，创建对话线程 200。控制台除故意中断的请求外无错误。

浏览器旅程暴露的问题（未修）：
1. Today 不反映新联系人与已暂存草稿，仍显示"当前没有待你处理的事"。
2. 顶部导航把 Today 标为"日程"，页面眉标却是"今天"，Today 与日程概念混淆。
3. OCR 全空的项目以"已提取"呈现：无提示、缩略图空白、姓名为空也能点"确认并下一张"；`手工录入` 只对 `failed` 项目出现。
4. 移动端 390×844：姓名聚焦时"确认并下一张"位于 y≈870（屏外），滚动后 y≈594–638，若系统键盘约 300 px 仍会被遮住；无头 Chrome 不能模拟键盘，此为几何推断。
5. 注册页是带遮罩的模态卡（移动端底部抽屉，衬线 19–20 px 标题），Today 是全宽页面（无衬线 28 px 标题、完整导航），两页视觉不一致；390 px 下均无横向溢出。

截图与 `report.json` 在会话 scratchpad `browser-journey/`；日志 `/tmp/orbit-local-browser-journey.log`。Vercel 登录保护下的 Preview 浏览器旅程仍未完成：内置浏览器停在 vercel.com 登录页，Chrome 扩展未连接。

### 2026-09-09：Preview 真实浏览器旅程（内置浏览器，已过 Vercel 登录保护）

固定 Preview（`orbit-cbnyba82i`）现已能在内置浏览器直接打开 Orbit 登录页。用全新账号 `chrome-journey-*@example.invalid` 走 UI：

- 注册 `POST /api/auth/register` 201 → **不自动登录**，跳回登录页并提示"账号已创建。请登录后先完成通用档案"，需再输一次密码；登录后进入 Today，且没有任何"通用档案"引导出现，提示与实际不符。
- Today 桌面/移动（375×812）渲染正常，无控制台错误；导航仍把 Today 标为"日程"。
- **导入中心在未配置 OCR 时整体禁用名片上传**：`resolveBusinessCardCaptureAvailability` 只要 `DEEPSEEK/GEMINI/GOOGLE_API_KEY` 都缺就返回 `ocr_provider_unconfigured`，UI 显示"名片识别尚未连接 / 在此状态下不会上传图片，也不会创建联系人"，来源卡片 `disabled`。因此 `5f46e260` 的"OCR 未配置 → 失败可复核 → 手工录入"路径在浏览器里不可达，只有直接调 API 才能走到；两者是产品层面的矛盾，需要决定是配置 provider 还是放开"无 OCR 仅手工录入"模式。管理员提示只列了 `GEMINI_API_KEY or GOOGLE_API_KEY`，没有 `DEEPSEEK_API_KEY`，与解析逻辑不一致。
- 741 px 宽度下来源卡片标题逐字竖排（"名/片/扫/描"），栅格列过窄；375 px 与 1280 px 正常。
- 联系人 API 只有 GET，无法绕过上传另建联系人，所以联系人详情与草稿步骤在 Preview 浏览器里本轮无法验证。
- 登出后忘记密码：`POST /api/auth/password-reset/request` 202，页面显示中性的"申请已受理"文案（不泄露账号是否存在）；SMTP 仍 `535`，邮件不会真正送达。
- Chrome 扩展未连接（`list_connected_browsers` 为空），本轮全部在桌面应用内置浏览器完成；视口偶发 0×0，重新导航即可恢复。

### 2026-09-09：Preview 真实 OCR 浏览器旅程（DeepSeek 已配置）

用户在 Vercel Preview 配置 `DEEPSEEK_API_KEY` 后，从干净 worktree 重新部署（`orbit-351wu6svc`，固定地址已切换），在内置浏览器用同一空账号走完 UI：

- 导入中心不再显示门禁，出现"拍照扫描 / 上传图片 / 批量上传照片 / 上传 PDF"。
- 页内 canvas 绘制的中英双语合成名片（1400×800 JPEG，75 KB）注入"批量上传照片"文件框 → 预约 201 → 令牌 200 → 私有直传 → 导入任务 202 → 进度页 `/app/contacts/new/import/{id}` 显示"准备名片导入 0/1" → 26 秒后"文件准备完成 1/1"，提供"查看识别批次"按钮（不自动跳转）。
- DeepSeek 单张识别 3.3 秒（1109 入 / 275 出 token）：姓名"林 若曦"、罗马字"Ruoxi Lin"、职位、公司（中英合并为一个字段）、邮箱、电话、地址全部正确，`detectedLanguages: zh,en`。
- 复核页显示卡图缩略与预填字段 → 确认 200 → 联系人 `contact:business-card:d3fad8c9a7564f6ff8fa1ed0` → 完成批次 200。
- 联系人详情正确展示各字段与"名片扫描"标签；发现中文页面英文残留：头部"认识于 Business card confirmed by <账号邮箱>"、"下一步建议"正文"Review the live contact detail before taking action."、联系方式里"行业"值为字面量"relationship context"。
- "起草邮件"→ 收件箱面板 "AI 起草邮件" → `POST /api/chat/assist/email-draft` 422 `MODEL_API_KEY_MISSING`：`resolveProvider` 在没有 `ORBIT_AGENT_PROVIDER` 时固定选 Gemini，不看已配置的 DeepSeek 密钥；面板把它显示成"出了点问题，请重试"。手填主题正文后"创建对话（确认）" 200，线程出现在对话历史。
- Today 仍显示"当前没有待你处理的事"：Today 只读取 agent 账本 `awaiting_confirmation` 条目和待确认约见，用户自建的暂存草稿线程与新联系人不在其来源里（设计层面而非缺陷，见 `docs/superpowers/specs/2026-07-25-today-schedule-merge-design.md`）。

同日修复（见提交记录）：注册后自动登录、导入中心 741 px 竖排标题、管理员提示补 `DEEPSEEK_API_KEY`、复核页全空识别提示与空姓名禁用确认、移动端操作栏吸底、Today 眉标改为与导航一致的"日程"、AI provider 按已配置密钥自动选择、AI 起草未配置的诚实提示、联系人详情英文残留。

**同日复验（部署 `orbit-mju0tsmpp`，提交 `d8562bc1`）**：第二个空账号注册 201 后直接以 credentials 建立会话并落到 Today，不再经过登录页；741 px 导入中心变为单列、标题横排；联系人详情"认识来源"显示"名片扫描确认"、行业行隐藏、下一步建议本地化；健康检查 `/api/agent/operations/health` 报告 `provider: deepseek, selection: auto`。AI 起草仍失败但原因变为 `MODEL_REQUEST_FAILED: deepseek request timed out after 20000ms`——agent 文本路径默认开启 DeepSeek thinking 且只给 20 秒，与名片 OCR 的"禁用 thinking + 60 秒"不一致；提交 `f4f219f6` 让草稿服务默认 `deepseekThinking:false`、45 秒超时，路由 `maxDuration 60`。另发现：用其他账号打开不属于自己的联系人时，空态页整段是英文（"CONTACT DETAIL / No contact detail is available / Return to contacts list"），隔离正确但未本地化；"所在地 Unknown location" 占位已在同一提交按空处理。

**同日再验（部署 `orbit-81s91qvlq`，提交 `f4f219f6`）**：同一联系人上 `POST /api/chat/assist/email-draft` 200，DeepSeek（`deepseek-v4-flash`，thinking 禁用）6.7 秒返回中文主题与正文，`evidenceIds` 指向名片批次证据；收件箱面板点击"AI 起草邮件"后主题、正文自动填入，无错误提示。至此 Preview 真实浏览器核心旅程 `注册（自动登录）→ 上传名片 → 准备进度 → 真实 OCR → 复核确认 → 联系人详情 → AI 草稿 → 暂存线程` 全部走通；QA 联系人已归档。

**订正（2026-09-09 晚）**：本文件上面记的「SMTP 仍 535，邮件不会真正送达」已过时。Preview 换用 Gmail 应用专用密码后，密码找回真实收件已验收通过；诊断日志与排查顺序见 `web-readiness.md` 的「2026-09-09：密码找回真实收件打通」。另注意 `/tmp/orbit-check-deployed-smtp.cjs` 测的是 party-app 项目的凭证，不能用来判断 Orbit Preview 的 SMTP 状态。
