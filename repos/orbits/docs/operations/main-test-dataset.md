# 主测试账号与30人／10活动数据集

## 使用范围

主账号：**organizer@orbit.example.test**（测试主办方·林）。

[进入独立测试站](https://orbit-staging-20260917-ktlfo1sjb-liqys-projects-33c8ddec.vercel.app/app/account/login)。密码沿用本机私有配置中的 password，另行交付用户，不提交仓库。此入口有 Vercel 访问保护；不是 www.orbitailink.com 的旧 Production。App 必须连接同一个测试 API 才能看到这套数据，本次不擅自切换 App 或正式域名。

辅助账号 participant.a、participant.b、empty（均为 @orbit.example.test）保持原样，仅用于隔离和参与者测试；它们不是主数据所有者。30位联系人是主账号的私有CRM联系人，不是假造30个登录用户。

## 数据安排

- 主账号30位联系人：进行中9、待跟进9、长期维护6、归档6。进行中都有具体目标，待跟进／维护共15条开放任务，均有明确日期；归档关系没有开放任务。
- 新增人物围绕产品设计、软件与AI、品牌增长、社区创业四个领域，带姓名、机构、角色、能力／需求、沟通背景及人工来源证据。没有制造不存在的交换名片、已报名或实际出席记录。
- 主账号实际拥有10个 canonical 活动：保留原有1个发布活动和1个草稿，追加8个不同主题的发布活动。公开目录应为9个，草稿在主办方管理流程中，不以“目录未展示草稿”为缺数据。
- 新活动分布在扩充日期之后7至35天，时间18:30–20:30（Asia/Shanghai），容量12／16／20／24，均有运营配置、报名窗口、候补政策及 canonical 空报名基线。
- 没有生成虚假的匹配、签到、参与者资料或AI分析。首页“我的活动旅程”统计报名，不等于主办活动数量；报名后才应出现相应旅程。
- 保留原28条通用记录、4个账号和2个活动，不改旧记录、密码、工作区或主办方所有权。主工作区还有参与者A自己的1位联系人，因而全库联系人总计31，主账号视角才是30。

## 业务链与安全

追加联系人通过实际人工录入草稿→确认服务；新关系使用明确标记的合成基线，再经真实生命周期服务执行阶段选择，持久化任务、证据、审计及幂等回执。canonical读取是阶段权威，不用旧投影字段替代。

追加活动复用 canonical backfill／运营配置／报名政策／激活函数；逐一检查主办方可配置和参与者A不可配置。所有步骤同一事务，失败回滚。仅允许原独立测试Neon endpoint或专用本地库，绝不读取环境中的 DATABASE_URL。

默认 dry-run 完全在内存构建，不连云：

```sh
node --import tsx scripts/expand-staging.ts --config=/absolute/private/credentials.json
# 本地专用数据库 orbit_staging_20260917
node --import tsx scripts/expand-staging.ts --config=/absolute/private/credentials.json --apply
# 仅已核对额度的独立测试项目
node --import tsx scripts/expand-staging.ts --config=/absolute/private/credentials.json --cloud --apply
```

脚本要求初始主账号恰有2位联系人和2个活动；若已被用户改变就拒绝，不删数据凑数。成功后保存 rich-staging-30-10-v1 标记；重跑只报告现状，不补写或覆盖用户后续编辑。

## 免费额度与验证

执行前 Neon 项目页面显示：0.09 CU-hrs、存储34.6MB、月网络传出386.75kB。扩充预算最多400条通用记录、512KiB种子包、900次SQL、2MiB解码后返回JSON；后者不是 Neon 实际计费字节。无模型、邮件、自动worker或定时器；不删除必要证据来压低数量。

本地6/6验证通过：构建、身份／工作区／基线拒绝、真实PostgreSQL生命周期与主办方权限、30/10精确数量、重复执行不新增、事务回滚保持原库。含重复执行的本地验证722次SQL、1,347,518字节返回估计；种子331,240字节，最终225条通用记录。Web完整类型检查通过。

云端事务已成功提交：主账号30位联系人、10活动、225条通用记录、生命周期预检0问题；718次SQL，923,576字节返回JSON估计。密码经真实认证服务验证，未重置任何账号。原有记录逐条比较无变化，8个新活动的owner正例／参与者拒绝在事务内全部验证。

实际浏览器回读：

- 人脉页显示30位；待联系9、在推进15（包含长期维护）、归档6，与canonical阶段集合一致。
- 公开目录9场（8新增＋1原发布），原草稿不公开。
- 新活动STAGING-03运营后台可进入，时间门禁、每人推荐数1／每桌2及0报名／0生成等真实状态加载成功；没有按“生成匹配”按钮。
- 列表当前以东京时间显示19:30，运营配置按活动上海时区显示18:30，同一UTC时刻；没有为对齐文案擅自改期。

本次只验证数据、登录凭据、目录和主办方后台；未承诺匹配／AI／原生全流程已完成。专用验收页面验证后关闭，避免留存后台刷新；无模型调用、邮件、云端worker执行或Production修改。
