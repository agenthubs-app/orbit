[project]
name = "orbit-harness"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "claude-agent-sdk>=0.2.107",
  "playwright>=1.56.0",
  "pyyaml>=6.0.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
